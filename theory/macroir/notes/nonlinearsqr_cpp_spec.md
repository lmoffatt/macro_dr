# nonlinearsqr — C++ implementation spec (Option 2)

Companion to `nonlinearsqr_lse_plan.md`. Exact edit-level spec. Line numbers verified via reads + audits `w0gc29u72` / `wa86cqi0f` (2026-07-18); re-confirm at edit time. Split: **[USER]** = hot-path compute (Luciano writes); **[MECH]** = mechanical plumbing (Claude can do); **[VERIFY]** = must be resolved against live code during implementation.

## A. Flag surface — non-breaking overload (chosen over bool→int in place)
Precedent: `taylor_qdt_approximation` (bool DSL) maps to `uses_qdt_method<int>` internally (likelihood.h:35; the flag is qmodel_types.h:125-127). NOTE: that precedent proves only the INTERNAL bool→int mapping; the new part here is a SECOND DSL registration (`build_likelihood_function_with_family`), which is safe because push_function is keyed by NAME. Extend the idiom to 3 values without breaking scripts.

- **[MECH]** New constexpr flag `uses_nonlinear_sqr_aproximation`… NO. Family is ONE 3-valued selector: add `uses_family_aproximation<int>` {macro=0,micro=1,nonlinearsqr=2} in qmodel_types.h (mirror `uses_averaging_aproximation<int>` class @94-96 + its `_value` @97-98 + the `_c` concept @134-135 — NOT @156, which is the MICRO concept). KEEP `uses_micro_aproximation` during transition (option (a) below — do NOT retire it; the visit sites keep reading micro_type).
- **[MECH]** Core builder `build_likelihood_function_with_family(model, adaptive, recursive, averaging, variance, taylor_vc, **int family**, taylor_qdt)` in likelihood.h — same body as current build_likelihood_function (24-108) but the family selector is int, and it merges a **4th branch** (the nonlinearsqr Likelihood_Model_regular, §B). Register in command_manager.cpp with a NEW `push_function("build_likelihood_function_with_family", …)` (slot 7 type `int`, name `family_approximation`).
- **[MECH]** Keep `build_likelihood_function(…, bool micro_approximation, …)` (likelihood.h:24) as a THIN WRAPPER: `return build_likelihood_function_with_family(model, adaptive, recursive, averaging, variance, taylor_vc, micro_approximation ? 1 : 0, taylor_qdt);`. Registration @command_manager.cpp:1014 unchanged → every existing script keeps working, no sweep.
- **[MECH]** `likelihood_algorithm_type` (likelihood.h:110) = `decltype(build_likelihood_function_with_family(… , /*family*/0, …))` so the variant includes all 4 branches (macro / macro-taylor / micro / nonlinearsqr). Both builders return this same widened variant.
- **[VERIFY-fam]** Confirm the visit sites read `micro_type::value`; either (a) keep `uses_micro_aproximation` as a derived alias on the model so those sites are untouched and add a NEW `family_type`/`nonlinear_sqr_type` alias for the nonlinearsqr arm, or (b) replace micro_type with family_type at the ~7 sites. (a) is less churn. Read likelihood.cpp:761… and Likelihood_Model_constexpr (qmodel.h:7789, alias `micro_type` @7797) before deciding.

## B. The nonlinearsqr Likelihood_Model_regular branch (in the core builder)
Mirror the macro-non-taylor branch (likelihood.h:43-62) with these domains:
- adaptive{false}, recursive{**false**}, averaging{**§F value**}, taylor_vc{false}, qdt{0}, family{2}.
- variance: **accept BOTH** `constexpr_Var_domain<bool, uses_variance_aproximation, false, true>` and IGNORE it in compute. **[VAR-BLOCK fix]** — the live .macroir hardcode `variance=true` and it is not injectable; a `{false}`-only domain would make merge_Maybe_variant find no match → hard run abort. Accepting both (2 identical instantiations) dodges it with zero dispatcher edits.
- **[MECH]** append this branch to the merge in BOTH the core builder and (transitively, via the wrapper) the existing one.

## C. Compute — lean LSE driver [USER]
New `nonlinearsqr_logLikelihood<…>` (+ derivative overload) near `log_Likelihood` (qmodel.h:6693) / the top-level entrypoints (8158). Own fold; do NOT thread family through the macro fold.
- Per interval: reuse `calc_Qdt` (shared, algorithm-agnostic). Propagate P_mean only: `r_P_mean = to_Probability(p_P_mean·t_P)` (pattern @qmodel.h:4408, P_Cov-independent — verified). Compute μ_i = `N·(P_mean·gmean_i)+baseline` (pattern @4395). NO P_Cov, NO gSg, NO y_var, NO Kalman.
- New per-interval `sqr_dif_i = (y_i − μ_i)²`. In the derivative path μ_i carries dμ_i.
- **NaN guard [blocker]:** at NaN y (pre-agonist prefix), contribute 0 and do NOT increment n (mirror calculate_logL @4077 which returns 0 for NaN). Else SSE=NaN kills every recording.
- Accumulate additively: SSE, dSSE = −2·Σ(y−μ)dμ, Σdμ dμᵀ, and **n = count of finite y** (own counter; vlogL is NOT valid n — counts NaN/gap steps).
- **Finalize** (once, post-fold): σ̂²=SSE/n; logL = lgamma(n/2) − (n/2)·log(π) − (n/2)·log(SSE) [Jeffreys marginal, §plan]; score = −(n/(2·SSE))·dSSE; Fisher = (n/SSE)·Σdμ dμᵀ. Guard SSE→0.
- **AD caveat [USER flag — Fisher ≠ AD Hessian]:** the score comes for free from FIRST-order AD if SSE rides the fold as a `Derivative<>` (logL=−(n/2)log(SSE_deriv) auto-carries it). Do NOT get the Fisher from `derivative(derivative(logL))` (2nd-order AD) — that is the TRUE Hessian −∂²ℓ_p = (n/SSE)JᵀJ − (n/SSE)Σr_i∂²μ_i − (2n/SSE²)(Jᵀr)(Jᵀr)ᵀ (last two terms nonzero OFF the optimum). Assemble the GN block explicitly: `Gaussian_Fisher_Information = (n/primitive(SSE))·Σ sqr_X<true>(derivative(μ_i))` — mirror macro's `sqr_X<true>(derivative(get<y_mean>))` @6096 but (i) NO /y_var, (ii) NO (dy_var)² term, (iii) ONE global 1/σ̂²=n/primitive(SSE) applied post-fold (macro divides per-step by its own y_var). Use **primitive(SSE)** for the prefactor — do NOT let AD differentiate it.
- **[VERIFY-grad] — RESOLVED (confirmed):** the MLE fit funnels calc_MLE_per_group_of_replicates → evaluate_combined_group_as_dlogPs → `evaluate_likelihood_as_dlogPs_impl` (likelihood.cpp) → `calculate_mdlikelihood` (visit @787), which packs `logL(primitive(get<logL>))`, `Grad(derivative(get<logL>), p)`, `get<Gaussian_Fisher_Information>` into a `dlogPs`. So Grad IS `derivative(get<logL>)` — the finalize MUST build `Derivative<logL>` carrying value=logL AND derivative=score together, or every Newton step is garbage. NAME TRAP: emit the macrodr:: `dMacro_State_Hessian_minimal` (qmodel_types.h:1469, Derivative-based, no explicit Grad slot), NOT the unrelated global one (distributions.h:1139, which lists Grad); name lookup disambiguates so both compile.
- **[VERIFY-state] — RESOLVED (6 state types instantiated @qmodel.h:8158+):** ROUTE 2, GUARD 4. Route to LSE: `Macro_State_reg` (value/logL) and the Derivative-based `dMacro_State_Hessian_minimal` (MLE — the ONLY two the `_G` deliverable needs). GUARD (explicit error) the four Evolution-carrying states — Macro_State_Ev_predictions (8210/8280), Macro_State_Ev_diagnostic (8298/8364), dMacro_State_Ev_gradient_all (8324, = calc_dlikelihood_predictions), dMacro_State_Ev_detailed (8340). The lean no-Evolution driver CANNOT produce an Evolution, so those are guards, NOT finalizes. A missed ROUTED type silently returns raw SSE in the logL slot; a missed GUARD silently macro-fallthroughs.

## D. sqr_dif slot [MECH]
- qmodel_types.h:~1187 — `class sqr_dif : public var::Var<sqr_dif,double>{friend std::string className(sqr_dif){return "sqr_dif";}};` beside Chi2.
- Append to `Algo_State_Dynamic_Space` (@~1235) AND `Algo_State_space` (@~1269) AND the two hand-written re-enumerations inside `class Algo_State` (1275-1293): the `base_type` alias (~1283, a full Vector_Space re-listing) and the `Algo_State(const Algo_State_Dynamic&)` ctor initializer (~1290, e.g. `get<Chi2>(p())`). FOUR coordinated edits, not one — appending to Algo_State_space alone will NOT compile (base_type + ctor must carry the new slot; Algo_State_Dynamic_Space must carry it for `get<sqr_dif>(p())` to resolve). Do NOT repurpose Chi2 (dy² for LSE rows vs dy²/y_var for macro rows — footgun). NOTE: only needed if a per-interval dump is wanted; the `_G` MLE deliverable (cloud/pool) does NOT require it.

## E. Visitor arms — used functions only [MECH]+[USER]
PRODUCTION fig3 = `figure_3_mle_G.macroir` (NOT fig3_mle; drops numerical Fisher). Used by fig2 + `_G` (grep-verified): `calc_MLE_per_group_of_replicates` (the LSE fit — funnels to `calculate_mdlikelihood` @787), `calc_dlikelihood_predictions`, `likelihood_derivative_basic_diagnostics` (NON-`_paired`; `_paired` has no gaussian overload), `calc_empirical_distortion_gaussian`, `build_gauss_newton_options`, `get_parameters_mean`, `write_csv`. `calc_numerical_fisher_information` = fig2 + OLD fig3_mle only. The raw `calculate_mlikelihood/mdlikelihood/mdiff` and the raw-ModelPtr overloads are NOT called by figures.
- At each std::visit site in likelihood.cpp (761/787/813/834/854/890/932), add the nonlinearsqr arm:
  - **MLE / likelihood-value / cloud path** (what fig2/fig3 LSE needs — the @787 `calculate_mdlikelihood` route is the load-bearing one): route to `nonlinearsqr_logLikelihood` (§C). [USER for the numerics, MECH for the dispatch]
  - **Distortion / per-interval-prediction functions** (`calc_empirical_distortion(_gaussian)` = per-step GFI battery → garbage; per-interval predictions y_var/r_std → NaN): explicit **`error("unsupported for family==nonlinearsqr")`** guard, NOT a silent macro fallthrough. WARNING (plan [G-ABORT]): a hard error aborts the unconditional `_G` script at Stage 4 — either ship a reduced `figure_3_mle_LSE.macroir` or make the guard a graceful empty-result skip.
  - **`calc_numerical_fisher_information` (fig2's LSE Fisher ellipse — ROUTE, do NOT guard):** it does no visit of its own; it central-differences `derivative(get<logL>)` via `calculate_mdlikelihood` (@787, verified likelihood.cpp:1032 — SCALAR, NO Evolution). Once @787 is routed for LSE it returns a VALID FD of the LSE Fisher (n/SSE)JᵀJ with ZERO extra work → it IS fig2's LSE deliverable, not a guarded function. ([3] correction: the earlier "guard the Gaussian functions" over-guarded this; fig2's LSE column = this routed FD Fisher + the MLE cloud from fig3_G. Moot for `_G`, which drops it.)
- `diff_logLikelihood` (qmodel.h:8200/8257, FD Gaussian FIM): guard unsupported for nonlinearsqr (or later, LSE FD-FIM = (n/SSE)·Jᵀμ Jμ). Analytic dlogLikelihood is the primary path.

## F. Config / open decisions
- **[VERIFY-avg]** averaging value for μ: pin one (candidate 1). av=0 interleaves a P_mean half-step with the P_Cov update (qmodel.h:4374-4383) — avoid. Set the branch domain (§B) and the fig2/fig3 axis to that value.
- **Amplitude ridge:** fix `unitary_current` (scheme_CO idx **2**, NOT idx 4/9 — those were scheme_1's 10-param order) to `Fixed` — NOT Num_ch_mean (the swept + plotted axis). Do it in the `.macroir` create_parameters ("Log10"→"Fixed"), NOT the scheme par CSV (the dispatcher-injected .macroir overrides it). Fixed_Tr @parameters.h:170; isfixed() drops it from the free/derivative set (verified).
- **Pure-variance flat directions (blocker):** the LSE mean (r_y_mean @4395) has NO noise term, so `Current_Noise` (scheme_CO idx 3, FREE at figure_3_mle_G.macroir:48) and Pink/Proportional have identically-zero score/Fisher rows → singular θ-Fisher. Set ALL of them `Fixed`. (Fixing Pink/Proportional removes only y-proportional heteroscedasticity, not the intrinsic gating variance N·gSg @4398 — that misspecification is what the exact-sim validity test measures.)

## G. Non-CLI C++ call sites [MECH]
`tests/optimization/test_gauss_newton.cpp:126`, `tests/macroir/test_dlogLikelihood_equivalence.cpp:83` pass `/*micro=*/false` → still compile via the bool wrapper (unchanged). No edit needed; update the comment if the wrapper is later retired.

## H. Per-interval Evolution arm — fig 4 (cumulative J_T/F_T) + fig 5 (distortion). [USER]+[MECH]
**SCOPE REVERSAL (2026-07-19, Luciano):** LSE is a FULL member of fig 2/3/4/5. The old "EXCLUDE from fig 4/5 — no per-step GFI" was WRONG: it confused the macro *implementation* (per-step GFI divides by y_var) with the *concept* (per-interval score/Fisher contributions), which LSE has — score and Fisher are both sums over intervals.

**CENTRAL RESULT (3 independent mappings converged; numerically verified 1.4e-13 / 2.3e-14):** the LSE *is* the Gaussian model with a CONSTANT plug-in variance and a DEAD variance channel. Set, per interval t, `y_var_t ≡ σ̂² = SSE/n` (the same scalar at every t) with an identically ZERO derivative. Then the EXISTING per-step formula `XXT(dμ)/v + XXT(dv)/(2v²)` collapses to `(n/SSE)·dμ_t dμ_tᵀ` = exactly the LSE per-interval Fisher, and the variance channel switches off by the zero derivative. **Consequence: NOTHING downstream changes** — no new type/Var, no writer change, no R formula change, no battery/distortion edit. `dMacro_State_Ev_gradient_all` is reused VERBATIM; the y_var-dependent code is *fed* LSE-correct values, not rewritten.

Per-interval `gradient_all_element` (qmodel_types.h:1454-1460 + micro_types.h:409-411):
- `Derivative<y_mean>` = μ_t with dμ_t. Already in hand (qmodel.h:7421-7422, 7427).
- `Derivative<y_var>` = σ̂², derivative **explicit p-vector of ZEROS via `var::init_with_dx<DX>(sigma2, dx)`**. NOT default-constructed: a 0×0 derivative emits no `y_var/derivative` rows and fig4's inner merge on dv **silently drops every row** (empty panel, not an error). Zero-derivative is exact, not an approximation — same plug-in discipline as the finalize (§C AD caveat).
- `Derivative<logL>`: derivative = `(n/SSE)·(y_t−μ_t)·dμ_t` (the per-interval score). Σ_t == `derivative(get<logL>)` of the scalar finalize (verified 2.3e-14) — **assert in a smoke test**. PRIMITIVE has no principled value (the marginal logL is global, does not decompose) → write 0 and document; fig4 filters `calculus=="derivative"` so it never reads it.
- `Derivative<r_std>` = (y_t−μ_t)/σ̂. **TRAP: Σ_t r_std_t² = SSE/σ̂² = n IDENTICALLY** → r̄²_std ≡ 1 is a TAUTOLOGY for LSE, not a calibration result. Flag it in any panel reading r2_std or it silently reads "perfectly calibrated".
- NaN-y interval: dμ_t = ZEROS, score 0 (the driver skips NaN in Fisher_acc @7423, so Σ_t F_t only matches the total GFI if NaN intervals contribute nothing).
- `trust_coefficient` = 1.0 (no clipping in LSE); taylor_* = 0. Top level: ZERO the `Derivative<Patch_State>` P_Cov before returning (LSE never touches it; today it would dump uninitialised garbage).

**Driver change:** `nonlinearsqr_logLikelihood` (qmodel.h:7367-7501) grows an Evolution arm, **TWO-PASS** (the one genuine structural difference vs macro: the (n/SSE) prefactor is unknown until the fold ends). Pass 1 = the existing loop, retaining per-interval (μ_t, dμ_t, y_t). Pass 2 = cheap O(T) POST-pass after the SSE>0 guard: σ̂²=SSE/n once, then fill the Evolution. **Do NOT re-run calc_Qdt.** The scalar finalize is unchanged and stays the MLE path.

**Routing — ONE guard flips.** `calculate_mdlikelihood_predictions_visit`, src/core/likelihood.cpp:928-929 (`error_message("unsupported for family==nonlinearsqr")`) → route to a new `calculate_mdlikelihood_predictions_nonlinearsqr_impl` (sibling of `calculate_mdlikelihood_nonlinearsqr_impl` @650-664: load_dmodel + Derivative-capable re-wrap), dispatching to an Evolution-producing `nonlinearsqr_dlogLikelihoodPredictions`. **That single site is the SOLE producer of `dMacro_State_Ev_gradient_all` and unlocks BOTH fig4 and fig5** (funnels: single @990-1002, batched @1013-1055, DSL `calc_dlikelihood_predictions` @command_manager.cpp:1106-1112). The other four guards (846 mdiff, 869 predictions, 891 diagnostics, 973 detailed) STAY guarded — none feeds fig4/fig5.

**Caveats to carry into the figures (narrative, not bugs):**
- **Martingale caveat (fig 4):** the macro J_T/F_T→1 test rests on the per-step score being a martingale-difference sequence. The LSE per-interval scores share the GLOBAL (n/SSE) estimated from the SAME recording, so they are NOT a martingale difference sequence — a rank-1 common-mode enters the across-sim covariance. The identity is *conditionally* exact; state it, do not plot it silently as if it were the macro test.
- **G_b means something different:** for macro, G_b vs F_b = moment-matched Gaussian FIM vs numerical truth; for LSE, G_b = (n/SSE)JᵀJ is the Gauss-Newton curvature. GIDM = Ḡ^{-1/2}·J·Ḡ^{-1/2} is the honest F-vs-J sandwich; identity ⇔ homoscedastic-white holds (it does not, by gating) → the gap IS the LSE result.
- **fig5 provenance seam:** figure_5_master.Rmd reads the numeric-Fisher `_battery_*`, but LSE can only produce the gaussian `_battery_*_G` (the `_paired` overload needs a numerical Fisher).
- **UNRESOLVED:** which fig4 is the paper's (paper/figure_4.Rmd per-step vs archive/figure_4_score.Rmd cumulative). And whether the fig4 LSE run keeps all 6 params free (fig4 never inverts the Fisher, so it is safe and preserves param-index alignment) vs the "fix the flat directions" rule needed for the MLE.

## Implementation order
1. [MECH] qmodel_types.h family flag (BESIDE micro) + sqr_dif slot (only if a per-interval dump is wanted — 4 coordinated Algo_State edits, §D).
2. [MECH] likelihood.h core builder `build_likelihood_function_with_family` + bool wrapper + widened alias + nonlinearsqr branch (variance accepts both, §B); command_manager.cpp register `_with_family`.
3. [USER] nonlinearsqr_logLikelihood + finalize (route 2 states, guard 4 — [VERIFY-grad]/[VERIFY-state] now RESOLVED above).
4. [MECH]+[USER] visitor arms (route the MLE @787; guard the rest — FD-Fisher guard at the FUNCTION level, §E).
5. [config] amplitude (unitary_current) + Current_Noise + Pink/Proportional set Fixed in the fig .macroir; Num_ch stays FREE. PREREQUISITE for the fit check — do it with the compile step, not after.
6. Compile (Luciano). Iterate on errors. Validate: single-recording logL vs R; MLE θ̂ vs nls; local FD of the score at a NON-optimal θ.
7. [MECH] switch fig2 + a NEW reduced `figure_3_mle_LSE.macroir` (or a graceful-skip `_G`, per plan [G-ABORT]) + dispatchers to `_with_family` + `family=indexed_int_by([…,2])` + an `--algo_family_approximation` injection; add the `nonlinearsqr` axis label. (Only after the C++ compiles.)
