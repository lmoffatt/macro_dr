\section*{Introduction}

% Owner: papers/1_method/approach.md (the framing) and papers/_program/program.md 1 (merged macro
% paper, least squares as the comparison anchor).
%
% ============================================================================================
% OUTSIDE-READ PASS 2026-08-13. A fourth outside read (GPT, on the built PDF of that morning,
% 69 pages) returned a 54-item cutting list. What was taken here: one paragraph deleted and three
% compressed, 1,310 counted words -> see the log at 08_length_plan.md. Each site carries its own
% note below and each note says what was NOT taken and why, so the trade is not re-litigated.
% THE THREE THINGS THE LIST ASKED FOR AND DID NOT GET, all in this section:
%  - the closing paragraph ("Making the measurement takes an ensemble"), which it would cut to its
%    first two sentences. That paragraph carries the narrow-novelty concession to
%    anderson1973voltage, and the v6 ledger above lists that concession among the protected items.
%    Untouched.
%  - the goodness-of-fit pre-emption, which it would compress. It is v7 restoration (a) and the
%    argument for restoring it against one outside read is in this header already.
%  - the volume-term clause. 05_discussion.tex's own header records that the volume and
%    effective-sample formulas rolled out of that section and survive HERE, in prose. Cutting it
%    would take them out of the manuscript.
% ============================================================================================
%
% ============================================================================================
% BODY REPLACED 2026-08-10 BY tmp/intro_v7.tex. 1771 countable words -> 1305, measured the same
% way (comments stripped, cite keys stripped). eLife reference points from
% _program/elife_main_text_length_survey.md:50: median Introduction 873, p75 1079, p90 1324. This
% section now sits just under p90; the predecessor was at 1771.
% PREDECESSOR KEPT VERBATIM at ../archives/01_introduction_SUPERSEDED_20260810.tex. Its own header
% record is reproduced below under "PREDECESSOR RECORD" and is still the authority for anything
% that happened before today.
%
% WHY v7 AND NOT v6, v8 OR v9. All four were written and measured on 2026-08-10 (v6 1158, v7 1305,
% v8 1038, v9 1097; drafts kept in tmp/). v6 is dominated by v7: it keeps the tau_int offer whole
% and drops the analogue-filter guard, leaving the paper's one actionable offer unqualified on an
% oversampled record, and it is the only version that loses the lower-bound scope caveat. v8 and v9
% each buy about 250 words by sacrificing something v7 keeps, v8 the field context and v9 the
% results preview with the tau_int offer. v7 is the complete version and is what ships.
%
% CITATION AUDIT, run before the swap and the reason it was safe. Two keys leave this section:
%   delcore2025parameter  -> homes at 05_discussion.tex:183 and :214. Not orphaned.
%   lei2020considering    -> home at 05_discussion.tex:77. Not orphaned.
% One key arrives: moffatthume2007responses, already in biblio.bib and already used at
% 05_discussion.tex:289. NOTE THE BOOKEND: the Discussion closes on the flip-state delay and this
% section now opens on it. That is deliberate as a frame, but the two sentences must not drift into
% saying different things; the Discussion adds "other groups later confirmed it" and this one does
% not. Every other key in the section is unchanged.
% THE QUANTITATIVE ORDERING CLAIM leaves this section unharmed: "one to three decades of noise"
% survives at 00_abstract.tex:2, 04_results.tex:600-602 and 05_discussion.tex:216. v7 states the
% ordering here without the number, which is why the figure_7.html src note moved to the paragraph
% that now carries it.
%
% DISCHARGED BY THIS SWAP, so they are not looked for again:
%  - The STILL-OWED item "each sample equals the last plus noise needs its derivation or a
%    naive-forecast citation". That sentence is not in v7. The debt is closed AT THIS SITE only; if
%    the limiting-case argument is ever restored anywhere, the debt comes back with it.
%  - The length-pass shortfall of 521 words declared on 2026-08-09. The target was ~1250 and the
%    section is now 1305.
%
% OWED AFTER THIS SWAP, and this is the whole list:
%  - THE MECHANISM DEFENCE. That the rate constants are properties of the protein, comparable
%    across preparations, and that a correlation function fitted freely to the residuals is
%    neither. It stood at the predecessor's fourth paragraph and it is in NO other section. v7 drops
%    it deliberately, on the ground that this paper audits members of a likelihood family already in
%    use rather than choosing the family, so the argument is Discussion material. IT HAS TO LAND IN
%    05_discussion.tex. It has not landed yet.
%  - THE GLS/ARMA HALF of the two-alternatives paragraph, which is the same debt and should move
%    with it: the answer to "why not just whiten the residuals". lei2020considering is not orphaned,
%    but the argument it carried here is gone from the manuscript.
% NOT OWED, CHECKED 2026-08-10 rather than assumed. "What the classical arm of this paper is, said
% plainly and once: least squares on the deterministic mean current with the unitary current held
% fixed" was an inline note on the predecessor's third paragraph, and v7 does not say it here.
% Methods carries it in full and in its own subsection: 06_methods.tex:513 defines the two
% least-squares configurations, and :586-589 states that the four-parameter configuration, the one
% behind every grid cell, fixes the unitary current at 1 and Current_Noise at the cell value at
% their simulated values. Methods is now the only statement of it, and it is a sufficient one.
% ============================================================================================
%
% ============================ PROVENANCE OF THE BODY BELOW (v5 -> v6 -> v7) ===================
% LINE NUMBERS INSIDE THIS BLOCK ARE PRE-SWAP. It was written while the body lived at
% tmp/intro_v7.tex and the shipped section was the predecessor, so every "01_introduction.tex:NNN"
% in it points into ../archives/01_introduction_SUPERSEDED_20260810.tex, not into this file. The one
% that matters, "01_introduction.tex:317-319 records [BESSEL-BOUND] as closed here", is now the
% [BESSEL-BOUND] CLOSED note reattached to the closing paragraph below. The canonical note pointer
% is right and was verified today: 08_appendix_derivation.tex is the canonical [BESSEL-BOUND] site as of 2026-08-12.
% V7, 2026-08-10. v6 plus four restorations, after three outside reads (GPT, DeepSeek, Gemini) all
% took v6 as the base. v6 stays at tmp/intro_v6.tex, v5 at tmp/intro_v5.tex. The block below is v6's
% record; the v7 changes are at the foot of it.
%
% V6, 2026-08-10. v5 cut by REMOVING CONTENT, at Luciano's instruction
% (1,100 words). The hook, the spine and the five grafts are unchanged
% and every factual repair recorded in v5's header is carried over unaltered. Read v5's header for
% the decision record; this one records only the loss.
%
% CITATIONS ARE FREE IN THE COUNT and ELEVEN KEYS HAVE NO OTHER HOME IN THE MANUSCRIPT BODY:
% katz1970membrane, katz1972statistical, anderson1973voltage, conti1980conductance,
% sigworth1981covariance, neher1977conductance, hodgkin1952quantitative, celentano2004use,
% clerx2019four, owen2025ionbench, stepanyuk2014maximum (measured 2026-08-10 across 0*.tex, body
% text only). So all 22 keys are kept and the whole reduction comes out of prose. Do not "simplify"
% a citation block here without re-running that measurement.
%
% THE LOSS LEDGER, 1,594 -> 1157 body words as measured. What is GONE, not compressed:
%  1. THE GOODNESS-OF-FIT PRE-EMPTION, ~45 words: "an approximation can reproduce the mean current
%     closely and still misreport the information by more than an order of magnitude, and comparing
%     two approximations against each other does not answer it either, because both may be wrong."
%     It is the answer to the two objections a methods referee raises first, and it exists NOWHERE
%     ELSE in the manuscript. Highest-value thing on this list; restore it first if room appears.
%  2. THE MECHANISM DEFENCE, ~45 words: that the rate constants are properties of the protein,
%     comparable across preparations, and that a correlation function fitted freely to the residuals
%     is neither. This is the section's only argument for a mechanistic likelihood over an empirical
%     one, and 05_discussion.tex does not make it. GONE, and it is the one loss that changes what the
%     section argues rather than how much it says.
%  3. THE GLS/ARMA HALF of the alternatives, ~55 words. lei2020considering survives at
%     05_discussion.tex:77 for a different claim, so no key is orphaned, but the answer to "why not
%     just whiten the residuals" leaves the manuscript. The fluctuation-analysis disclaimer STAYS as
%     one sentence, because stepanyuk2014maximum has no other home and because it is a
%     non-competition caveat.
%  4. THE ANALOGUE-FILTER CLAUSE in the closing, ~30 words. THIS REOPENS [BESSEL-BOUND] AT THIS SITE:
%     01_introduction.tex:317-319 records it as closed here on 2026-08-07 by exactly those fifteen
%     words, on the argument that the paper's one offer to a reader with data in hand is misleading
%     on an oversampled record. Canonical note at 08_appendix_derivation.tex (moved there 2026-08-12). If v6 ships, the guard has
%     to land in Methods or the Discussion.
%  5. delcore2025parameter's cost sentence, ~15 words (two homes at 05_discussion.tex, no loss).
%  6. THE LOWER-BOUND CAVEAT, ~28 words: "which isolates the error the approximation itself commits
%     and is the most favourable case for a Gaussian treatment of the occupancy, so what is reported
%     is a lower bound for anything richer." THIS IS A SCOPE CAVEAT AND IT IS THE FIRST ONE CUT in
%     any version of this section, taken only because 1,100 could not be reached without it. It is
%     the cheapest thing to restore (one clause) and the first that should be. Its half-sentence
%     survives at 05_discussion.tex; the "lower bound" reading does not.
%  7. The two-sided standoff sentence ("the field is choosing between a method whose residual can be
%     read and whose interval is wrong, and one whose interval may be right"), ~35 words. It is
%     spine B stated outright; the paragraph now implies it.
% Everything else on this page is compression, not removal.
%
% WHAT WAS NOT TOUCHED. One scope caveat WAS cut, item 6 above, and it is flagged there. Still
% present, and none of these moved: the simulation-study scope, the lower bound for richer schemes,
% the evidence disclaimer, the construction concession, the anti-promotion clause, the narrow-novelty
% concession to anderson1973voltage, and the non-competition disclaimer. If 1,100 has to become 900,
% those are what is left, and cutting any of them is a different kind of decision from this one.
%
% ==========================  V7 CHANGES, 2026-08-10  ==========================================
% THE THREE OUTSIDE READS AGREE ON THE BASE (v6) AND SPLIT ON WHAT TO PUT BACK. GPT would restore
% ONLY the lower-bound sentence and unpack the two-failures paragraph, and says in terms that it
% would NOT restore the goodness-of-fit pre-emption or the mechanism defence, on the ground that
% anticipating objections makes the section sound less confident. DeepSeek marks BOTH as critical.
% Gemini would ship v6 unchanged and is wrong on two checkable points: it says v6 "retains the
% necessary scope caveats" (v6 cut the lower bound, ledger item 6, verified: zero occurrences of
% "lower bound") and it reports v6 at ~1,100 against a 1,100 target (measured 1,157, plan target
% 1,200). DeepSeek is also wrong on one: it says v6 "reverts to" the sentence "Two design variables
% ... enter the answer and through nothing else". That sentence is not in v6, at all. Its conclusion
% about the filter survives for a different reason, given in item (c) below.
%
% RESTORED HERE, and why the split was resolved this way:
%  (a) THE GOODNESS-OF-FIT PRE-EMPTION, ~45 words, against GPT's advice. It is not defensive prose
%      and it is not anticipating a referee: it is the PREMISE. If a good fit established that the
%      reported information was right there would be no paper, and without these words a reader can
%      reach the design paragraph still thinking a good fit settles it. Placed immediately after the
%      departure sentence, which is where DeepSeek puts it too.
%  (b) THE LOWER-BOUND CAVEAT, ~28 words. Unanimous, and it was ledger item 6, the only scope caveat
%      this section has ever lost. Item 6 is now discharged.
%  (c) THE ANALOGUE-FILTER CLAUSE, ~30 words. GPT did not notice it. The exposure is real and it is
%      NOT the one DeepSeek describes: v6 kept the tau_int offer whole ("it costs nothing, since the
%      fit was going to be made anyway") and dropped only its guard, so the paper's one actionable
%      offer stood unqualified on a record sampled faster than its own bandwidth. Restoring the
%      clause re-closes [BESSEL-BOUND] at this site (canonical note 08_appendix_derivation.tex).
%      Ledger item 4 is discharged.
%  (d) THE TWO-FAILURES PARAGRAPH IS UNPACKED, ~35 words, GPT's best point and uncontested. It now
%      names the distinction before using it (what a method gets wrong against what it says about
%      how wrong it is) instead of asking the reader to assemble it from the bias contrast.
%
% NOT RESTORED, deliberately: THE MECHANISM DEFENCE (ledger item 2). GPT is right that it answers a
% question this paper does not ask. The paper does not choose a likelihood family, it audits members
% of one already in use, so "why mechanistic and not ARMA" is Discussion material.
% *** DEBT, and it is the one thing v7 leaves owed: that argument exists NOWHERE ELSE in the
% manuscript. It has to LAND in 05_discussion.tex, not merely leave here. Ledger items 2 and 3 (the
% GLS/ARMA half) are the same debt and should move together. ***
%
% NOT DONE: GPT's optional fifth change, one more sentence before the closing on why the result
% matters biologically. The opening paragraph already carries it, and repeating it at the end is
% where this section starts sounding like a second abstract (00_abstract.tex note 1f(iv)).

% ================================ PREDECESSOR RECORD =========================================
% Everything below this line described the version archived at
% ../archives/01_introduction_SUPERSEDED_20260810.tex. Kept verbatim: it is the decision record
% for how this section got here, and several of its notes are still live (marked where they are
% reattached to the paragraphs below).
%
% Owner: papers/1_method/approach.md (the framing) and papers/_program/program.md §1 (merged macro
% paper, least squares as the comparison anchor).
%
% LENGTH PASS 2026-08-09. 2025 countable words -> 1771. Target for this section was ~1250 and it is
% NOT met, by 521 words; the shortfall is declared rather than taken out of protected material.
% What binds: six paragraphs were flagged untouchable by the prior triage (the two opening
% paragraphs, the pair on what a least-squares residual can be read for and what modelling the
% correlation takes away, the P2X2 paragraph, the falsifiable ordering, the narrow-novelty claim)
% and they are 704 words on their own. Adding the protected scope clause and the protected
% non-stationary-fluctuation-analysis concession puts the floor near 1000 before any of the
% argument is written. Cutting to 1250 would have required deleting the lineage or the score
% apparatus outright.
% WHAT CAME OUT, all of it verified to survive somewhere else except where marked:
%  - the error-bar conversion table (1.15 -> 7%, 2 -> 41%, 4 -> 95% becomes ~68%), which is stated
%    verbatim at 02_framework.tex and belongs there.
%  - "held at fixed spectral density so that shortening the interval raises the noise in each
%    sample and leaves the noise per unit bandwidth unchanged" from the design paragraph. Methods
%    owns it: 06_methods.tex:760-765 defines Current_Noise as a white-noise power spectral density
%    reaching the likelihood only through e_t = Current_Noise/Delta_t. CONSEQUENCE FOR THE BESSEL
%    NOTE below: the clause it calls "the clause at risk" no longer exists here, so the [OPEN]
%    choice it poses is settled by default as "nothing here", and the guard has to land in Methods
%    or in the Discussion's Bessel paragraph. The note is left in place unedited so the decision
%    stays visible.
%  - the ARMA/GLS half of the two-alternatives paragraph, 168 words -> 67. The family, the
%    citation, the empirical finding and the two structural reasons all survive; what went is the
%    free-timescales sentence and the gloss naming N_ch and i as the parameters the fluctuations
%    are richest in, which the sentence before it now carries.
%  - "behaves as though the recording had supplied more observations than it did", which the
%    closing paragraph says in its own words and 05_discussion.tex:137 says with a number.
%  - the Anderson gloss in the lineage paragraph, because the closing paragraph states the same
%    priority claim and states it as the novelty baseline.
%  - "it keeps the exact simulation cheap enough to run thousands of replicates at every design
%    cell": the fact is in the Figure 4 caption (ten thousand recordings per cell) and in
%    06_methods.tex:142.
%  - the delcore gloss ("do not distinguish stochastic channel gating from measurement noise and
%    return biased estimates") and the sweep counts "256 to 504 repeated sweeps". NEITHER IS SAID
%    ELSEWHERE. Both are supporting detail on other people's work, not caveats of ours.
% NOTHING was taken out of a scope caveat, a limitation or a concession. The full list is in the
% pass report.
%
% REWRITTEN 2026-08-04. Predecessor kept verbatim at
% ../archives/01_introduction_SUPERSEDED_20260804.tex (2095 words). This version is ~1700.
% What changed and why, so the trade is not re-litigated:
%
% (a) THE THREAD. The previous version had no single line: it was context, gap, lineage, then four
%     separate defensive digressions before the criterion. This one runs on one, that the memory of
%     the gating process is both what carries the information and what lets a model track the data
%     without earning it. Chosen over four alternatives in a controlled comparison (five drafts on
%     five threads, same fact packet, same length, three critique lenses; 2026-08-04).
% (b) THE OPENING no longer attributes least squares to Hodgkin and Huxley, who fitted by hand, and
%     no longer uses the events-per-sample ratio to separate the single-channel from the macroscopic
%     regime. That separation is the CHANNEL COUNT. The ratio is the interval axis and appears where
%     it belongs.
% (c) THE LINEAGE is compressed from a nine-name walk to the two axes it establishes, with the
%     citations hanging off them. The two members that act in the paper (the non-recursive member,
%     the interval-conditioned one) stay visible.
% (d) THE SCORE PARAGRAPH now says, in the sentence that introduces the identity, that measuring it
%     needs an ensemble from known parameters and is therefore not something a reader runs on one
%     record. That removes an expectation the old closer had to withdraw. It also avoids implying an
%     approximation has a true parameter of its own (the score vanishes at the pseudo-true value,
%     which is not the simulator's).
% (e) GODAMBE 1960 IS OUT. Optimum estimating functions, not the score identity.
% (f) THE ARMA AND FLUCTUATION-ANALYSIS CONCESSIONS MOVED after the criterion. In the predecessor
%     they sat before it and cut the line twice at the point of maximum tension.
% (g) THE CLOSER names the measurement rather than promising it, and raises nothing it then declines.
%     "Locating the reader on the map" was wrong and is gone: the map coordinates come from peak
%     current and baseline variance, while the residual autocorrelation answers a different and
%     narrower question, whether the interval a least-squares fit reports is credible. Whether to use
%     least squares is the experimenter's decision and the paper does not raise it. The demarcation
%     of what is not settled belongs in the Discussion, not here.
%
% STILL OWED, see sections/01_introduction.md:
%  - the "each sample equals the last plus noise" claim needs its derivation (the first difference of
%    a mean-square continuous process vanishes with the interval) or a naive-forecast citation. It is
%    currently an assertion.
%  - covariance fitting cost: "square of the number of samples" (moffatt2007estimation) vs "cube of
%    the record length" (predecessor). Open. The wording below avoids the exponent.
%
% PENDING-BIB CLOSED 2026-08-10. All six keys (katz1970membrane, katz1972statistical,
% anderson1973voltage, conti1980conductance, sigworth1981covariance, lei2020considering) plus
% hodgkin1952quantitative, colquhoun1981stochastic and neher1977conductance are in biblio.bib,
% which now holds 26 entries, and every cite key in the eleven section files resolves against
% it. The build emits no citation warning.

Some mechanisms leave a mark in the record that does not depend on fitting a particular model. The
flip state we proposed for the P2X$_2$ receptor is one: it shows up as a delay before the current
rises \citep{moffatthume2007responses}. As inferences get finer, claims are reached that leave no
such mark. Two decades later nine kinetic schemes for the same receptor were ranked by Bayesian
evidence over the same recordings, in work of our own \citep{moffatt2025bayesian}. Under MacroIR,
which averages the current over each acquisition interval and conditions it on the data already
observed, a conformational scheme, an asymmetric activation, came first; a published control,
MacroINR, the same likelihood
without the recursion, put the subunit-specific allosteric alternative first, overturning a margin
of a factor of six. The direction was reported, the control systematically underestimating the
evidence for schemes with conformational intermediates; but the choice between the arms rested on
argument, and nothing in those recordings settles which likelihood tells the truth, the way the
delay once settled the flip.
% REWRITTEN 2026-08-25 (Luciano: "la Introduccion se basa en un analisis superficial de lo que hace
% comms biol 2025"), against a full read of the published paper. Two defects repaired:
% (i) "under two likelihoods" presented the arms as co-equal; the 2025 paper's own text makes the
%     non-recursive arm a control ("As a control, we repeated the analysis with the non-recursive
%     MacroINR likelihood"), so the symmetric-doubt framing misdescribed the cited paper.
% (ii) "The sensitivity was reported, but not which ordering to believe" was FALSE against the 2025
%     text, which commits with direction: "the control method (MacroINR) systematically
%     underestimated evidence for schemes with conformational intermediates". What the 2025 paper
%     could not do is MEASURE that the commitment was justified (no ground truth on recordings);
%     "rested on argument" is the accurate form, and it is also this paper's gap.
% ADDED the overturned margin: Scheme IX over the top non-conformational rival (VI) was a Bayes
% factor of 6.4 (90% equal-tail 5.5-6.7) under the recursive arm; that is the decision the control
% flips. The >5000 margin is IX over VIII (its symmetric counterpart) and is NOT what flipped; do
% not swap them. The axis wording "the same likelihood without the recursion" is deposit-verified
% (A.11: all flags constant but recursive; av=2 == av=1 without update, byte-identical on the
% reference binary, tmp/verify_av2_no_update.macroir 2026-08-25).
% src: docs/bibliography/Moffatt_PierdominiciSottile_2025_Functional_Asymmetry_P2X2_CommBiol.pdf
% (Results, "Bayesian selection of kinetic schemes"; Fig 1d-e)
% ---- REATTACHED 2026-08-10: the record below was written for the predecessor's P2X2
% paragraph, which stood ninth. v7 moves that material into the OPENING paragraph above and
% NAMES MacroIR and MacroINR, which the 2026-08-10 withdrawal at the foot of this note
% explicitly permits. The "must not adjudicate" instruction still binds the paragraph above.
% ADDED 2026-08-07, 116 words. Owner: the desk-rejection pass recorded at 00_abstract.tex note 1f.
% WHY HERE. The paragraph above ends on the two questions an experimenter cannot separate and then
% went straight to the classical apparatus, so "this" in "The apparatus that answers this" had an
% abstract antecedent. This paragraph is that same sentence with a date and a receptor, and it hands
% the reader to the apparatus with a concrete thing for it to answer. It rides the section's thread
% rather than cutting it: the thread is that the memory of the gating process is both what carries
% the information and what lets a model track the data without earning it, and this is the case where
% the unearned tracking reached a mechanism.
% NO NEW CONCEPTS. Recursion, the acquisition interval, conditioning on data already observed and the
% gating variance are all defined in the lineage paragraph above, which already cites this same
% paper for the interval-conditioned member.
% THE AXIS WORDING IS ACCURATE AND IS KEPT. "What separated them was the recursion" is exact: the two
% runs behind Figs 1d and 1e of that paper shared averaging over the acquisition interval and the
% same gating variance term and differed in the recursion alone, which is the axis this paper
% measures. Nothing below asks for it to change.
%
% THE NAMING PROHIBITION THAT STOOD HERE IS WITHDRAWN, 2026-08-10. It read: 'Never write "MacroIR
% against MacroINR" or "the boundary-conditioned member" here. The object that ran there is not the
% member specified in Theory, so naming it as IR would be false.' The published names MAY be used;
% see 00_abstract.tex note 1f(i) ("NAMING, settled 2026-08-10"), which also records at its foot that
% the axis discipline itself was withdrawn the same day. The bridges IR = MacroIR and INR = MacroINR
% are closed at _program/nomenclature.md:286-296.
%
% HOW IT WAS WRONG, recorded so it is not rebuilt, because the error is a repeat of one this project
% has already made twice. TWO OBJECTS CARRY ONE NAME. What the freeze in THIS REPOSITORY labels
% macro_NMR omits the N*ms interval-variance term (refactor regression at a3e0a89), so NMR is indeed
% not the member Theory specifies. The PUBLISHED MacroINR is not that object and carries the term:
% confirmed 2026-07-31 (nomenclature.md, same section, "the bridge holds for INR, the fixed one") and
% again 2026-08-10 against the run's own _likelihood_model.csv. The prohibition took a true statement
% about a local build and applied it to the algorithm in a peer-reviewed paper. The premise was
% already refuted in writing eight days before the prohibition was written.
%
% WHAT THIS DOES NOT DECIDE. Whether the paragraph below SHOULD name them is a separate call and is
% still open; naming them is now permitted, not required. The prohibition is what is dead.
% UPGRADED 2026-08-10: "PUT DIFFERENT MECHANISMS FIRST" IS NOW THE SENTENCE, and the restriction
% that stood here is withdrawn. It said the identity of the top-ranked scheme under each likelihood
% could not be checked from the record, because the main text numbers nine schemes I-IX while
% Table S1 numbers eleven I-XI with no published mapping. The premise is true; the inference is not.
% Figure 1 of that paper is in the main text, uses main-text numbering, and draws the schemes in
% panels a-c, so panels d and e give both rankings without touching the SI. Read 2026-08-10:
% Fig. 1d (recursive) IX, VI, VII, VIII, IV; Fig. 1e (non-recursive) VI, IX, IV, VII, VIII. The
% main text supplies the direction as well, "the control method (MacroINR) systematically
% underestimated evidence for schemes with conformational intermediates", and the margin the bias
% erases, a Bayes factor of 6.4 (5.5-6.7) for IX over VI. Full record at 00_abstract.tex note 1f(i).
% WHAT THIS PARAGRAPH MUST NOT DO is adjudicate. It says the ordering moved and that nothing in that
% analysis could say which to believe. The matching declining sentence is at 05_discussion.tex, in
% the paragraph that carries the evidence bridge; the two must stay consistent.

What the recordings hold is the reason. Single-channel durations are read off the record
\citep{neher1977conductance,colquhoun1981stochastic} and macroscopic currents follow a deterministic
relaxation \citep{hodgkin1952quantitative}, but most recordings are in neither place: the averaging
is incomplete, so a fluctuation around the mean current survives and carries the memory of the
gating. That memory is where the remaining information is, since channel number and unitary current
enter the mean as a product and no fit to the mean separates them.

Current practice fits the mean and discards the fluctuation. The four approaches surveyed by
\citet{clerx2019four} are deterministic fits with a root-mean-square objective, and the IonBench
benchmark states that it does not cover optimisation approaches for stochastic models
\citep{owen2025ionbench}. Discarding the fluctuation usually leaves the point estimate standing.
What it damages is the count: samples closer together than the channel's relaxation time are
correlated, so a recording supplies fewer independent observations than it has samples while the fit
counts them all, and the interval it reports is narrower than the spread it delivers.

Likelihoods that model that correlation have been available for decades, from stationary power
spectra and the exact non-stationary two-time covariance
\citep{katz1970membrane,katz1972statistical,anderson1973voltage,conti1980conductance,sigworth1981covariance}
to whole-record likelihoods
\citep{celentano2004use,milescu2005maximum,moffatt2007estimation,stepanyuk2011efficient,munch2022bayesian},
differing in what they condition on, MacroIR the most recent \citep{moffatt2025bayesian}.
Where they break was stated when they were published and never located: the recursive member of that family, the one that samples instantaneously, was expected to degrade as the channel count falls and as the record is averaged,
its non-recursive counterpart to have no useful regime at all, the damage falling on the error rates
rather than on the estimates \citep{moffatt2007estimation}.
% COMPRESSED 2026-08-26 (shortening_plan.md, intro item 2). The clause that went pre-explained the
% family's two axes, how much gating stochasticity a likelihood carries and whether its observable
% is an instant or the interval average a recording holds, which 02_framework.tex states at full
% length in "The methods differ along two axes" and in Table 1, both protected survivors. MacroIR
% was already defined two paragraphs above, with the same citation and with the interval average
% named there ("averages the current over each acquisition interval and conditions it on the data
% already observed"), so the observable is still introduced before the framework section needs it.
% The citation is kept rather than dropped: it costs nothing and it holds the credit at the point of
% the claim. The Moffatt 2007 expectations in the sentence after it are untouched.

% CUT 2026-08-13, outside-read pass (header note of that date). "They are also harder to watch",
% 62 words contrasting a least-squares trace whose error a reader sees with a recursive prediction
% whose error they do not. Explanatory rather than evidential, and 04_results.tex opens on the same
% fact with the measurement behind it ("A recursive likelihood cannot be checked against a record by
% eye ... it follows the record whether or not the uncertainty it reports is the uncertainty it
% delivers"). "settles it either" below still resolves: the paragraph above says where the members
% break was never located.
No statistic computed on a single recording settles it either: one recording is one draw from the
sampling distribution, and with the generating model unknown a departure may belong to the
approximation or to the model. The question has to be put from outside, over an ensemble drawn from
known parameters, and for this class of model that is possible: the forward process simulates
exactly where its likelihood cannot be evaluated exactly.
% COMPRESSED 2026-08-26 (shortening_plan.md, intro item 3): the sampling-distribution explanation
% ("Whether the error bar a method reports matches the spread it delivers is a property of the
% sampling distribution") is given per check, and definitively, in the protected diagnostics
% definitions of 02_framework.tex, which say of each of the three what it needs: "needing no
% ensemble and no known truth", "cannot be run on an experiment at all", "one recording gives one
% score". The intro keeps the claim. All four things the item protects survive: the no-single-
% recording claim, the confound clause (stated nowhere else), the ensemble from known parameters,
% and the exact-forward-simulation sentence verbatim.

The instruments are classical. For a correctly specified likelihood the score, the gradient of
the log-likelihood, has mean zero at the generating parameters and its covariance across recordings
equals the reported Fisher information \citep{huber1967behavior,white1982maximum}; how far an
approximation departs from either identity is a measurable quantity rather than a test to be passed.
Goodness of fit does not answer the question,
since an approximation can reproduce the mean current closely and still misreport the information by
more than an order of magnitude, and comparing two approximations against each other does not answer
it either, because both may be wrong. For macroscopic currents neither departure had been measured.
% ADDED 2026-08-30: the approved abstract opens on "had not been measured"; the Intro said "never located"
% (L39) and "a measurable quantity" but never the gap itself. Scheme naming "an asymmetric activation"
% added the same day so the abstract's "asymmetric activation of P2X2" resolves to the conformational
% scheme (Comm Biol 2025 title: Functional Asymmetry in the P2X2 Receptor).
% REPAIRED 2026-08-30: the line above had swallowed "Near the maximum that same matrix enters the
% Bayesian evidence" into the comment, leaving "through a volume term ..." as a live fragment (the
% mid-sentence-% trap, third time in this manuscript). "That same matrix" was the reported Fisher
% information two sentences up; with the gap sentence now between them it became "the reported
% information" (2026-08-30).
Near the maximum the reported information enters the Bayesian evidence
through a volume term and a rescaling of the effective sample size, so misreporting it carries the
error into every comparison built on it. The correction is not derived here; this
paper measures its input and computes no evidence,
% 2026-08-30: was "is derived elsewhere", which reads as a citation a referee will ask for. Luciano: the
% evidence correction is a future project, not a written manuscript; so is the microscopic paper.
 and it never benchmarks against non-stationary
fluctuation analysis \citep{stepanyuk2014maximum}, which answers a different question, returning the
channel number and the unitary current with no likelihood and no kinetics, from many exchangeable
sweeps.
% REASON ADDED 2026-08-26, the disclaimer itself kept verbatim. Three independent outside reads
% arrived at "why did you not compare against NSFA?", which is what a bare declination invites: it
% states the omission and not the ground for it. The ground is already in the Discussion's classical
% route paragraph, thirty pages later; this is one clause of it at the first mention, so the
% declination reads as a scope decision rather than as a gap. Nothing is claimed about NSFA's
% calibration, which this paper does not measure.
% COMPRESSED 2026-08-26 (shortening_plan.md, intro item 4), and by less than the item asks, which is
% recorded here with the arithmetic so it is not reopened. What came off is wording on the identity
% sentence and the evidence sentence, about twenty words: "in the parameters" after gradient, "at
% the parameters that generated the data" for "at the generating parameters", "the Fisher
% information the likelihood reports" for "the reported Fisher information", and "a likelihood that
% misreports it carries" for "misreporting it carries".
% WHY NOT FORTY-FIVE. Three of this paragraph's five sentences are held whole by the item's own
% binding adjustment and by this file's header: the two negatives are required IN FULL, the scope
% disclaimer is required VERBATIM and is stepanyuk2014maximum's only home, and the volume-term
% clause is the one the header lists among the three things an outside read asked for and did not
% get, because 05_discussion.tex records that the volume and effective-sample formulas rolled out of
% that section and survive HERE. Those three are 107 of the paragraph's 169 words. Cutting to 130
% would have to come out of one of them.
% COMPRESSED 2026-08-13, same pass, 245 words to 210 across the two paragraphs above. What went is
% wording only: "The generating model is unknown as well, so a departure between what a likelihood
% reports and what it delivers may belong to the approximation or to the model" keeps its clause and
% loses its restatement, and "An approximation can depart from either identity or from both, and how
% far is a measurable quantity" becomes one clause. PROTECTED AND UNTOUCHED, verified against the
% v6/v7 ledger in the header: the goodness-of-fit pre-emption (restoration (a)), the volume-term and
% effective-sample clause, which the Discussion's own note records as surviving HERE and nowhere
% else, and the non-stationary-fluctuation-analysis disclaimer, which is stepanyuk2014maximum's only
% home in the body.

We measured both departures over a plane whose coordinates are the number of channels, the
acquisition interval in units of the relaxation time, and the instrumental noise in units of the
gating noise. The rate constants are held fixed. The scheme is two states at an open probability of
one half, driven by a single concentration jump, which isolates the error the approximation itself
commits; whether it bounds anything richer is a conjecture, not a result of this paper.
% "so what is reported is a lower bound for anything richer" DEMOTED 2026-08-25 (naive round,
% CONFIRMED three times): asserted, never derived, and the paper's own few-channel departure,
% whose mechanism the Discussion admits is not understood, undercuts the premise that the
% K=2, p=1/2 case minorizes richer schemes.
% COMPRESSED 2026-08-26 (shortening_plan.md, intro item 5), about 36 words. Two justifications go
% and the facts stay. "because those coordinates are dimensionless, the closing rate folding into
% the interval" survives at 02_framework.tex, in the natural-units paragraph, and in Methods; "is
% the most favourable case for a Gaussian treatment of the occupancy" survives in Methods, where
% p = 0.5 is shown to maximize the gating variance at zero skewness, and in the Discussion's
% protected limitations. The conjecture hedge is now one clause; its full form is the Discussion's.
% KEPT VERBATIM, per the item's binding adjustment: "which isolates the error the approximation
% itself commits", which the abstract's own appositive no longer duplicates only because the
% reconciliation header DECLINED that abstract cut, so the two now stand together as they did
% before this pass. Also kept verbatim, being its only occurrence: the implementation-error
% sentence below.
Ten thousand recordings were simulated at every cell and scored by
the eight members of the cost ladder, from least squares to the filter conditioning each interval
average on both ends. An implementation error shows up as a departure like any other, so the same
test checks the code.
% ---- REATTACHED 2026-08-10: this is the design paragraph the note below guards. v7 restores
% the lower-bound scope caveat that v6 had cut, but NOT the fixed-spectral-density clause, so
% the [OPEN] question below is still settled here by default as "nothing here" and the guard
% still has to land at 06_methods.tex or in the Discussion's Bessel paragraph.
% POINTER CORRECTED 2026-08-10: the note below says "02_theory.tex". There is no such file; the
% canonical [BESSEL-BOUND] block is at 08_appendix_derivation.tex (moved 2026-08-12), verified today. Same correction
% applies to the two copies of that pointer in the closing paragraph's notes.
% [BESSEL-BOUND] CLOSED 2026-08-11. The bound is PRINTED, at 08_appendix_derivation.tex (moved there 2026-08-12) in the
% paragraph beginning "How much of the kernel the uniform window misses". Everything below was
% written while it was open (2026-08-06) and is kept for its site-specific content only.. Canonical note at 08_appendix_derivation.tex, the acquisition-kernel subsection (moved 2026-08-12).
% READ WITH THE LENGTH-PASS NOTE IN THE HEADER: the clause this note guards was removed on
% 2026-08-09 as specification detail Methods owns, so the [OPEN] question below is settled here by
% default and the guard has to land at 06_methods.tex or in the Discussion's Bessel paragraph.
% Note kept verbatim so the decision is not lost.
% THE CLAUSE AT RISK is "held at fixed spectral density so that shortening the interval raises the
% noise in each sample and leaves the noise per unit bandwidth unchanged". That is exactly right
% for the simulator and it has a physical ceiling on a real rig, which the sentence does not say.
% A real amplifier's noise is flat only up to its own anti-alias filter: once the window falls
% below the filter's response time the per-sample variance stops rising as 1/Delta and the excess
% appears as correlation between neighbouring samples instead. So the interval axis of this study
% is realisable on a bench down to about Delta = a few / f_c and no further, unless the filter is
% opened with it, which costs noise.
% WHY IT IS WORTH A CLAUSE RATHER THAN A PARAGRAPH. The same dimensionless group, f_c*Delta,
% governs both this and the observable's kernel (canonical note, items i and ii), so the two
% departures are one departure seen from two sides, and both vanish at the same rate. Nothing in
% the paper's measurements changes: the simulator's noise IS white by construction and the sweep
% is over a model, which this paragraph already says. What is missing is that the shortest
% intervals of the sweep are the ones a bench cannot deliver at fixed spectral density, and that
% is the same corner where the boundary-conditioned member's advantage is largest.
% [OPEN] Either a subordinate clause here ("... unchanged, which a real amplifier delivers down
% to its anti-alias filter's response time and no further") or nothing here and one sentence in
% the Discussion's Bessel paragraph. Do NOT put it in both; this paragraph is already the densest
% in the section.

The plane separates two failures usually named as one: what a method gets wrong, and what it says
about how wrong it is. On the first the ladder is nearly flat. What separates the members
is what they report about those estimates: the ones that leave the correlation unmodelled
misreport their information by an order of magnitude wherever the residual retains enough memory,
while the one conditioning on both ends of the interval is calibrated over almost the whole plane.
The map includes that member in what it audits, since a method whose limits are
unmapped has been endorsed rather than characterised.
% COMPRESSED 2026-08-13, same pass, 196 words to 166. The bias contrast is stated once instead of
% twice ("So the members are hard to tell apart by their estimates" was the same sentence read
% backwards). NOT TAKEN, against the outside read, which would have stopped this paragraph after the
% few-channel corner: the audit clause (a method whose limits are unmapped has been endorsed rather
% than characterised) is a scope statement, and the closing ordering is the section's falsifiable
% claim and was flagged untouchable by the 2026-08-09 triage. Both stay whole.
% COMPRESSED AGAIN 2026-08-26 (shortening_plan.md, intro item 6), 166 words to about 100, and this
% is the pass that takes what the 2026-08-13 note above declined. Read the two together: the audit
% clause STILL STANDS, so half of that note is honoured; the closing ordering is the half that goes,
% and it goes on a three-way duplication count the earlier pass did not make. The ordering is stated
% in the abstract ("The noise at which the least-squares error bar becomes honest lies one to three
% decades above the noise at which the fluctuations stop carrying the unitary current: no measured
% region gives both") and in full in the Results, at "The ordering of the boundaries is the result
% the map exists to state", which carries the one-to-three-decades number this copy never had. Both
% survivors verified present on 2026-08-26 before the cut. The section therefore still makes its
% falsifiable claim; it makes it once, in the abstract, instead of twice within two pages.
% ALSO CUT HERE, each with its survivor checked: the averaging-removes-bias mechanism and "least
% squares carries no bias in what it fits at all" (Results, the flat-ladder paragraph); the
% score-time-correlation mechanism and "which is why it does not show up one interval at a time"
% (02_framework.tex, the protected F_t/J_t accumulation sentence, and Figure 3); and the failure
% corner "at the fewest channels and lowest noise" (abstract, "with departures growing toward few
% channels and low noise", and the Results corner paragraph).

Making the measurement takes an ensemble. Using it does not: the integrated autocorrelation of the
standardized least-squares residual reads the effective interval count off a fit already being made.
It also picks up the analogue filter on a record sampled faster than its own bandwidth, and that is a
number the reader chose as they chose the sampling rate. What is new here is narrow, and the
likelihood's construction is not part of it: that was published with the P2X$_2$ analysis. Its
derivatives are. The gradient of the log-likelihood is analytic and the Fisher information follows
from the same pass (Methods). Standard errors from fluctuation data go back to
\citet{anderson1973voltage} and analytic gradients to \citet{milescu2005maximum}.
What this work adds, for a non-stationary protocol read from
a single record, is an interval whose calibration has been verified rather than assumed.
% COMPRESSED 2026-08-26 (shortening_plan.md, intro items 7, 8 and 9, and DELETE item 10), about 90
% words off this paragraph. Site by site, because two of the four touch things earlier notes guard.
% (7) THE TAU_INT GLOSS, "counts how many intervals a fit effectively has for every interval it
%     believes it has", is a fourth copy: duplication ruling (c) makes the 02_framework.tex
%     definition the sole full gloss, and the Figure 5 caption keeps the axis decode. Per the item's
%     binding adjustment only the gloss and the costs-nothing clause come off. THE PIVOT AND THE
%     FILTER SENTENCE STAY, the second being the only statement of the filter pickup in the whole
%     manuscript and the words that close [BESSEL-BOUND] at this site, as the notes below record;
%     "off a fit already being made" carries what the costs-nothing clause said, and the Results say
%     it again in their own words ("a quantity an experimentalist can compute on a real recording
%     without an ensemble and without a known truth").
% (8) THE DERIVATIVE MACHINERY loses its mechanism and keeps its claim. The templated C++ type, the
%     one expression returning the log-likelihood and its score, and the wired-by-hand Fisher with
%     its no-second-pass are near-verbatim in Methods, whose own residue is required to keep the
%     one-templated-type sentence and the automatic-differentiation link. The novelty sentence
%     itself, which is what the 2026-08-13 note below was written to defend, is untouched: what is
%     new is narrow, the construction is not part of it, the derivatives are. Note that the Fisher is
%     STILL NOT on the differentiated side here, which that note requires: "follows from the same
%     pass" is a cost claim, not a derivative claim.
% (9) THE PRIOR CREDITS keep both names and both citations, immediately before the novelty sentence,
%     and lose the two appositives. The Discussion carries the fuller Milescu treatment; the
%     Anderson and Stevens standard-errors credit exists only here and is kept.
% (10) DELETED: "Richer schemes, stationary protocols and filtered recordings remain to be tested."
%     The Discussion's protected limitations subsection carries the fuller list and names them as
%     companions rather than omissions, so this is its function stated twice. Nothing points at it,
%     and the section now closes on "verified rather than assumed".
% DERIVATIVE MACHINERY ADDED HERE 2026-08-13 (Luciano), 62 words, and it is a NOVELTY CLAIM and not
% a method detail, so it is on the record why it sits in this paragraph and in this form.
% WHY IT GOES IN AT ALL. Until today the fact lived only at 06_methods.tex, which eLife prints after
% the Discussion, and the Discussion's own "what is specific here" list did not carry it. Precedent
% in the field settles that it earns the space: milescu2005maximum bills the analytic gradient in its
% ABSTRACT, as the reason its method beats celentano2004use ("in part due to the analytical
% calculation of the likelihood gradients"), and that key is already in this manuscript.
% WHAT THE CLAIM IS, and it was corrected twice before landing. It is NOT "we used automatic
% differentiation rather than finite differences": the repo's finite-difference construction is a
% central difference OF THE ANALYTIC SCORE, so it presupposes the derivatives and the contrast is
% empty. The claim is that the derivatives are ANALYTIC and come from ONE implementation, the same
% expression returning the log-likelihood and its gradient, where the comparison class (Milescu) has
% analytic gradients written out by hand. Nothing is asserted here about how anyone else implemented
% theirs; the citation stays a citation for analytic gradients over independent intervals.
% THE FISHER IS SEPARATED ON PURPOSE AND THE SENTENCE MUST NOT BE COLLAPSED. The Gaussian Fisher does
% NOT fall out of the differentiation: it is an expectation under the model, not a derivative of the
% recorded log-likelihood, so it is wired by hand over the moments and the first derivatives the
% score already produced. That is exactly why it is positive semidefinite by construction and costs
% no extra pass, which 02_framework.tex and 06_methods.tex both state. An earlier draft of this
% paragraph, and the shipped Methods sentence it replaced, both put the Fisher on the differentiated
% side; that reading is wrong and is what this wording fixes.
% "PRIMITIVE" WAS ASKED FOR AND IS NOT USED, deliberately: beside "derivative" a reader takes it as
% the antiderivative. It is the code's word (primitive() on a Derivative<>), it appears nowhere in
% the printed manuscript, and 08_appendix_derivation.tex already uses it in a second sense. "Value".
% "ITS CONSTRUCTION" replaces "the likelihood" in the first sentence so that the claim does not
% depend on the OPEN attribution seam: this paragraph says the construction was published with the
% P2X2 analysis while 06_methods.tex:344 says the specification is this manuscript's and 2025 is the
% first application. That is unresolved and is LUCIANO'S CALL; the derivative claim is true under
% either reading, which is why it could be written before the seam is closed. Do not let a later
% edit make this sentence depend on it.
% ONE CONSEQUENCE THIS CREATES, and it is now urgent rather than latent: 06_methods.tex:1005 records
% that \citep{moffatt2025macroir} is the Zenodo software record v1.0.0 of September 2025, while the
% differentiated recursion enters the codebase in October. With the derivative machinery claimed as a
% contribution, a reader who downloads the cited version cannot reproduce any score-based result.
% The backmatter has to pin a later version before submission.
% src: projects/eLife_2025/figures/paper_both/figure_7.html (crossing table, fitted slopes)
% supports the falsifiable ordering stated in the paragraph above this one. v7 states it
% without the "one to three decades" figure, which is carried at 04_results.tex:600-602.
% ---- REATTACHED 2026-08-10: the notes below were written for the predecessor's closing
% paragraph and still apply to this one. [BESSEL-BOUND] REMAINS CLOSED AT THIS SITE, by
% different words: v7 says "It also picks up the analogue filter on a record sampled faster
% than its own bandwidth, and that is a number the reader chose as they chose the sampling
% rate", in place of the fifteen-word clause the note names. The "ALSO EXPOSED" item below is
% moot here: the sentence it guarded ("Two design variables ... and through nothing else") is
% not in v7 at all, so there is nothing left at this site to break.
% src: projects/eLife_2025/figures/paper_both/figure_5_variant_2members.Rmd (was figure_6B.Rmd; header, measured points 1 to 5): tau_int
% in normalized Sokal/Kish form, null 1 for a white residual; at matched tau_int the least-squares
% size agrees to three digits over four decades of N_ch (1.127 / 1.135 / 1.123 / 1.145 at ~1.04), while
% at matched tau_int two sampling intervals differ by a factor 5.7 (8.9 at interval 0.01 against
% 1.56-1.63 at 0.5). The error-bar scale is the square root of the distortion, which is why 4 and not 3
% is the factor-two mark.
% LENGTH PASS 2026-08-09: the conversion table this src note documents ("in units a reader can act
% on: a distortion of 1.15 is a 7% error on a confidence interval, 2 is 41%, and 4 turns a nominal
% 95% interval into about 68%") was cut from the sentence above because 02_framework.tex
% states it verbatim, in the paragraph that defines the distortion it converts. The src note stays
% here because the sentence it also supports, on what the two numbers say about the width of the
% reported interval, stays.
% CORRECTED 2026-08-04: an earlier draft of this paragraph said the residual autocorrelation alone
% locates a recording, and quoted a factor of two at three. Both were wrong, and that file had
% already measured the right version.
% [BESSEL-BOUND] CLOSED HERE 2026-08-07 by the fifteen-word clause the note below asked for
% ("as they did the analogue filter, which contributes to the same statistic on a record sampled
% faster than its own bandwidth"). The other two sites, 02_framework.tex beside the
% residual-whiteness check and the scope clause in the design paragraph above, are still open, and
% the canonical note with the bound and the script stays at 02_theory.tex under the Bessel
% paragraph. What was bought: the paragraph no longer asserts that two design variables enter the
% answer and nothing else does, on a record where a third one demonstrably enters, and the repair
% rides the sentence's own logic rather than contradicting it, because the filter is a number the
% reader chose exactly as they chose the sampling rate. What was NOT bought and must not be read
% in: no size for the contribution is stated here, deliberately, because stating it commits to the
% expansion, which belongs in Theory. Original note follows.
% [BESSEL-BOUND] CLOSED 2026-08-11. The bound is PRINTED, at 08_appendix_derivation.tex (moved there 2026-08-12) in the
% paragraph beginning "How much of the kernel the uniform window misses". Everything below was
% written while it was open (2026-08-06) and is kept for its site-specific content only.. Canonical note at 02_framework.tex; the argument for why this
% particular sentence needs guarding is at 02_framework.tex, beside the residual-whiteness
% check.
% THIS PARAGRAPH MAKES THE PAPER'S ONE OFFER TO A READER WITH DATA IN HAND: "the integrated
% autocorrelation of the standardized least-squares residual ... costs nothing, since the fit was
% going to be made anyway". On a real recording that statistic also picks up the analogue filter,
% which contributes a lag-one residual correlation of 0.075/(f_c*Delta) on its own, 8.6% where
% the acquisition window equals the filter's inverse cutoff. The offer is sound where the record
% is sampled at or below the filter's bandwidth and misleading where it is not, and the reader
% cannot tell the two apart from the number alone: a filter and a badly correlated fit both push
% tau_int up.
% ALSO EXPOSED, and it is the same fix: "Two design variables that look as though they should
% matter, the channel count and the instrumental noise, enter the answer through that one
% statistic and through nothing else." On an oversampled record a THIRD variable enters it, the
% filter setting, and that one the reader also chose, like the sampling rate. The sentence's own
% logic therefore extends to it rather than being broken by it, which is the cheapest repair:
% name the filter alongside the sampling rate in the clause that already says the reader knows
% the acquisition interval because they set it.
% COST: about fifteen words. This is the highest-value fifteen words the Bessel scoping buys
% anywhere in the manuscript, because it is the only place a reader is told to act.
