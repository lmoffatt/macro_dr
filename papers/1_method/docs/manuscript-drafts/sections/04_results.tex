\section*{Results}

% Owner: papers/_program/program.md §1 (body roster and figure order) and
% papers/1_method/decisions.md ("The figure set", revised 2026-07-28: six body figures).
% Every quoted number is on the freeze (1c2ae6f + 87889e6 + 0ffbda7), anchored on the Gaussian
% Fisher information, at n_sim = 10^4 unless the sentence says otherwise, and carries a `% src:`
% comment per LINT-SRC. Nothing is quoted from 433ed13.
% Definitions belong to 02_theory.tex and 02_framework.tex; run values, the two least-squares
% configurations and the noise conversion belong to 06_methods.tex. Do not restate them here.
%
% REWRITTEN 2026-08-05, 5899 -> ~3700 words of prose. What changed and why, so the trade is not
% re-litigated:
%
% (a) THE OPENING states the question the section answers, which is why a recursive likelihood
%     needs measuring at all: it cannot be checked against the record by eye. That was in the
%     abstract and nowhere here.
% (b) THE NUMBER SERIES ARE GONE. The section quoted 284 numeric literals, one every 21 words,
%     including 14 series of four values across the channel decades. A series that the figure
%     draws is now a range and a trend with a pointer. Every number that carries a claim the
%     figure cannot make is kept, with its % src.
% (c) DEDUPLICATED. 122 words appeared verbatim in this file's own captions and 257 in
%     05_discussion.tex, measured. The captions describe the display, the Discussion draws the
%     consequence, and the prose states the measurement.
% (d) NEW SUBSECTION, "Whether one correction can undo it". The anisotropy of the distortion was
%     measured and drawn (figure_4_S2.Rmd, 2026-08-02) and the manuscript
%     referred to it in one clause without ever citing the figure. It is the result that decides
%     whether the residual autocorrelation can be used to repair least squares, which is the rule
%     an experimenter would otherwise reach for, so it now has the subsection and the citation.
% (e) THE INFORMATION-BUDGET SUBSECTION IS OUT, finishing the pass the note at the corner-figure block
%     asked for. That float stopped holding the budget figure when the figure was demoted; the
%     subsection introduced a display item the paper no longer carries, and the label is renamed
%     fig:design -> fig:limit in the same pass. Nothing measured is lost: the budget figure and
%     its producer are in figures/paper_both (figure_5_budget.Rmd) and can come back as a figure
%     with a subsection, which is what putting it back means.
% (f) CLOSED 2026-08-25. The run that did not exist RAN: the fisher_only lane
%     (dispatch_figure_3_fisher_only.sh, seed 20260814) put the differenced Fisher on the FULL
%     roster, LSE/ILSE/INR/VR included, at nsim 1e4 and noise label 0.1; data in
%     figures/data/a202e03 (beware the VR nsim_8 smoke files in the same directory; the reader
%     script filters to nsim_10000). The vehicle chosen (Luciano, this session) is the
%     supplementary table + one Results sentence: the table and the direction convention live in
%     supplementary_file_1.tex S1.5, the sentence stands in this file after the "Least squares
%     carries no entry" paragraph, and the NR caveat it owed ("narrows by about a third, not
%     overturned") is now printed there. DIRECTION REPAIRED IN THE SAME PASS: the 2026-08-11
%     version of this note said the analytic form "over-states"; the producer's convention
%     (C = inv(sqrt(G_b)) F_b inv(sqrt(G_b)), src/core/likelihood.cpp:3509) makes C > 1 mean the
%     differenced Fisher EXCEEDS the analytic one, so the verb was wrong while the arithmetic
%     (76.5 narrowed by 1.6) was right; all three sites (here, Methods, S1.5) now state the
%     direction by the formula, never by a verb. The 433ed13 medians this note used to quote are
%     retired to S1.5's closing paragraph.
%
% LENGTH PASS 2026-08-10, 5371 -> ~4070 words of prose against a 3300 target. IT COMES IN OVER
% TARGET, deliberately, and the accounting is here so the last 770 words are not shaved off the
% wrong things later. Reaching 3300 from here means deleting measurements, not compressing prose.
% WHAT COULD NOT BE TOUCHED, about 950 words of this section: the two least-squares configurations
% and which one every number comes from (opening paragraph, the Figure 3 lead, the map's classical
% arm); the unequal coverage quoted with its number; the partial first-moment prediction; the
% constant-variance qualification on the least-squares ratio; the two easy misreadings of the
% plane; the empty-column paragraph; the two limits on Figure 6 (Figure 5 when this note was written);
% "the evidence is thinner than the
% picture"; the concession that least squares recovers the rates; Milescu's diagnosis; the
% non-stationary-fluctuation-analysis disclaimer. Those were COMPRESSED (they lost about 120 words
% between them) and none was cut.
% WHAT THE PASS DID CUT, with the rule in each case:
%   (i)   sentences that restated one of this file's own captions, the caption describing the
%         display and the prose stating the measurement: the ellipse roster and the
%         groups-of-ten construction at the head of "Recovery"; the definition of the second
%         orange number and of the dot-dash ellipse; the column/row/axis description of Figure 4
%         and its two anchors; the four-boundary roster of Figure 7 (Figure 6 when this note was
%         written); "a factor of 1.5 is a faint
%         tint and here it needs an axis"; the noise-level control sentence, which is Figure
%         3--figure supplement 3 word for word.
%   (ii)  surviving numeric series reduced to a range, with the per-member values left in the
%         % src that was already carrying them.
%   (iii) paragraph-closing restatements, forward announcements and the "three readings" roadmap,
%         which the three paragraphs then performed.
%   (iv)  three consequence sentences stated with the same numbers in 05_discussion.tex: the
%         "usable quantity rather than a complaint" closer, the degrees-of-freedom gloss on
%         tau_int, and the second statement of the no-safe-region ordering. The numbers stay
%         here; the consequence stays there.
% ONE MEASUREMENT WAS DROPPED, and it is the only one: the per-panel eigendirection numbers for MR
% and VR in the noise pair (1.04 and 1.02 along the noise axis against 1.59 and 2.02 across it),
% which supported a generality claim that the design-plane anisotropy measurement then makes
% properly. Twenty-five words if it is wanted back.

A recursive likelihood cannot be checked against a record by eye: it predicts each sample from the
samples before it, so it follows the record whether or not the uncertainty it reports is the
uncertainty it delivers. What follows measures it against least squares, on recordings simulated
exactly from the process every member approximates.

% THE FIGURE ROLL CALL IS DELETED, 2026-08-05, and it should not come back in any form.
% It was the brief's own policy line transcribed into the prose: sections/04_results.md:15 reads
% "Each figure answers the objection the previous one raises", which is an instruction about how to
% ORDER the figures and not a sentence addressed to a reader. Three reasons it fails as prose.
% (i) It is a claim about the document rather than about ion channels, and the reader cannot check
%     it where it stands; it asks them to hold six labels before any of them carries content.
% (ii) The section headings and the placement of the floats already say what it says.
% (iii) The instruction's real target is the TRANSITIONS, and they already carry it: most of the
%     subsections open by naming the limit of the one before. Announcing a chain the prose then
%     performs is redundant with its own execution.
% QUOTATIONS DROPPED 2026-08-13. Four openings were quoted here as the evidence for (iii), and by
% today the list was wrong three ways: the design-plane opener was rewritten the same day and no
% longer exists in any form, "on a scale that Figure 4 cannot show" had drifted to "that Figure 4B
% cannot show", and "four of the six subsections" counted six when this section now holds seven.
% A note that repeats what it points at goes on asserting the old text after the text moves. The
% claim itself is checkable in two minutes at the subsection openings, which is where it belongs.
% The paragraph above, on why a recursive likelihood cannot be checked by eye, is the real opening:
% it says why the section exists and it is about the measurement rather than about the manuscript.
% Checked in the same pass: no other stretch of seven words or more from 04_results.md appears in
% this file, so this was the one leak and not a pattern.

Throughout, $k_{\mathrm{on}}$ and $k_{\mathrm{off}}$ are the opening and closing rates, $i$ the unitary current, and $\widetilde{\Delta}$ and $\widetilde{S}$ the acquisition interval and the instrumental noise in the natural units of the model, $\widetilde{\Delta} = \Delta\,k_{\mathrm{off}}$ and $\widetilde{S} = S\,k_{\mathrm{off}}/i^{2}$ with $S$ the instrumental noise power spectral density. The eight members are those of Table~\ref{tab:members}. The two least-squares arms are one flag apart, \texttt{LSE} fitting the conductance at the sample and \texttt{ILSE} fitting it averaged over the acquisition window, and both are run over the whole design plane, on the same cells as each other and as every other member. % COVERAGE CORRECTED 2026-08-12 (Luciano): this read "Their coverage differs: ILSE is run over the whole design plane, and LSE has the clouds of the single-cell comparison at three channel counts and the distortion measurements across the plane", from a check dated 2026-08-06 that predated the run landing. Re-measured 2026-08-12 from figures/figure_4_source_data/*.csv, which is what every plane figure draws: nonlinearsqr_g and nonlinearsqr each carry 210 cells, the same 30 (N_ch, noise) combinations at n_sims 1e4 by the same 7 sampling intervals. ILSE's only extra cells are two at N_ch = 5, off this paper's grid, and four superseded ones at n_sims 1e3. The two do differ in where the single-cell clouds sit, which is a fact about Figure 2 and not about the plane: ILSE has them at N_ch 10, 100 and 1000, LSE at 10, 100 and 10^4. 02_framework.tex and 06_methods.tex were corrected the same day; this was the one site left.
% TRIMMED 2026-08-12 (10_concept_census.md, defect 3): the two tilde definitions moved up into the
% framework section, where they are now stated before their first use. What stays here is the
% reminder and the three symbols this section reads off figures.
% TWO FIXES 2026-08-12, same paragraph.
% (i) The trim above was written as a comment on the same LINE as the surviving prose, so the
%     member roster and the least-squares coverage sentence were commented out with it. They are
%     live again. The subsection-deletion note below explicitly parks the coverage sentence in
%     "the paragraph above", so losing it silently would have taken a claim the Results makes
%     nowhere else out of the manuscript.
% (ii) The reminder now carries the two laws instead of pointing at "the natural units defined
%     above". They ARE defined above, in the framework section, and the pointer was correct; the
%     laws are repeated here because this is the first line a reader who jumps to the Results sees,
%     and every design-plane axis is labelled in these two symbols. Methods still owns the sweep,
%     the run values and the twelve-level ladder, so the duplication is one formula each and no
%     number.

% SUBSECTION DELETED 2026-08-06 (Luciano): "One filter step along the cost ladder", together with
% the Figure 1 float, which is now in 02_theory.tex after the two-axis paragraph. The subsection was
% Beat 0 of the narrative plan (approach.md:61, "least squares works and you can watch it work"),
% and it is dropped rather than rewritten: the figure as built does not tell that story, it draws
% where inside one interval each of the two choices acts, which is Theory's subject and which its
% caption states it makes no measurement of. Its prose was in any case the third statement of one
% roster, after Table 1 and the caption itself. The one sentence in it with Results scope, which of
% the two least-squares arms is run where, is now in the paragraph above. The Results therefore
% open on the first measured claim.

% SUBSECTION HEADINGS REWRITTEN 2026-08-12 (Luciano: "too little informative"), against a verbatim
% survey of six eLife research articles (62714 Munch, 77220 Boelts, 56265 Bittner, 89470 Sellinger,
% 82566 Schutt, 106194 Ziaeemehr). eLife uses exactly two shapes: a DECLARATIVE FINDING with a named
% subject and a verb ("MNLE learns accurate likelihoods with a fraction of the simulation budget";
% "EPI identifies two regimes of rapid task switching"), which is what carries a result, and a
% gerund/noun-phrase TASK LABEL ("Simulation of ligand-gated ion-channel data"), which carries setup
% and validation. Sentence case, 5-14 words, no colons, no questions, no interrogative fragments,
% the object named by its own name. Methods already matches the task-label shape and was not touched.
% What was wrong here: five of seven headings were fragments ("Whether one correction can undo it",
% "What each axis of the design buys"), two named a display item rather than a result ("The design
% plane, in both moments" is Figure 4's own caption title), and none stated a finding. The new ones
% use the English member names the prose uses, not the codes, per 02_framework.tex's naming block.
% REWRITTEN 2026-08-12 (Luciano), 1152 -> ~640 words of prose, and rewritten twice: once long, with
% every measurement in the line, and then to this, on the instruction "describi la figura lo mas corto
% posible manteniendo el mensaje". The long version is recoverable from the git history of this file.
% STYLE RULE APPLIED HERE, and it is the one the August pass already set at the head of this file:
% the verb of the sentence carries the finding and the parenthesis carries exactly the numbers that
% decide it, in the same order as the claim. Test for each case: replace the numbers by "measurably";
% if the sentence still states the finding they go in the parenthesis, if it dies they stay in the
% line. FOUR CASES STAY IN THE LINE because the number IS the finding: NR's 0.333 of absorbed noise,
% the 10.5 against 1.03 of INR's two directions, the identical 1.65 and 1.63 of the two least-squares
% arms, and the non-monotone series 1.32 / 1.97 / 2.18 / 1.02, whose SHAPE is the result and which a
% range would destroy. Everything else is in a parenthesis or in a % src.
% THE ORGANISING PRINCIPLE CHANGED, and that is the substantive edit, not the length. This figure is
% where the two quantities every later figure reports are introduced and can be SEEN: the bias is the
% cloud displaced from the truth, the distortion is the reported ellipse away from the empirical one.
% The prose is ordered by those two and no longer by member ranking.
% FIVE CLAIMS ADDED, each measured, none of them in the old version:
% (i)   WHICH CONSTRUCTION this figure uses. Here the distortion comes from a cloud of fits; in every
%       other figure it is H^-1/2 J H^-1/2 from the score, with no fitting. 02_framework.tex:539-541
%       promises exactly this verification, "without leaning on the expansion", and the promise was
%       never collected in prose. The blue number IS the collection.
% (ii)  THE INTERVAL AVERAGE ALONE DOES NOT SHRINK THE DISTORTION. The old text read NR 8.84 -> INR
%       3.28 in the noise pair as the average acting on the second moment. Re-run of
%       fig2_shape_and_floor.R on 2026-08-12: the two eigenvalues go 11.64 / 6.71 to 10.48 / 1.03 and
%       the calibrated one lies 0.1 deg off the noise axis. ONE direction is repaired, the same one
%       where the average repairs the noise bias; the worst direction does not move. The geometric
%       mean hid it, which is the argument for reporting the spread beside the size.
% (iii) ANISOTROPY IS NOT A FUNCTION OF SIZE, counter-examples both ways from the same run: NR on the
%       rates is 14.83 with a spread of 1.15, INR in the noise pair is 3.28 with 3.20, R on the rates
%       is 1.32 with 1.04 (below the null median, so a scalar WOULD serve there) and R in the
%       amplitude pair is calibrated in size at 1.09 and not in shape at 1.20 (null 99th pct 1.102).
% (iv)  THE PARTIAL MEMBERS GO BACKWARDS IN BOTH MOMENTS and in both scalars, not in size alone.
% (v)   The joint four-parameter ratio, 154.65, is DROPPED rather than quoted beside 11.41: the first
%       is sqrt(det) over four parameters and the second over two, so they are m^2-scale and m-scale
%       and the old prose set them side by side as comparable. In error-bar units they are 3.5 and
%       3.4, the same fact twice. Putting it back requires stating the exponent.
% SRC CORRECTED: the aggregate ratios are NOT in figure_2.Rmd's diagnostics chunk, which computes
% three pairs and no aggregate at all. They are D-4_ranking_verdict.md 1a, and the % src now says so.
% NOT A DISCREPANCY, checked 2026-08-12: the corrected range 0.87-1.07 is D-4's four members over six
% pairs and 0.88-1.09 is the eight members over the three drawn pairs. Both hold.
% THE HEADING no longer leads with 150, which was the four-parameter volume of the OPEN-LOOP member
% (not of least squares, which has no four-parameter ellipse at this cell) and which the abstract
% states as an order of magnitude.
\subsection*{Only the boundary-conditioned member is centred and reports its uncertainty correctly in every direction}
\begin{figure}[htbp]
\begin{fullwidth}
\centering
% Rebuilt 2026-07-30 from figures/paper_both/figure_2.Rmd on the full seven-rung roster. The three
% members that were missing resolve from three different engine-commit directories (LSE and VR from
% RESOLVED 2026-08-01: the third column is macro_INR, from the corrected engine (1f7138b, clean
% stamp), run on dirac for this cell alone. It replaced macro_NMR, the build that had lost the N*ms
% term since a3e0a89; the two are different algorithms and not two spellings (nomenclature.md).
% Swapping them changed the column: NMR read 14.0 / 9.8 / 9.0 on the three pairs and INR reads
% 12.6 / 9.0 / 3.3, and the noise bias went from +0.34 to -0.005 in log10. That is the paragraph
% below, and it answers the open question nomenclature.md left on whether INR separates from NR.
% 0ffbda7, NMR from 87889e6, the rest from 1c2ae6f); DATA_DIRS is a search path, so adding 87889e6
% was the whole change. The IR correlation corner that used to be panel B is out of the body figure
% and is a supplement. Panel size in the .Rmd is measured, not set: it is what the columns
% leave once the 7 pt type has taken its space, so raising the type re-sizes the panels by itself.
% The file is 7.00 x 6.82 in and renders at 7.425 x 7.23 in, since width=\linewidth inside fullwidth
% enlarges it by 6.1%. PAGE BUDGET, since this float is now the tallest in the paper: elife.cls sets
% top 2 cm, bottom 2.5 cm, headheight 21 pt and headsep two baselines on letter, which leaves about
% 8.6 in of text block. 7.23 in of art leaves ~1.3 in for the legend, and the legend is cut to 153
% words to fit it.
% RE-RENDERED 2026-08-12, and the defect it fixes is the one an author saw on the page: in the k_off
% row the ratio labels of LSE and ILSE were clipped by the panel edge and printed over their own
% ellipse, and NR's were in the N_ch row. The labels now sit in a band above the data that a top-only
% y expansion opens (YPAD in the .Rmd), and the panel grew by the same amount (ASPECT), so the band
% costs the cloud none of its height: panels went from 0.736 x 0.736 in to 0.736 x 1.236 in and the
% figure from 6.07 to 7.22 in tall. One attempt in between put all three numbers on ONE row and they
% overprinted each other; at 7 pt in a 0.74 in panel they do not fit on a line, so the two rows stayed.
% 7 PT FLOOR, the one item the 2026-08-10 note left open, now discharged: the type is authored at 7 pt
% and W is still 7.0 in, so the 6.1% enlargement puts it on the page at 7.43 pt. Nothing about the
% type changed in this pass; only the panel height and the y range did.
% THE DOUBLED MULTIPLICATION SIGN, found on the page and not in the code: the pair printed as
% "15x xdiv1.4", two multiplication signs with one space between them, which reads as one garbled
% symbol. The anisotropy is now parenthesised, "15x (xdiv1.4)". No overlap test catches this; the word
% boxes never touch.
% THE CAPTION was cut from 392 to 153 words across this pass. What went was duplication of the
% figure's OWN key (the four ellipse names, the three marker names) and of the in-panel notes ("i
% fixed at the truth"), plus the per-pair rationale, which the prose carries. What stayed is the cell,
% the count, what the three numbers mean and where the covariances come from.
% RE-RENDER DONE, note closed 2026-08-10. Figure_2.pdf and Figure_3.pdf both carry the eight
% columns, verified by reading the column headers out of the built PDFs: LSE ILSE NR INR R MR
% VR IR. The prose numbers were already the eight-column ones. The 7 pt floor still has to be
% re-measured on the next render, which is the one part of the old note that is not discharged.
\includegraphics[width=\linewidth]{Figure_2}
\end{fullwidth}
\caption{\textbf{Parameter recovery and the calibration of the reported interval, for the whole ladder at one design cell.} $N_{\mathrm{ch}}=100$, $\widetilde{S} = 0.01$, $\widetilde{\Delta}=0.1$; $10{,}000$ recordings in groups of ten, so one thousand maximum-likelihood estimates per member. Columns are the eight members in ladder order, rows the kinetic, amplitude and noise pairs, with $\sigma$ the instrumental noise level, free in every likelihood member and simulated at $10^{-4}$. Each panel carries the cloud of estimates and four $95\%$ ellipses at its mean, named in the key. Above the cloud, the orange numbers are the magnitude of the distortion, the typical factor by which the reported uncertainty is wrong, and, in parentheses, its anisotropy, how much that factor changes with direction; the blue number is the empirical ellipse over the corrected one. All three read $1.0$ for a calibrated member. Covariances are the Gaussian-Fisher family at the pooled estimate, scaled by the reciprocal of the group size.}
\label{fig:clouds}
% MOVED HERE 2026-08-11 from Figure~\ref{fig:plane}, where it had been supplement 2. Its citing
% sentence is in this subsection, it runs on one cell of the cloud files, and it uses no part of
% figure_4_layout.R. Parentage follows the claim.
\figsupp[The sandwich prediction against the empirical multivariate distribution of the estimates, as a Mahalanobis quantile-quantile plot.]{\textbf{The reported covariance against the empirical one, in all six directions at once.} The squared Mahalanobis distance of each estimate from the cloud mean, sorted and drawn against the quantiles of a $\chi^2$ with six degrees of freedom, under the covariance the member reports and under the distortion-corrected sandwich. A calibrated covariance lies on the identity; above it the reported region is too small and under-covers. The reported Fisher covariance under-covers the nominal $95\%$ region and the sandwich covers it, $0.914$ against $0.947$ to $0.949$. Centring on the cloud mean rather than on the simulation truth tests the second moment alone, which is what the correction claims to repair; the first moment is the separate question the body figure's markers carry. Two things differ from the body figure and both are forced by the sample: the member is \texttt{IR} alone, and the cell is $N_{\mathrm{ch}}=10$ at $\widetilde{S}=0.005$ with group size $100$, which is the one cloud in the set carrying a thousand fits of that size, every other cell being simulated at ten thousand recordings and so giving a hundred. The $\chi^2$ reference is exact at either count, so what the smaller cells lose is resolution and not validity.}{\includegraphics[width=\linewidth]{Figure_2_S1}}
\end{figure}


Figure~\ref{fig:clouds} reads the ladder at one design cell ($N_{\mathrm{ch}}=100$,
$\widetilde{S}=0.01$, $\widetilde{\Delta}=0.1$), and each panel shows the two quantities the rest of
the paper maps. The cloud away from the truth is the bias. The reported ellipse away from the
empirical one is the distortion, and it takes two numbers rather than one: its magnitude, the typical factor by
which the reported uncertainty is wrong, and its anisotropy, how much that factor changes from one
direction to another ($m$ and $a$ of Eq.~\eqref{eq:magnitude-anisotropy}, the two orange numbers of
each panel). Both are one when the member is calibrated, and both are read here from a cloud of a thousand
maximum-likelihood fits, where every later figure reads them from the score with no fitting and the
two constructions agree only to first order (Appendix~\ref{app:diagnostics}).

The truth and the cloud mean coincide only for the two least-squares arms and for \texttt{INR} and
\texttt{IR}. The other four macroscopic members are displaced, and the displacement has a structure:
it lives in the amplitude directions, the unitary current a fifth to two fifths low ($0.103$ to
$0.228$ in $\log_{10}$) and the channel number high by nearly as much ($0.115$ to $0.243$), with the
product $N_{\mathrm{ch}}\,i$ that the mean current determines right to within $0.023$, while no rate
is off by more than $0.04$. The noise level is biased in \texttt{NR} alone, $0.333$ high in
$\log_{10}$, the unmodelled interval variance absorbed into it and returned to $0.005$ by
conditioning on the interval; every other member holds it at or under $0.036$.
% REWRITTEN 2026-08-25 (Luciano's three-fact reduction of this subsection, plus the two bias-structure
% facts he specified). THE SENTENCE THAT STOOD HERE ("Only the macroscopic members that model the
% instant rather than the interval are seriously displaced ... in exactly the members whose interval
% variance is wrong") WAS FALSE AGAINST ITS OWN % src: MR (-0.228 in i, +0.243 in N_ch) and VR
% (-0.117) model the interval and are displaced, MR the worst of the roster; the quoted range "0.103
% to 0.228" itself spans NR to MR. Fossil of the four-member roster (LSE/NR/R/IR), where it was true.
% Its mechanism tail could not carry MR either: from the same state MR's total variance is IR's
% (Methods caption); the gain is what separates them.
% src: papers/1_method/decisions/recompute/fig2_first_moment.R, re-run 2026-08-25 (bias in log10 with
% 2000-resample percentile bootstrap, type 6, n=1000 per member. unitary_current: MR -0.2275, R
% -0.1556, VR -0.1171, NR -0.1026, INR -0.0280, IR -0.0013 unresolved. Num_ch_mean: MR 0.2425, R
% 0.1788, NR 0.1217, VR 0.1149, INR 0.0341, LSE 0.0122, ILSE 0.0075, IR 0.0020 unresolved. rates:
% |on| <= 0.0401 (R), |off| <= 0.0123 (VR). Current_Noise (truth -4): NR +0.3333 [0.3260, 0.3405];
% INR -0.0045, IR -0.0054, R -0.0234, VR -0.0309, MR -0.0363.)
The third marker of each panel is where the score and the information alone put that displacement,
with no fitting, and it lands on the same side as the cloud and close to it: on the unitary current
$-0.287$ against a measured $-0.228$ for \texttt{MR}, and $+0.001$ for \texttt{IR} where the cloud
resolves no displacement at all. Figure~\ref{fig:time} shows the score that produces it. % src: the marker is truth + the program's Probit_statistics_Likelihood_Distortion_Induced_Bias from _battery_sim, read at Num_ch = 100, interval_in_tau = 0.1, noise_in_conductance_tau = 0.1 (which is S_tilde = 0.01), the same cell as the cloud; drawn by figure_2.Rmd:232-259 (dib_vector and centers_for_pair). THE OTHER TWO PAIRS OF THE SERIES, retired to this note 2026-08-13 and stated NOWHERE else, so they come from here if they are wanted back: predicted against measured on the unitary current, -0.181 against -0.156 for R and -0.089 against -0.103 for NR. The measured column is the empirical cloud mean of papers/1_method/decisions/recompute/fig2_first_moment.R, quoted two sentences above. The channel-number half of the same marker reads +0.324 MR, +0.226 R, +0.103 NR and -0.004 IR. NOT RECONCILED: an independent first-order computation from the Figure 3 digests, b = F^-1 E[sum s] at the truth, gives the same signs and structure with magnitudes 25 to 40 per cent smaller (R -0.131 on the unitary current against the marker's -0.181, MR -0.178 against -0.287). Two computations of the same first-order object over different batteries; the gap is recorded in the guion and nothing in the body depends on which one is right. % src: papers/1_method/decisions/recompute/fig2_first_moment.R (amplitude-split table: bias in log10 i, in log10 N_ch and in their sum, per member; log10 i bias MR -0.228, R -0.156, VR -0.117, NR -0.103, INR -0.028, IR unresolved)
On the rates no member is off by more than a tenth, the two least-squares arms least of all, and the
plane is where their rate bias is established (Figure~\ref{fig:plane}A). The interval average is what
removes the displacement, in all three families that carry both arms, and recursion on its own does
not: it leaves \texttt{R} the least centred member of the eight in the rates and further out in the
unitary current than the open-loop member it is built from. % src: same script (per-parameter table; 1000 estimates, 2000-resample percentile bootstrap, type 6. log10 k_on: LSE -0.0148, ILSE -0.0069, NR -0.0210, INR -0.0077, R -0.0401, MR -0.0310, VR -0.0021 unresolved, IR +0.0015 unresolved. k_off: MR -0.0090, VR -0.0123, NR +0.0104, the rest at or under 0.003)
% RETIRED 2026-08-25: "NR, modelling no interval-mean conductance variance at all, absorbs what is
% missing in the noise level instead (0.333 in log10 high); conditioning on the interval returns it
% to 0.005." Absorbed verbatim into the displacement paragraph above (Luciano's fact: the noise is
% biased in NR alone).
% CUT 2026-08-12, following the guion, and it is the only measurement the guion leaves out: "The open
% marker is the truth displaced by the predicted bias, which diagnoses rather than reproduces (right on
% k_off, 71 to 85% of the size in amplitude)." % src was fig2_first_moment.R, predicted-against-measured
% table with the bootstrap interval of the measured value; it over-states NR's noise offset by 1.8.
% Thirty words if it is wanted back. The caption names the marker; nothing else in the body evaluates
% the first-order bias prediction.
% DO NOT restore the retired caption sentence "the open circle sitting on the filled circle means the
% theory predicts that bias". Measured 2026-08-06 and false at this cell in the amplitude and noise
% directions: the prediction is systematically short and the bootstrap interval of the measured
% displacement excludes it. It holds on k_off only.

The distortion orders the ladder the other way, and not monotonically. \texttt{IR} reads $0.99$ to
$1.02$ over the three pairs and \texttt{R} $1.06$ to $1.32$; the two one-endpoint corrections built
on \texttt{R} fall behind it in every pair, \texttt{MR} at $1.29$ to $1.97$ and \texttt{VR} at
$1.43$ to $2.18$, and what recovers the calibration is the second endpoint entering the gain
(Appendix~\ref{app:members}). The members that leave the correlation unmodelled sit an order of
magnitude above, and evenly: \texttt{NR} at $8.8$ to $14.8$, \texttt{INR} at $9.0$ to $12.6$ except
in the noise pair, the one direction its interval variance repairs ($3.3$), and both least-squares
arms at $14.7$ on the rates.
% src: papers/1_method/decisions/recompute/fig2_shape_and_floor.R, re-run 2026-08-25 (per-pair
% magnitude by member: NR 14.83/10.04/8.84, INR 12.59/9.00/3.28, R 1.32/1.09/1.06, MR 1.97/1.53/1.29,
% VR 2.18/1.77/1.43, IR 1.02/1.00/0.99; LSE 14.73 against ILSE 14.77; MR/VR spreads 1.25/1.35/1.24
% and 1.61/1.47/1.41 against R 1.04/1.20/1.07)
% RETIRED 2026-08-25, the three-fact reduction. What stood here, with every number preserved in the
% src above or in this note: (i) "Recursion buys the factor of ten and the interval average closes
% what is left (11.41 to 1.17 to 1.01 averaged over the six pairs the four informative parameters
% form: a standard error 3.4 times too small at the foot of the ladder and correct at its top)" —
% the plane-level version is owned by Figure 4-S1 (INR->IR correlation part 22.1 -> 1.000) and the
% accumulated one by Figure 3F. (ii) "The average without the recursion buys almost nothing on the
% rates or in amplitude, and inside the least-squares family nothing at all" (NR->INR 14.83->12.59
% rates, 10.04->9.00 amplitude). (iii) "Averaging over the interval from one end only is worse than
% not averaging at all" — kept above in measured form; "MR carries the largest unitary-current
% offset in the roster" moved to the displacement paragraph's src.
% "only" removed 2026-08-06: the predictive variance divides the gain, so the variance half moves the
% update too and the two halves are algebraically separable without being separable in what a
% recording does with them (program.md:78, decisions.md:161, Appendix 1).

The factor is not even one number per member: \texttt{INR}'s noise-pair distortion factors into
$10.5$ along the channel-number direction and $1.03$ along the noise axis, the member that repairs
the noise estimate reporting the noise uncertainty exactly right and the channel number with a
standard error $3.2$ times too small, so no single rescaling serves. % src: same script (per-pair eigendirections; in the same pair MR and VR read 1.04 and 1.02 along the noise axis against 1.59 and 2.02 across it)
% RETIRED 2026-08-25, the three-fact reduction: "The anisotropy reaches 3.20 against a floor of 1.08
% from finite sampling alone, and it cannot be guessed from the magnitude, the most distorted panel
% being among the most isotropic (14.83 with 1.15) and one calibrated in magnitude anisotropic (1.09
% with 1.20)." Floor provenance: 20,000 null draws, median 1.038, 95th 1.081, 99th 1.102 at p=2,
% n=1000; the calibrated-but-anisotropic case is R in the amplitude pair. The plane-level anisotropy
% is owned by Figure 4-S2.

What does serve is a matrix. The distortion is measured as one, and the same matrix corrects the
covariance it measures (the sandwich, Appendix~\ref{app:diagnostics}), which the rest of
the paper reports. Whether that correction is right is what this figure settles, dividing the
empirical ellipse of each panel by the corrected one: at one, the correction has recovered the
covariance the estimator actually has. It does for every gating-aware member ($0.88$ to $1.09$ over
the pairs drawn), and at a second cell it lifts the coverage of \texttt{IR}'s nominal $95\%$ region
from $0.914$ to $0.947$ in all six directions at once (Figure~\ref{fig:clouds}--figure supplement 1).
It fails alike for the two least-squares arms, $1.65$ and $1.63$, in two members that differ in every
first-moment quantity, which puts the failure in what they share; they are also the two arms whose
analytic Fisher the differenced check flags (Supplementary File~1). % src: papers/1_method/decisions/recompute/fig2_shape_and_floor.R (corrected column over the eight members and the three drawn pairs: LSE 1.65, ILSE 1.63, NR 1.07/1.05/0.94, INR 1.05/1.09/1.03, R 0.96/0.94/0.96, MR 0.93/0.88/0.91, VR 0.97/0.94/0.95, IR 1.01/1.07/1.05) ; papers/1_method/decisions/D-4_ranking_verdict.md §1a (0.87-1.07 over four members and six pairs, a different set and not a correction of this one) ; the coverage pair is 0.914 against 0.947-0.949 over a thousand fits in six parameters at group size 100, N_ch 10 and S_tilde 0.005

% OPEN 2026-08-06, and the two-arm agreement narrows it: no mechanism is given for the 1.6. The
% candidate is that they are the two members that are not likelihoods, the reported information of
% each being the Gauss-Newton form (n/SSE) sum dmu dmu^T of a mean-only model with the noise scale
% marginalized out under Jeffreys, so the bread of the sandwich is not the Fisher of the process that
% generated the data. That both arms fail identically while their means differ is consistent with it.
% Verify against the producer before writing any of that; the fact stands on its own meanwhile.

Least squares does estimate the channel number, to within $0.012$ in $\log_{10}$, but with the unitary
current pinned at its true value that estimate is the mean current under another name, which is why
its other two columns are empty. What this figure measures is the separation of the two factors, the
harder half and the one the gating variance carries.
% TRIMMED 2026-08-12: the caption already states that the four cells are empty by construction and
% why, and the rule is that the caption describes the display while the prose states the measurement.
% The measurement here is the 0.012 and the mean-current reading.

\subsection*{The uncertainty is misreported for two independent reasons, one within a sample and one across them}
\begin{figure}[htbp]
\begin{fullwidth}
\centering
\includegraphics[width=\linewidth]{Figure_3}
\end{fullwidth}
\caption{\textbf{The calibration cascade in time.} One set of $1{,}000$ recordings ($N_{\mathrm{ch}}=100$, $\widetilde{S} = 0.01$, $\widetilde{\Delta}=0.1$) is scored by each of the eight members, in cost-ladder order and with no fitting. Rows A and B are properties of the data, in grey; C to G are per parameter, the closing rate and the channel number in colour. (A) An example recording, its band the member's own predictive $\pm1$ s.d., annotated with the ensemble-mean log-likelihood. (B) The mean squared standardized residual. (C) The per-interval Fisher information. (D) The mean per-interval score over $\sqrt{F_t}$, standardized so that the two parameters share an axis. (E) The per-interval information ratio $\mathrm{Var}(s_t)/F_t$. (F) The same ratio accumulated, $J_T/F_T$. (G) The autocorrelation against lag, of the residual in black and of the score in colour. Bands are $95\%$ bootstrap intervals over the recordings and curves stop where the information reaches zero. Non-negative quantities are on a logarithmic axis and signed ones on a linear axis, with the calibrated value on the ticks; the four open-loop columns share a y-scale block distinct from the four recursive ones. The channel number is not drawn for either least-squares arm, which cannot separate it from the unitary current.}
% THE FIT, MEASURED 2026-08-12 rather than estimated, because the first trim was budgeted against
% the wrong line width. THE CAPTION IS NOT INSIDE fullwidth: it sets in the 5.62 in text column
% (612 - 170 - 37 pt), about 99 characters a line, not in the 7.4 in of the art. The arithmetic:
%   art          Figure_3.pdf measures 504 x 424 pt, and width=\linewidth inside fullwidth scales it
%                by 7.425/7.0, so it renders 450 pt tall. NOTE the .Rmd's own message says it wrote
%                "7.0 x 7.5 in"; the artefact on disk is 7.0 x 5.89 in. Measure the PDF.
%   page         792 - 57 (top 2cm) - 71 (bottom 2.5cm) - 21 (headheight) - 24 (headsep) = 619 pt
%   left over    619 - 450 = 169 pt, about 15 lines at 11 pt
%   needed       caption 1313 characters = 13.3 lines, plus three supplement short titles at
%                117 + 87 + 100 characters = 3.1 lines. Total 16.3.
% So it is still about 1.3 lines long, which is 14 pt of art, or 0.19 in of authored height. That
% is cheaper to buy from the figure than from the legend: the panels lose 3% of their height and no
% font moves, since the type is authored in points. Trimming the caption further would start
% deleting reading instructions, which is the wrong trade.
%
% TRIMMED TWICE ON 2026-08-12, 422 words to 199 (2657 characters to 1313, about thirteen lines).
% Four cuts, each one a sentence whose content moved into the rewritten Results block above and
% none of which is a fact that lives only here:
%   (i)  the gloss on (F) as the product of (E) and the correlation factor. In the body, in the
%        paragraph beginning "The second source is correlation in time".
%   (ii) the gloss on (G) saying why (E) can hold where (F) does not, and that the residual is the
%        only quantity here an experimentalist can compute. Both are in the body, the second in the
%        three-objects paragraph, which is also where the panel letters are now assigned.
%   (iii) the fractions block, from 85 words to 40: the reading rule and the two extreme columns
%        stay, the paragraph about correlated steps and about resolvable-versus-large goes. The
%        (D) fractions are quoted in the body (45 / 10 / 50 / 4 per cent).
%   (iv) the channel-number exclusion, halved. It has to stay in some form or the missing curve
%        reads as a rendering failure (Luciano, 2026-07-30), and the short form still says why.
% TRIMMED 2026-08-05, about 100 words, to stop the float running 185 pt past the page: LaTeX was
% dropping the last 109 words of this legend and both supplement lines off the bottom of page 19.
% What went, and why each is safe: (i) the factorial sentence with 0.9995 / 20.8 / 0.782 / 1.07,
% which the Results state verbatim 40 lines above at 04_results.tex:186-187; (ii) the sentence
% putting MR outside the R-IR interval and VR further out, which the Results make with their own
% numbers at 04_results.tex:46 (R 1.32, MR 1.97). Nothing cut is a fact that lived only here.
% The reading instructions (rows, axes, bands, the log/linear split) all stayed.
% POINTER REPAIRED 2026-08-10, length pass. Item (i) above pointed at 04_results.tex:186-187, which
% was the design-plane paragraph's second statement of the factorial; that second statement is gone
% and the four corner values now live ONCE, in the accumulated-ratio paragraph of "The calibration
% cascade in time". Item (i) still holds, at the new site. Item (ii) is unchanged.
  % src: projects/eLife_2025/figures/paper_both/figure_3.Rmd (promoted 2026-07-31 from the four-column version, now archived; seven columns, seven rows, two parameters) ; Figure_3_caption.md (the long form this compresses)
% [BESSEL-BOUND] CLOSED 2026-08-11. The bound is PRINTED, at 08_appendix_derivation.tex (moved there 2026-08-12) in the
% paragraph beginning "How much of the kernel the uniform window misses". Everything below was
% written while it was open (2026-08-06) and is kept for its site-specific content only.. FOURTH site making the experimenter-facing offer, and the only
% one inside a caption: row (G) is called "the only quantity here an experimentalist can compute
% on a real recording". The other three are 01_introduction.tex (closing paragraph),
% 02_framework.tex (where the argument is written out) and 04_results.tex (the integrated-
% autocorrelation paragraph). Canonical note at 08_appendix_derivation.tex (moved there 2026-08-12).
% On a real record the analogue filter puts a lag-one term into that same residual
% autocorrelation, 0.075/(f_c*Delta) from the instrument alone, so the offer holds where the
% acquisition window is not short against the filter's response time. NOTHING DRAWN IN THIS
% FIGURE IS AFFECTED: the recordings behind it carry no filter (Methods).
% [OPEN] A caption is the wrong place to qualify a transfer claim and eLife legends are already
% long. The likely resolution is that the qualification is written ONCE in the body, at whichever
% of the three body sites Luciano picks, and this caption is left alone. Recorded here so the
% site is not missed if the wording of row (G) is ever revised.
\label{fig:time}
% SPLIT IN TWO, 2026-08-05. This was one 7.0 x 11 in figure, which a supplement cannot be:
% elife.cls places \figsupp content in the 5.65 in text column, so the art arrived scaled by 0.81,
% took 639 of the ~648 pt of the page, dropped 154 of the legend's 273 words off the bottom of the
% page and shrank the axis numbers to a measured 4.82 pt against eLife's 7 pt floor. Two halves at
% 5.6 x 5.6 in, drawn by the same producer in one loop, keep the panels the size they were on the
% page and fix both. The legend was split with them; the two shared sentences (the least-squares
% ratio row, the six-interval washout floor) are stated where each half can show them.
\figsupp[Per-step information against score variance, for the opening rate and the closing rate.]{\textbf{Per-step information against score variance, the opening rate and the closing rate.} Seven members across the columns, the least-squares arm being the interval-averaged \texttt{ILSE}, and two parameters down the page as bands of two rows: $k_{\mathrm{on}}$ (A, B) and $k_{\mathrm{off}}$ (C, D). The upper row of each band carries the per-interval Fisher information and the score variance as magnitudes, which lie on each other where the member is calibrated at that interval; the lower row is $\log_{10}(J_t/F_t)$ with its $95\%$ bootstrap interval, zero when they do. Per-interval calibration is not monotone along the cost ladder: on $k_{\mathrm{off}}$ the median reads $+0.184$ for \texttt{ILSE}, $-0.038$ for \texttt{NR}, $+0.007$ for \texttt{INR}, $-0.145$ for \texttt{R}, $-0.229$ for \texttt{MR}, $-0.135$ for \texttt{VR} and $+0.033$ for \texttt{IR}, so the cheapest member that models the interval average is better calibrated interval by interval than any of the three recursive members that cost more. Curves stop at the information mask, which is also where the upper axis ends. In the two recursive columns the $k_{\mathrm{on}}$ band reaches that floor within six intervals of the agonist being removed while the $k_{\mathrm{off}}$ band does not. The least-squares ratio row is the same curve in every band of both halves, with a median of $0.183889$ in each: with a single constant variance the parameter cancels out of the ratio, which leaves the true residual variance over the assumed one.}{\includegraphics[width=\linewidth]{Figure_3_S1}}
\figsupp[The same, for the unitary current and the channel number.]{\textbf{Per-step information against score variance, the unitary current and the channel number.} The other half of the preceding supplement, on the same seven members and read the same way: the unitary current (A, B) and $N_{\mathrm{ch}}$ (C, D), the upper row of each band the per-interval Fisher information against the score variance and the lower row $\log_{10}(J_t/F_t)$ with its $95\%$ bootstrap interval. In the two recursive columns the $N_{\mathrm{ch}}$ band reaches the information floor within six intervals of the agonist being removed while the unitary-current band does not, which is the contrast the Discussion draws on how a recording is spent. The channel-number band carries no least-squares panel, which with the gating fluctuations unmodelled cannot separate that parameter from the unitary current.}{\includegraphics[width=\linewidth]{Figure_3_S2}}
\figsupp[The residual and the score in the three parameters not coloured above.]{\textbf{The checks this figure has no room for.} The seven members of the preceding supplements at the same cell, carrying (A) the mean standardized residual, the one of the three properties of $r_t$ not drawn above; (B) the score bias for the three parameters not coloured above, $k_{\mathrm{on}}$, the unitary current and the instrumental noise level; and (C) their score autocorrelation. The first residual moment separates nobody, which is what makes the other two properties the informative ones. In (B) the ordering repeats the one in figure supplement 2: counting informative intervals whose interval excludes zero against a chance level of $0.05$, \texttt{IR} reads $0.067$, $0.050$ and $0.050$, \texttt{INR} $0.100$, $0.100$ and $0.000$, and the three recursive members between $0.409$ and $0.975$. The noise level is the control direction: it is the only parameter measured before the agonist arrives, and its score is white even in the members whose kinetic directions are not, so the memory that carries the accumulated information error lives in the gating directions and not in the instrumental one. Least squares carries no noise curve because its noise level is a plug-in from the same record rather than a free direction, so its score there vanishes identically, which is the same fact as its mean squared residual being pinned at one.}{\includegraphics[width=\linewidth]{Figure_3_S3}}
% SUPPLEMENT ORDER, set 2026-07-31: declaration order must match first-citation order in the text.
% The residual triptych is cited at 02_framework.tex, the four-parameter widening at
% 02_framework.tex and twice more; Diagnostics precedes this section. The family widening is
% cited nowhere and takes the last slot.
\end{figure}

% RETITLED 2026-08-12, third and current. The subsection is organised by what each quantity is able
% to say (closeness, displacement, misreported uncertainty) and its distinctive claim is the split of
% the last one into a per-sample source and a temporal one, which is what Figure~\ref{fig:plane} then
% maps over the design plane. Two earlier titles and why they went:
%   "Calibration holds interval by interval and fails only when the intervals accumulate" was
%   contradicted by the figure's own table, the per-interval ratio excluding one at 95% of the steps
%   for least squares and every step for VR on the channel number; those fractions now live in the
%   legend and what is small per interval is the MAGNITUDE, a factor of 1.6 at the widest.
%   "The likelihood gap splits into what the marginals miss and what independence costs" belonged to
%   the decomposition, which was cut from the body on the same day (see the note above).
% RESTORED 2026-08-12. The float opened at the Figure 2 block and was never closed: commit
% 3cdd986 left \begin{figure} 6 and \end{figure} 5, and the build died at the NEXT float with
% "Not in outer par mode" (l.348), which names the innocent figure and not this one. HEAD~1 was
% balanced, so it entered with that commit. Nothing else about the block was touched.

The miscalibration of Figure~\ref{fig:clouds} is a property of the likelihood and not of the
optimiser, and Figure~\ref{fig:time} shows what produces it, scoring one common set of one thousand
recordings at the same cell interval by interval with no fitting. The rows are ordered by what each
quantity is able to say: how close a member is to the process that generated the data, whether the
estimate it leads to is displaced, and whether the uncertainty it reports is the one it delivers.

The log-likelihood adds up everything a member gets wrong, so the $118.9$ nats the ladder spans in
(A) rank the family without saying how far from the truth its top sits. Every other row is read
against a value a correct likelihood must give, and can therefore test. One entry is not the same
kind of number, both least-squares arms
fitting a noise level to each recording they score. % src: projects/eLife_2025/figures/paper_both/figure_3.html (caption-numbers chunk; ensemble-mean total logL over 1000 recordings, -262.30 +- 0.73 for both least-squares arms to -143.45 +- 0.24 for IR) ; tmp/fig3_tc.R (cv of y_var across recordings at fixed sample_index: 0.478 for both least-squares arms at every interval, 0.000 for NR and INR, 0.093 mean for IR, where the variation is conditioning on the past and not a fitted parameter).
% COMPRESSED 2026-08-13, outside-read pass, 112 words to 57. What went is the entropy-and-divergence
% reading of row (A): "Averaged over recordings from the exact simulator it estimates minus the
% entropy of the distribution that generated the data minus the Kullback-Leibler divergence of the
% member from it, so the 118.9 nats ... are differences in that divergence. It does not say which of
% those things is wrong, and the entropy is shared and unknown". The identity is
% E_p[log q] = -H(p) - KL(p||q), which holds because the ensemble is drawn from the exact simulator
% and every member reports a proper Gaussian density, verified as -0.5*log(2*pi*y_var) - 0.5*r_std^2
% to 1e-13; it is kept here and in the guion and is nowhere in the printed text.
% WHY IT GOES, and this is the SAME call Luciano made on 2026-08-12 on the block immediately below
% ("interesting, diverts from the objective"), which cut the exact two-term split of that divergence.
% The section's claim is calibration, not a Kullback-Leibler ranking, and what survives above is the
% only part the row ordering needs: the ladder ranks and the other rows test.
% WHAT IS KEPT DELIBERATELY: the least-squares qualification. It is on the untouchable list of the
% 2026-08-10 pass and it is what stops a reader comparing that column's logL with the others.
%
% CUT FROM THE BODY 2026-08-12 (Luciano: interesting, diverts from the objective). KEPT HERE BECAUSE
% IT IS MEASURED AND WOULD OTHERWISE BE LOST; full version in the guion, section "La escalera de
% logL se descompone en las otras dos filas".
% For the members that treat the intervals as independent the divergence splits exactly in two,
%     KL(p || prod_t q_t) = sum_t KL(p_t || q_t) + [ sum_t H(p_t) - H(p) ],
% what the marginals get wrong plus the total correlation of the observed sequence, which belongs to
% the data. Both terms are measurable and the identity closes: NR reads 230.15 nats against
% 215.34 + 14.87 predicted, INR 215.37 against 215.34 + 0.09, where 215.34 is the sum of the
% marginal entropies of the recorded current estimated directly (215.17 by Vasicek m-spacing, so the
% marginals are Gaussian to 0.16 nats over the record) and the second term comes from row B, a
% variance wrong by a factor r costing 0.5*(r - 1 - log r). INR is the case that makes the floor
% visible: its marginals are the true marginals to 0.03 nats over a hundred intervals and it still
% pays 71.9 nats for treating them as independent. The three open-loop members agree on that floor
% to 3% (69.76, 71.89, 71.90), as the identity requires since it belongs to the data. Both
% least-squares arms miss the identity by 2.2 nats, which is where the per-recording noise estimate
% stated above was found. Scripts: tmp/fig3_kl.R and tmp/fig3_tc.R.

What remains is measured from three objects and the figure keeps them apart. The residual is the
observation against the member's prediction, both drawn in (A), divided by the standard deviation
the member claimed: its variance is row (B) and its memory the black series of (G). The score is the
gradient of the log-likelihood, which a correct likelihood leaves with zero mean (D) and with
variance equal to the information (E, F), its memory being the coloured series of (G). The
information is the precision the member claims, drawn alone in (C) and serving as the reference in
(D), (E) and (F). Only the residual can be computed on a real recording.

The mean of the score is what says whether the estimate will be displaced. Per interval it departs
from zero at $45\%$ of the informative steps for \texttt{NR} and $10\%$ for \texttt{INR}, at $50\%$
for \texttt{R} and $4\%$ for \texttt{IR}, so averaging the model over the acquisition window is what
removes it. What moves
the estimate is the accumulated score divided by the whole information matrix,
$b = F^{-1}\,\mathrm{E}[\sum_t s_t]$, and read that way the two interval-averaged members cover zero
on all four identified parameters and no other member does: \texttt{R} is displaced $1.38$ marginal
standard errors on the unitary current and $0.82$ on the channel number, and \texttt{MR} $1.96$ and
$1.14$. It is the same quantity the third marker of Figure~\ref{fig:clouds} carries at this cell and
Figure~\ref{fig:plane}A maps over the plane. % src: figure_3.Rmd (caption-numbers chunk, accumulated block added 2026-08-12; fractions of informative steps whose interval excludes zero, and z = b_a/sqrt((F^-1)_aa) with its bootstrap interval, four parameters per member). Dividing each parameter's accumulated score by its own Fisher diagonal instead of by the whole matrix reads -0.34 for R on N_ch where the marginal displacement is +0.82, sign included, since the channel number and the unitary current are strongly correlated. Agreement with fig:plane is claimed in order and not to the digit: this cell gives 0.082 NR and 0.146 R against 0.006 for either interval-averaged member, and fig:plane reports plane medians of 0.099 and 0.110 against 0.003 and 0.002.

Misreported uncertainty has two independent sources and the figure separates them. The first is per
sample, and the residual gives it without any parameter at all: the mean squared standardized
residual is one for the two interval-averaged members, $0.78$ for \texttt{R}, $0.69$ for \texttt{MR}
and $1.15$ for \texttt{NR}, and the score says the same per parameter. Most of
\texttt{NR}'s excess sits in one interval of the hundred, the one in which the agonist steps on,
where its residual variance reads $28.45$ against $0.95$: inside that acquisition window the
occupancy changes, and a member that evaluates its variance at a point of the window cannot see the
change. Least squares makes the same point from the other side, its residual variance being one over
the whole recording by construction while it runs from $0.010$ before the agonist to $1.91$ over the
pulse. % src: figure_3.html (caption-numbers chunk, B block: mean r^2 1.1526 NR, 0.9995 INR, 0.7816 R, 0.6904 MR, 0.8118 VR, 0.9989 IR, and 1.0000 for both least-squares arms, whose 98% of steps exclude one) ; tmp/fig3_kl.R (worst interval and its cost: 12.05 of NR's 14.81 nats at sample 20) ; tmp/fig3_claims.R (r^2 by segment: for either least-squares arm 0.010 over samples 1-19, 1.91 over the pulse, 0.69 through the washout; r^2 at sample 20 is 28.45 NR, 3.06 VR, 0.95 INR MR and IR, 0.61 R).

The second source is correlation in time, and no per-sample check can see it. \texttt{INR} has the
calibrated residual variance of \texttt{IR} and reports its accumulated information twenty times
wrong. The accumulated ratio is the product of the per-sample ratio and a correlation factor, and
\texttt{IR} carries both at one, $1.07$ and $1.01$, while \texttt{R} reaches a comparable $1.07$ as
the product of a per-sample variance it under-reports by $28\%$ and a correlation that inflates it
by $48\%$, so the calibration of recursion alone is a cancellation. At lag one the score
autocorrelation on $k_{\mathrm{off}}$ is $-0.004 \pm 0.004$ for \texttt{IR} against $0.191$ for
\texttt{R} and $0.864$ for \texttt{NR}, the local time correlation of the current that
\citet{milescu2005maximum} named as the dominant error of independent-interval fitting, measured at
the level of the inference. The same memory shows in the residual, which is the half of this row
that transfers to a real recording. Figure~\ref{fig:plane}B carries the second moment over the plane;
these are the two mechanisms behind it. % src: figure_3.html (caption-numbers chunk; accumulated J_T/F_T 13.63 NR, 20.84 INR, 1.074 R, 1.080 IR, and lag-one score ACF on k_off) ; figure_3.Rmd (accumulated block, Ebar and infl, whose product reproduces the accumulated ratio by construction: R 0.724 x 1.483, IR 1.069 x 1.010, MR 0.578 x 2.055, INR on N_ch 1.006 x 11.758) ; tmp/fig3_claims.R (residual ACF at lag one 0.846 either least-squares arm, 0.510 NR, 0.118 R, -0.010 IR, so the score reads 1.6 to 1.7 times the residual where either is large; the score ACF falls below 0.1 at lag 2 for the recursive members and lag 9 to 12 for the rest). TRAP: the drawn ACF does not reconstruct the accumulated ratio arithmetically, 1+2*sum(rho) giving 7.85 for NR against a measured inflation of 16.83, because the score is not stationary.

The information on its own answers a different question, about the recording rather than about the
algorithms: where each parameter is measured. About the channel number and the opening rate it
reaches the numerical floor within six intervals of the agonist being removed, while the closing
rate survives through the mean and the unitary current through the variance of the channels still
open (Figure~\ref{fig:time}--figure supplements 1 and 2). A member that has conditioned on the past
already carries the number of channels still open, so once a parameter reaches the recording only
through that number there is nothing further to learn. The two open-loop members go on reporting
information about the channel count through the washout, $15$ to $16\%$ of their pulse total against
$0.5$ to $0.8\%$, which is the same information counted twice that their accumulated ratio charges
them for. The noise level, measured before the agonist arrives, keeps a white score where the
kinetic directions do not (Figure~\ref{fig:time}--figure supplement 3). % src: figure_3_S1_S2.html (washout table, row C: mean F_t over the last ten intervals for IR, k_on 7.9e-44, N_ch 1.4e-21, unitary current 0.66, k_off 1.13; retention through the washout by algorithm) ; figure_3.html (informative-step counts on N_ch: 80 of 100 for NR and INR, 43 R, 46 MR, 44 VR, 46 IR) ; figure_3_supplement_3.html (noise informative at 100 of 100 intervals; lag-one noise score ACF -0.004 NR and -0.008 R, both covering zero). The two supplement display items are Figure_3_S1.pdf and Figure_3_S2.pdf, both from figure_3_S1_S2.Rmd:348-367; the stale Figure_3_supplement_1.pdf and Figure_3_supplement_2.pdf beside them are written by nothing and are not display items.

% RETITLED 2026-08-13 (Luciano). It read "Averaging removes the bias, recursion removes the
% distortion, and neither does both", and two clauses of the three were wrong.
%  (i) "neither does both" reads, in a section whose every other heading has a MEMBER as its
%      subject, as "no member does both", which is false: IR does both and is the paper's
%      recommendation. Taken literally, on the two operations, it is a tautology of the two clauses
%      before it. Either way the heading ends by negating the section's constructive result.
%  (ii) "the bias" without its qualifier is contradicted two paragraphs down by this section's own
%      concession that least squares carries essentially no RATE bias. The measured claim is about
%      the amplitude bias in N_ch, and the body sentence says "amplitude"; the heading had dropped it.
% NOT FIXED HERE, and deliberately: the heading still implies a one-each division of labour, while
% averaging removes the bias AND the per-sample distortion (NR 47.2 -> INR 1.00 on k_off at noise
% 0.1, N_ch 1e4) and recursion removes none of the bias (0.099 -> 0.110, slightly worse). That
% asymmetry is Figure 4--figure supplement 1's, which owns it, and it does not fit in a heading.
\subsection*{Averaging removes the amplitude bias, recursion removes the distortion, and only their combination removes both}
\begin{figure}[htbp]
\begin{fullwidth}
\centering
% One file since 2026-07-30 (figure_4.Rmd, which replaces the stacked byNch pair): the moment is
% the outermost column grouping rather than a second file, which halves the height and puts both
% moments on one shared scale. 7.4 x 7.3 in, isotropic in the two axes.
\includegraphics[width=\linewidth]{Figure_4}
\end{fullwidth}
\caption{\textbf{The design plane: how wrong each member's report is, in both moments, on one scale.} Ten thousand recordings are simulated at every cell of the grid and scored by each member of the cost ladder. (A) The distortion-induced bias, evaluated at the simulation truth $\theta_{\mathrm{sim}}$: how far the estimate is displaced. (B) The information distortion, evaluated at the pooled optimum $\theta_{\mathrm{pool}}$, where the score vanishes by construction so a displaced gradient cannot contaminate it: how far the uncertainty a member reports is from the one it delivers. Columns nest twice, member and then parameter, $k_{\mathrm{off}}$ and $N_{\mathrm{ch}}$. Both least-squares arms are on the grid and each holds one column per half rather than two, their four-parameter configuration fixing the unitary current and the noise level at the simulated values (Methods). Rows are the channel count, each given a height proportional to the range of instrumental noise the sweep reaches there. Inside a panel the axes are the sampling interval in units of $\tau$ and the dimensionless instrumental noise, one decade the same distance on both. Colour is shared by the two halves: the factor by which the report is wrong, centred on one, pale right and saturated wrong in either direction, blue conservative, clipped at $10^{\pm3}$. What a factor means differs between them and the difference is not cosmetic: in (A) it is a factor on the parameter, so $2$ means the estimate is out by a factor of two, while in (B) it is a ratio of variances, so $2$ means the reported error bar is $\sqrt{2}$, about $40\%$, too narrow. Lines carry two codes: colour says which quantity, dark hairlines cutting the colour field and white lines on a dark casing cutting the distortion-corrected standard error, which asks whether the parameter can be measured at all rather than whether its error bar is honest; dash says which criterion, solid a factor $1.15$ and dashed a factor $2$, the two thresholds used throughout.}
% CAPTION TRIMMED 2026-08-13, 408 words to 323, against the published band of 150 to 260 with an
% observed maximum of 447 (_program, caption survey). What went is design justification rather than
% reading instruction, which is the rule the 2026-08-04 trim of this same caption already applied:
% "so a decade of noise is the same distance in every row"; "so a field's shape can be read as a
% shape"; the clipping count ("touches 22 of 3276 cells, twenty of them the open-loop member, where
% being wrong by 10^3 and by 10^12 say the same thing"), of which the clipping level itself is kept
% because a reader needs it to read the colour bar; the like-for-like gloss on the least-squares
% columns, the fact itself staying; and "Both criteria are edges of the colour bands, which is why
% the dark pair needs no key of its own", which explains the key rather than reading it.
% KEPT WHOLE AND DELIBERATELY: the two-anchors sentence, since it is the only place the reader is
% told why (A) and (B) are evaluated at different points, and the "what a factor means" pair, which
% the caption itself calls not cosmetic.
% TRIMMED 2026-08-04: the sentence explaining why the lines are white rather than a second hue (the
% field is red and blue, so hue would fail for red-green colour blindness where the lines matter most)
% is a design justification, not a reading instruction. It belongs in the figure's build note, which
% already carries it: projects/eLife_2025/figures/paper_both/Figure_4_caption.md. % src: projects/eLife_2025/figures/paper_both/Figure_4_caption.md (the long form this compresses) ; figure_4.Rmd (layout, shared factor scale, the two line codes, the clipping count) ; figure_4_common.R (EXCLUDE_ROWS, nch_noise_span, CRIT_SOL/CRIT_DSH)
\label{fig:plane}
% SUPPLEMENT SET CUT TO TWO, 2026-08-05. Figure 4 declared eleven supplements and the prose cited
% exactly two of them; the other eight were declared, captioned and never mentioned, which is
% something an eLife editor flags. Only the two cited ones are kept, they renumber to 1 and 2, and
% their files were renamed Figure_4_S1 / Figure_4_S2 from the old slot numbers 4 and 5. The removed
% eight and their captions are in git history at the commit before this one; their producers still
% exist under figures/in_progress/figure_4_supplements_20260731/ and figures/paper_both/, so any of
% them can come back, but coming back means writing the sentence in the text that cites it.
% The map version of the sample/correlation split (figure_4_supplement_3.Rmd) was never built; the
% line version below carries the same decomposition and is now supplement 1, which is the slot the
% Diagnostics section cross-references for the split.
% REPLACED 2026-08-11 (Luciano). What stood here was Figure_4_S1.pdf, the same decomposition drawn
% as lines against the channel count for TWO members, which broke this parent's grammar (every other
% supplement here is the plane) and folded the acquisition interval into a band. The map version was
% one of the eight declarations cut on 2026-08-05 for having no citing sentence; it has one now, and
% it carries five members instead of two and two readings the lines version could not show. The swap
% costs the prose nothing: the numbers at the paragraph above are the map's own, verified cell by
% cell against its measured table at noise label 0.1, which is S_tilde 0.01.
\figsupp[Figure 4's second moment factored into a per-sample part and a temporal-correlation part, over the same plane.]{\textbf{The distortion factored: what is a one-interval failure and what is a failure across intervals.} Figure~\ref{fig:plane}'s second moment split into the two congruences the battery computes separately, the per-sample part $\mathbf{H}^{-1/2}\mathbf{J}_s\mathbf{H}^{-1/2}$, the departure of a single interval from the Gaussian the member assumes, and the correlation part $\mathbf{J}_s^{-1/2}\mathbf{J}\mathbf{J}_s^{-1/2}$, the memory across intervals that a likelihood diagonal in time cannot see (defined above). Same plane, same rows, same scale, same six members and the same two criteria as the body figure, nested component over member over parameter. Three readings, and the first is what this page is for. \texttt{NR} and \texttt{INR} carry the \emph{same} correlation part, $16.5$, $19.5$, $21.2$, $21.2$ against $16.5$, $20.7$, $21.8$, $22.1$ on the closing rate across the four decades of channel count, as they must, since they differ by a one-interval variance term and by nothing about time; what separates them is the per-sample part, where \texttt{NR} runs $1.24$, $1.02$, $5.68$, $47.2$ and diverges while \texttt{INR} runs $1.72$, $1.15$, $1.02$, $1.00$ and converges, and on the channel number the same pair ends at $569$ against $1.00$. The divergence the body figure shows for \texttt{NR} is therefore not a temporal failure at all: it is the one-interval variance missing a term that grows with the channel count. Second, the recursion buys the other factor and only the other factor, \texttt{INR} to \texttt{IR} taking the correlation part from $22.1$ to $1.000$ and leaving the per-sample part where it already was. Third, \texttt{R} and \texttt{IR} are equally faithful to a single interval and part company on the correlation part alone, which is the paragraph above. One caveat the reader needs: the factorisation is exact in the log-determinant and not entry by entry, the reconstruction being $\mathbf{K}\,\mathbf{C}\,\mathbf{K}^{\mathsf{T}}$ with $\mathbf{K}=\mathbf{H}^{-1/2}\mathbf{J}_s^{1/2}$, so reading a cell of the body figure as the product of the two cells here is right to within that rotation and no further, $1.024\times1.433=1.468$ against a total of $1.481$ for \texttt{R} at a thousand channels.}{\includegraphics[width=\linewidth]{Figure_4_S1}}
% MOVED 2026-08-11 to Figure~\ref{fig:clouds}, where it is now figure supplement 1. Three reasons,
% and they agree: its only citing sentence lives in the one-design-cell recovery subsection (retitled
% twice on 2026-08-12, now "Only the boundary-conditioned member is centred and calibrated ...")
% this float; it runs on ONE cell out of the cloud files and uses no part of figure_4_layout.R, so
% it was never a figure on this plane; and a quantile-quantile plot is not this figure's grammar.
% The move also repaired an ordering defect here, this parent's supplement 2 being cited before its
% supplement 1. RENAMED 2026-08-12: producer and output are now figure_2_S1,
% so nothing on disk still carries this parent's number. The stale Figure_3_supplement_{1,2}.pdf and
% the old Figure_4_supplement_mahalanobis_qq.pdf, which no producer writes, went to the archive.
\figsupp[Can the distortion be undone by counting fewer intervals? The magnitude of the information distortion against its anisotropy.]{\textbf{The magnitude of the information distortion against its anisotropy.} Two invariants of the same distortion matrix over the same plane, for seven members: both least-squares arms, \texttt{NR}, \texttt{INR}, \texttt{R}, \texttt{MR} and \texttt{IR}. (A) its magnitude, the geometric mean of its eigenvalues and the typical factor by which the reported information is wrong over all directions at once, signed about a null of one. (B) its anisotropy, how much that factor varies from direction to direction, one-sided about a null of one. The two are the orthogonal components of the affine-invariant distance, so (B) at one means a single effective-count rescaling is exact and (A) is the factor it should use, and (B) above one means no such rescaling exists at any factor. The two least-squares arms are the same figure wherever the recording is finely sampled, and part at the coarsest interval: at $\widetilde{\Delta}=1$ and $10^{4}$ channels \texttt{LSE} reads $0.074$ in magnitude, understating its own information thirteenfold, where \texttt{ILSE} reads $1.00$. That is the mean model failing and not the variance model, and it is the deepest of the few cells anywhere on this page where a member is conservative rather than overconfident: $11$ of \texttt{LSE}'s $210$ cells and $4$ of \texttt{R}'s, all of them at the two coarsest intervals and none for any other member.}{\includegraphics[width=\linewidth]{Figure_4_S2}}
% src for the two sentences above: figures/figure_4_source_data/figure_4_source_data_spectrum.csv, pool anchor, param = mag, measured 2026-08-12. Estimate `m` for the quoted 0.074 (ILSE 1.001 in the same cell) and the CI-collapsed Sconf for the counts, which is what the panel paints: below 1/1.15 LSE 11 of 210 (worst 0.0751 at N_ch 1e4, noise label 1, interval 1), R 4 of 210 (worst 0.5994, same cell), ILSE 0, NR 0, INR 0, MR 0, IR 0. The first draft of this caption said LSE was the ONLY conservative member, which R refutes.
% ROSTER, flagged 2026-08-11, CLOSED 2026-08-12 by adding nonlinearsqr_g and re-rendering at seven
% members (14 columns, per_row 0.2072, 6.49 in, 100.0 per cent isotropic measured on the PDF). The
% flag read "as it stands that member has no magnitude/anisotropy evidence anywhere", and the
% reason given for it in the producer was that the un-averaged arm "has no run yet", which stopped
% being true on 2026-08-06 21:57. It now carries the same 210 cells as every other member.
% MR stays and VR is still out: sixteen columns is not measured and puts the cell under 1.4 mm.
% Note this parent's supplement 3 is the same plane for the recursive family, so between the two
% every member of the ladder appears on this plane except VR's invariants.
\figsupp[The same plane for the four rungs of the recursive family, in both moments.]{\textbf{The same plane for the recursive family: R, MR, VR and IR.} Figure~\ref{fig:plane} unchanged in every respect but the roster, the same two moments on the same shared factor scale with the same two line families and the same nesting, over the four rungs inside the recursive family: recursion alone (\texttt{R}), recursion conditioned on the interval mean at one endpoint (\texttt{MR}), the same with the between-endpoint variance removed (\texttt{VR}), and conditioning on both endpoints with that variance restored in the gain (\texttt{IR}). The two rungs the body figure omits are here measured against the two it draws, which is what makes the comparison readable: without \texttt{R} and \texttt{IR} the two partial corrections would be columns with nothing to be measured against. Sixteen columns rather than the body's twenty, since no least-squares arm appears and every member carries the channel number, so the cell is $1.32$~mm against the body figure's $1.00$. The reading is in the text: neither partial correction improves on the member it corrects, at any channel count, on either parameter.}{\includegraphics[width=\linewidth]{Figure_4_S3}}
% SUPPLEMENT ADDED 2026-08-12 (Luciano). It spent one day declared on Figure~\ref{fig:usage}, which
% was the wrong parent: that map only consumes this field as a level set, while what the figure
% itself carries is what each axis of the design buys, and the design plane is here.
\figsupp[What each member actually achieves: the distortion-corrected standard error over the plane, all five parameters.]{\textbf{What each member achieves, as opposed to what it reports.} The standard error each member delivers on each parameter: the square root of the diagonal of the distortion-corrected covariance, over the same plane as Figure~\ref{fig:plane} and in the same orientation. What the design axes buy is read directly off it, and the reading is in the text. It is also the field whose level set at a factor of two draws two of the boundaries of Figure~\ref{fig:usage}. The nesting is transposed against Figure~\ref{fig:plane}'s, the parameter owning the block and the members sitting side by side inside it, because the question here is how the members compare on one error rather than what one member reports across parameters, and adjacency is what answers it. The scale is sequential and not diverging: a standard error has no conservative side, its null is a parameter known exactly, so pale is a parameter pinned and dark is one barely determined, monotone in luminance. The two marks are the set's usual pair read in this quantity's units, an error bar of $15\%$ of the parameter and an error bar of a factor two. The corrected error is the one drawn rather than the reported one because Figure~\ref{fig:plane} spends its second half showing the reported one to be wrong by up to a factor of $74$, so only the corrected version compares members on the same footing. Both least-squares arms hold the unitary current and the noise level fixed and so appear on the rate blocks alone.}{\includegraphics[width=\linewidth]{Figure_4_S4}}
\end{figure}


% OPENING REWRITTEN 2026-08-13 (Luciano). It read "One cell is not a result, and the plane carries
% one the cell cannot: instrumental noise equalises the members", an aphorism plus a turn on the
% word "one", and the objection to it was not only the register. The paragraph opened with a RESULT
% about the plane and introduced the plane four sentences later, so the reader met a conclusion
% about a figure before being told what the figure's axes are. The float's own introduction, which
% stood at the foot of the paragraph, is now its head, and the noise result begins the paragraph
% after it. The one sentence added says what the figure is a function of, which is a fact about the
% experiment; how the layout encodes it (nesting, colour scale, row heights, isotropy) stays the
% caption's, per the one-owner rule, and none of it is restated here.
% SECOND AND THIRD PASS the same day, Luciano's wording both times. The sentence first read "the
% design varies three things ... so either moment is one number as a function of five", which
% counts the variables instead of naming them and is the same cleverness as the aphorism this
% paragraph had just lost. The version that replaced it carried three lists at once, the design
% axes, a re-gloss of the two quantities after a colon, and the parameters, and it opened by
% looking backwards at Figure 2. What stands now opens at this figure and drops the glosses, which
% the Figure 2 paragraph already owns ("The cloud away from the truth is the bias. The reported
% ellipse away from the empirical one is the distortion"). Two things are held against the drafts:
% N_ch is named as an AXIS in words (the channel count) and as a PARAMETER in symbols, since it is
% both and nothing else marks the two apart; and no row count is stated, the caption saying only
% "from 10 to 10^4" and the number of rows never having been checked against the producer.
% NOT QUALIFIED HERE, and the caption owns it: "every member ... on two parameters" is loose for
% the least-squares arms, which hold the rate column alone because their four-parameter
% configuration fixes the unitary current and the noise level.
Figure~\ref{fig:plane} maps the bias (A) and the distortion (B) of every member against the
acquisition interval, the instrumental noise and the channel count, on two parameters,
$k_{\mathrm{off}}$ and $N_{\mathrm{ch}}$.
% CUT 2026-08-13 (Luciano): "The two moments are drawn side by side rather than one per figure, so
% the least-squares arm stays alongside the likelihoods it is being compared with." It fails the
% test this section applied to the deleted figure roll call, being a claim about the document that
% the reader cannot check where it stands, and it raises the alternative layout in order to answer
% it. The reason is kept here because it is recorded nowhere else: the note above the
% \includegraphics justifies the single file by halved height and a shared scale, not by this.
Instrumental noise equalises the members. Where it grows past the gating variance every rung reports
honestly, so the differences
between them are a property of the regime in which the fluctuations still carry information, not of
the algorithms in general. Where that crossing sits is worth having in the plane's own coordinates,
because it moves: at the open probability of one half the instrumental variance overtakes the
gating variance at $\widetilde{S}/N_{\mathrm{ch}}$ equal to $0.0025$ at the shortest window and
$0.14$ at the longest, which puts it at $\widetilde{S} = 0.025$ for ten channels sampled fastest
and near $\widetilde{S} = 1400$ for ten thousand sampled slowest. % src: analytic, from the emission model and the run values in Methods; the locus and its three constants are derived in the note at 02_framework.tex beside the units paragraph (h = 0.0024834 / 0.023414 / 0.141917 at Delta_tilde = 0.01 / 0.1 / 1, so 0.0025*10 = 0.025 and 0.1419*1e4 = 1419).
The cell the two preceding figures use sits a factor of $234$ in variance below it, well inside the
regime where the gating fluctuations carry the information. % src: same, h(0.1) * N_ch / S_tilde = 0.023414 * 100 / 0.01 = 234; in amplitude sqrt(234) = 15.3.
% MOVED 2026-08-13 to the head of the subsection: "Figure 4 repeats the two moments over the whole
% grid, split by moment rather than by member so that the least-squares arm stays alongside the
% likelihoods it is being compared with." It is the figure's introduction and it stood last.
% ADDED 2026-08-12. The two sentences above answer a question the paragraph had been raising and
% dropping since it was written: it asserts that the members equalise where the instrumental noise
% grows past the gating variance, and then never says where on the plane that is, while the reader
% is looking at the axes. The locus is a LINE and not a level of the noise axis, which is the part
% worth printing, since it moves four decades with the channel count and nearly two with the
% window; that is also why no single S_tilde marks the boundary and why the collapse further down
% this section is in S_tilde / N_ch. Derivation and its two caveats (p = 0.5 only, agonist plateau
% only) are in the framework note, which owns them; nothing is restated here but the numbers.
% NOT ASSERTED, and deliberately: whether the sweep actually spans the crossing at every channel
% count. The rows of Figure 4 reach different noise ranges (its own caption says so), so that is a
% coverage claim about the runs and it has not been checked against the data.

More channels do not repair \texttt{R}. Taking the median over the seven
intervals at $\widetilde{S} = 0.01$, its $k_{\mathrm{off}}$ distortion (B) sits on a flat floor near $1.4$
across four decades of channel count while \texttt{IR} converges to one; \texttt{NR} moves the other
way, from $22$ to $77$, and the least-squares arm holds a floor near $13$. % src: projects/eLife_2025/figures/paper_both/figure_4.Rmd (caption-numbers chunk; R 1.372/1.440/1.439/1.42, IR 1.324/1.099/1.000/1.00, NR 21.92/16.83/31.32/76.52, LSE 11.79/13.06/13.04/12.97 across N_ch 10 to 10^4)
In the channel-number direction the two recursive members separate in sign as well as in size:
\texttt{IR} stays conservative throughout, while \texttt{R} crosses from conservative to
over-confident and keeps rising. % src: same chunk (IR 0.851/0.936/0.987/1.00, R 0.866/1.100/1.230/1.26)

The first moment (A) carries a separate cost, and it separates on the other axis. The median absolute
bias in the channel number is $0.099$ and $0.110$ in $\log_{10}$ units for the two members that do
not average the model over the acquisition interval, against $0.003$ and $0.002$ for their
interval-averaged counterparts, so averaging is what removes the amplitude bias while recursion alone
does not remove it and slightly worsens it: \texttt{R}'s channel-number bias of about $0.14$ in
$\log_{10}$, close to a $35$ to $40\%$ error, does not shrink as channels are added, and \texttt{NR}
carries half of it in the same direction. % src: projects/eLife_2025/figures/paper_both/figure_4.Rmd (caption-numbers chunk; median |bias| in N_ch by member) ; same chunk (R 0.126/0.148/0.138/0.138, NR 0.073-0.084)
\texttt{IR} is at or near zero everywhere. Least squares has a rate bias of
essentially zero over the same plane, and that concession is worth making plainly: the classical fit
recovers the rates, and what it gets wrong is how well it says it recovered them. % src: projects/eLife_2025/figures/paper_both/figure_4.Rmd (caption-numbers chunk)

Recursion is what removes the distortion, in the amounts above. A member that takes one axis and not
the other repairs one failure and keeps the other, so the family cannot be ordered on a single scale,
and nothing in that is specific to ion channels: it holds for any state model whose observations are
averages over a window.

% PARAGRAPH DELETED 2026-08-13, outside-read pass. It read: "Conditioning localises the error as
% well as shrinking it. Counting the fraction of cells of (B) that depart from one by at least 15%,
% R departs on a fifth to a half of the plane in four of the five parameter directions, against at
% most a twelfth for IR, whose departures are confined to the few-channel corner." % src:
% figure_4.Rmd caption-numbers chunk; R 0.4375/0.5625/0.2232/0.3125/0.0000 and IR
% 0.0179/0.0804/0.0000/0.0268/0.0000 for k_on, k_off, i, N_ch and the noise level.
% WHY: it is the per-parameter form of the SAME statistic as the 93/70/31 paragraph below, which
% pools the same +-15% count over parameters. IR's worst direction, 0.0804, is that paragraph's 93%
% read one direction at a time. The one thing it carried alone, that IR's departures are confined to
% the few-channel corner, is folded into that paragraph, where it now does the work of pointing at
% Figure 6.
% THE NUMBERS ABOVE ARE NOT LOST and they are the better-behaved half of the pair: they are per
% member on a common basis, where the three percentages below are on three different cell sets.
Instrumental noise moves the whole picture continuously, and it moves the window-ignoring members
furthest. The closing-rate distortion of least squares (B) falls from its floor near $13$ to one over
about three decades of added noise, at every channel count, and \texttt{R} follows the same pattern
about three decades lower. % src: projects/eLife_2025/figures/paper_both/figure_4.Rmd (caption-numbers chunk; at N_ch 10, LSE 11.79 at label 0.1, 1.14 at 100, 1.00 at 1000; at N_ch 10^4, 12.97, then 2.54 at 10^4, 1.15 at 10^5, 1.00 at 10^6; R 1.42, 1.03 at 100, 1.00 at 1000 at N_ch 10^4)
The added variance carries no gating memory, so as it grows it dilutes the temporal structure a
window-ignoring likelihood fails to model until there is nothing left to fail on, and the channel
count at which each member's error bar becomes honest rises with it, which is the crossover
Figure~\ref{fig:usage} draws as a boundary.

Over the grid the three members share, counting every parameter at every cell, the reported uncertainty
is within $\pm15\%$ of the delivered one for $97\%$ of \texttt{IR}'s points, $78\%$ of \texttt{R}'s and
$37\%$ of \texttt{NR}'s; over its own wider grid \texttt{IR} reads $95\%$, and what it has left is
concentrated at few channels and low noise. % src: tmp/recount_9370_31.py re-run 2026-08-25 over projects/eLife_2025/figures/figure_4_source_data/figure_4_source_data_distortion.csv (anchor pool, comp total, statistic m: 1018 / 818 / 384 of 1050 each, over the 210 cells shared by IR, R and NR; on the CI-collapsed Dconf the same counts read 97/81/38. IR's own grid: 1402 of 1470 over 294 cells; of its 68 misses, 50 sit at N_ch 10 and 50 of 68 at noise labels 0.5 and below, i.e. S_tilde <= 0.05).
% THE THREE-DIFFERENT-CELL-SETS DEFECT (08_length_plan.md, phase 5 item 4) CLOSED 2026-08-25,
% Luciano's call this session: the sentence moves to the shared sub-grid. The counts are now taken
% from the figure's own source data, so the prose and the plane figure count the same object; the
% previous battery-directory counts (93/70/31 over 560/238/224 cells, figure_4.Rmd) are retired to
% this note and must not be quoted beside the new ones, since the two grids differ. The abstract's
% "calibrated over almost the whole measured range" rests on this sentence.
Least squares carries no entry, because its distortion matrix has four dimensions against the
likelihood members' six and the two counts are not commensurable (Methods). The coverage is unequal
and has to be quoted with the number: \texttt{IR} spans eleven channel counts from $5$ to $10^4$
across the twelve noise levels of the ladder (Methods), not on a full rectangle, and the others cover
less.

The anchor itself was checked over the full roster against a differenced Fisher information
(Supplementary File~1). For \texttt{INR} and \texttt{IR} the analytic Gaussian anchor and the
differenced construction coincide within one per cent at every channel count. For \texttt{NR} the
two part company as channels are added, so its largest factors at $10^{4}$ channels narrow by about
a third when re-anchored on the differenced form, without being overturned; at the coarsest
intervals there the differenced Fisher is not positive at all, the same ill-conditioned corner the
figure greys. For both least-squares arms re-anchoring would enlarge the reported factors by about
$1.3$, so their verdict is conservative under the anchor.
% src: tmp/fim_vs_gaussian_table.py 2026-08-25 over projects/eLife_2025/figures/data/a202e03/figure_3_fim_*_nsim_10000_*_battery_sim.csv
% (medians of geomean(eig C), C = inv(sqrt(G_b)) F_b inv(sqrt(G_b)), src/core/likelihood.cpp:3509:
% INR 0.995-1.000 and IR 0.994-1.001 across the four decades; NR 1.16 / 1.36 at 1e3 / 1e4 with
% retained maximum 1.97 at delta 0.1 and the three coarsest intervals indefinite; LSE 0.71-0.78,
% ILSE 0.71-0.78. Full table and the direction convention: supplementary_file_1.tex S1.5.)

The flat floor comes from the split of the distortion into a per-sample part and a correlation part
(Figure~\ref{fig:plane}--figure supplement 1). On the closing rate at $\widetilde{S} = 0.01$ the
per-sample part goes from $1.09$ to $0.998$ for \texttt{R} and $1.10$ to $1.007$ for \texttt{IR}
across the four decades of channel count, % src: projects/eLife_2025/figures/paper_both/figure_4_S1.Rmd, caption-numbers chunk (sample-part medians, k_off at noise 0.1). Re-knitted 2026-08-05 on figures/data/{1c2ae6f,87889e6,0ffbda7}: R 1.086 -> 0.998, IR 1.099 -> 1.007, both reproduced to the last digit
while the correlation part rises for \texttt{R} from $1.35$ to $1.47$ and falls for \texttt{IR} from
$1.24$ to $1.00$. % src: same chunk, correlation-part medians; re-knitted 2026-08-05, both reproduced exactly
The two are equally faithful to a single interval and part company on how much temporal dependence
they leave in the score, which is what conditioning on both endpoints removes.

Two features of the plane are easy to misread. Where the acquisition interval is far shorter than the
channel's correlation time, successive samples carry redundant information and a faithful likelihood
has to register that redundancy, so a large correlation distortion at the shortest intervals of (B)
is the correct cost of oversampling rather than a defect. And at $N_{\mathrm{ch}}=10^4$ with
$\widetilde{\Delta}=1$ the amplitude directions of \texttt{R} are not
identified: the Gaussian Fisher information is ill-conditioned there, so a displacement that costs
almost no likelihood is amplified into a large parameter shift. Those cells are drawn grey in both
halves, $23$ of them in (A) and $73$ in (B); their sign and their existence are real, their
magnitude is a property of the conditioning. % src: projects/eLife_2025/figures/paper_both/figure_4_common.R:219-231 (the greying rule) ; counts printed on every source() of figure_4_common.R, re-measured 2026-08-12 on the body figure's six-member roster: "greyed (unidentified): bias 23 cells, distortion 73 cells"
% [BESSEL-BOUND] CLOSED 2026-08-11. The bound is PRINTED, at 08_appendix_derivation.tex (moved there 2026-08-12) in the
% paragraph beginning "How much of the kernel the uniform window misses". Everything below was
% written while it was open (2026-08-06) and is kept for its site-specific content only.. Canonical note at 08_appendix_derivation.tex, the acquisition-kernel subsection (moved 2026-08-12).
% THE COLLISION. The first sentence of this paragraph uses the word "oversampling" for the
% GATING redundancy: samples closer together than the channel's correlation time repeat each
% other, a faithful likelihood must register that, and a large correlation distortion at the
% shortest intervals is the correct cost. That is right and it is the paper's own result. The
% risk is that "oversampling" is also the word an electrophysiologist uses for sampling above the
% anti-alias filter, which is a different redundancy with a different source, and at the short
% end of this axis the two would coexist in a real record. A referee reading this sentence can
% reasonably ask which one the figure is showing.
% THE ANSWER, and it is clean: only the gating one is here. The simulator emits the uniform window
% with white noise and no analogue stage (Methods), so the measured plane carries no filter at
% all, and the correlation distortion at short Delta is gating redundancy with nothing else mixed
% into it. That is a strength of the design and it currently goes unsaid.
% [OPEN] Six words in the sentence would discharge it, e.g. "carry redundant information about
% the gating". If a fuller version is wanted, the natural place is not here but Methods, where
% the absence of an analogue stage is already stated; this paragraph exists to pre-empt two
% misreadings and a third would crowd it.

\subsection*{Deflating the interval count does not repair the error bar, because the distortion differs by direction}
\begin{figure}[htbp]
\begin{fullwidth}
\centering
% PROMOTED TO THE BODY 2026-08-10 (Luciano). The producer was figure_6B_four.Rmd and is now
% figure_5.Rmd; the corner figure and the usage map moved to 6 and 7 in the same pass. Two design
% decisions were settled with the promotion and the producer's header owns both: FOUR members, the
% factorial of the two axes, because R is the only recursion-without-averaging member and without it
% the reader attributes the repair to the rest of the ladder in a lump; and ONE shared y, because the
% vertical distance between members at the same x is the quantity the figure delivers, and because
% IR's internal structure on a free axis is the estimation floor of the statistic (1.043 +- 0.010),
% so a per-panel scale draws the bootstrap at full panel height. The free-scale, own-tau_int reading
% is the supplement below.
% WHY IT IS IN THE BODY AT ALL, since it is the one figure of the set with no abstract sentence
% behind it: the Introduction's closing paragraph makes the offer this figure is the only display
% item for, and eLife's own norm is a median of six body figures with 5-7 the modal band
% (_program/figure_supplements_decision_guide.md, n = 1,079 articles), so the seventh is not the cost.
% ONE ROW OF FOUR PANELS, and 2026-08-11 that stopped being a default and became an argument. A 7x4
% grid with the interval on the rows, the noise in colour and the channel count in the symbol was
% built, drawn and rejected the same day. What decided it was measured while arguing about it: the
% noise and the channel count enter only through r = S_tilde/N_ch (the paragraph above, and Figure 5
% supplement 2), so tau_int and the interval are the whole parameterization and there is no third
% number for a row to carry. The grid's one real gain, the within-a-rate threshold, is not claimed
% anywhere in this manuscript, so the body figure is not asked for it; if it ever is, the producer's
% header says where the grid lives. The producers of the two rejected encodings are kept on disk as
% figure_5_Nch.Rmd and figure_5_noise.Rmd.
% WHAT WAS KEPT FROM THE DETOUR: the lines. Each is one channel count at one interval swept over the
% noise, in design order, and the four per colour superimpose, so the collapse is in the body figure
% without a panel spent on it. The x tick labels that used to collide at 7 in are also fixed.
% ONE EDIT THE PRODUCER STILL OWES, recorded in its header and not changing what is said: the
% ill-conditioned corner (N_ch 1e4, Delta.k_off 1) is not excluded here although Figure 4 greys it by
% a rule it owns, and it alone sets the floor of the shared axis. With the lines it is now traceable
% in the drawing: it is the low end of the lightest sweep of R and of least squares.
\includegraphics[width=\linewidth]{Figure_5}
\end{fullwidth}
\caption{\textbf{What each likelihood does to the reported error bar, against a number measurable on a single recording.} Every mark is one cell of the design grid, scored over the same $10{,}000$ recordings as Figure~\ref{fig:plane}, with no binning. The horizontal axis is shared by the four panels and is a property of the least-squares fit alone: $\tau_{\mathrm{int}}$, the integrated autocorrelation of its standardized residual in normalized form, which counts how many intervals a fit effectively has for every interval it believes it has and reads $1$ for a white residual. It is what a reader holds before choosing a method, the classical fit having been made anyway. The vertical axis, also shared, is the distortion of the reported information: the point is its magnitude, the geometric mean of the eigenvalues of the distortion matrix, and the bar runs from that magnitude divided by its anisotropy to the magnitude multiplied by it, so the bar's ends are the reader's best- and worst-determined directions and its length is what no single correction factor can remove. The error-bar scale is its square root. No threshold is drawn, the criterion belonging to the reader. Colour is the acquisition interval $\widetilde{\Delta}$, the second number the reader has, and it is drawn because the first does not suffice: at one measured $\tau_{\mathrm{int}}$ the distortion still differs between sampling rates. Each line joins one channel count at one interval swept over the instrumental noise, so twenty-eight are drawn in each panel and seven can be seen: the four channel counts land on one another, because the noise and the channel count act only through their ratio and $\tau_{\mathrm{int}}$ already reads that ratio off the record; the same cells are drawn against each of those two axes in turn in Figure~\ref{fig:memory}--figure supplement 2. The four members are the two axes of the family crossed, \texttt{ILSE} averaging over the window without recursion, \texttt{INR} adding the gating variance to it, \texttt{R} recursing without averaging and \texttt{IR} doing both. The least-squares distortion matrix has four dimensions and a different parameter set from the six of the likelihood members (Methods), so each panel reports what its own fit says about the parameters that fit estimates, and nothing is pooled across the two.}
% CAPTION TRIMMED 2026-08-13, 441 words to 363, the longest in the set against a published band of
% 150 to 260 and an observed maximum of 447. It stays above the band: what is left is the two axis
% definitions, the panel roster and the least-squares dimension caveat, and cutting further starts
% on those. Three cuts, none of them a reading instruction:
%  (i)  the gloss on what a mark MEANS to a reader ("so a member's mark at a given tau_int says what
%       that member would do to the error bars of a recording whose classical residual carries that
%       much memory"). The body owns the offer and states it at the head of this subsection; the
%       short form, that tau_int is what a reader holds before choosing a method, stays.
%  (ii) the error-bar conversion table (2 -> 41%, 4 -> 95% becomes 68%), which 02_framework.tex
%       states verbatim in the paragraph that defines the distortion it converts and which is that
%       section's by the one-owner rule. What stays is that the scale is the square root, which is
%       what a reader needs to read the vertical axis.
%  (iii) "This is why two numbers are enough and neither the noise nor the size of the patch needs an
%       axis of its own here", a justification of the layout. The supplement pointer it carried is
%       kept on the sentence that states the collapse.
% src: projects/eLife_2025/figures/paper_both/figure_5.Rmd (was figure_6B_four.Rmd; four members on
% one shared scale, 210 cells each, tau_int of the ILSE residual at the pool anchor) ;
% figure_5_variant_2members.Rmd (the header that owns why tau_int is the right independent variable,
% why it is not self-referential, and the five measured readings of the panel)
\label{fig:memory}
\figsupp[Each member against the memory left in its own residual, rather than in the least-squares residual.]{\textbf{Each member against the memory left in its own residual.} The whole ladder, with the horizontal axis of each panel now the integrated autocorrelation of that member's own standardized residual, which a reader cannot compute without running the member: a mechanism panel rather than a decision one. It asks whether the memory a member fails to remove predicts the information it then misreports. At a fixed sampling interval it does, for every member with any range in the statistic, the rank correlation between the two reading $+0.92$ for \texttt{ILSE}, $+0.96$ for \texttt{INR}, $+0.93$ for \texttt{MR} and $+0.91$ for \texttt{VR} over the whole plane and about $+0.99$ within one interval. What conditioning on both ends of the interval does is leave the statistic no range to have: \texttt{IR}'s own $\tau_{\mathrm{int}}$ spans $0.98$ to $1.07$ over the grid against $1.0$ to $17.4$ for the least-squares arms. The implication is one-directional and the counterexample is in the panel: memory implies distortion, while a white residual certifies nothing, \texttt{NR} carrying $73$ cells with $\tau_{\mathrm{int}}<1.05$ of which an eighth are distorted, the worst by a factor of $2217$. Each panel is on its own scales, which is what this reading needs and what the body figure declines.}{\includegraphics[width=\linewidth]{Figure_5_S1}}
% src: projects/eLife_2025/figures/paper_both/figure_5_S1.Rmd (was figure_6BB.Rmd). The rank
% correlations are measured 2026-08-10 from the same spectrum and cell-scalar files the figure reads,
% own tau_int against own magnitude over the 210 shared cells: ILSE +0.92, INR +0.96, MR +0.93,
% VR +0.91, LSE +0.80, NR +0.74, R +0.57, IR +0.40, and about +0.99 within a single interval for
% every member with range. THE COUNTEREXAMPLE IS NR AND IT IS NOT A DETAIL: at noise 0.1 its own
% tau_int FALLS from 7.86 to 1.34 across four decades of channel count while its distortion RISES
% from 6.7 to 99, so the statistic points the wrong way along that axis for the worst-calibrated
% member. The caption states the implication one-directionally for that reason. Do not write
% "the residual autocorrelation indicates the distortion in the non-recursive members" without it.
\figsupp[The instrumental noise and the channel count act only through their ratio.]{\textbf{The instrumental noise and the channel count act only through their ratio.} The same cells as the body figure, drawn twice. Each line is one channel count at one acquisition interval swept over the instrumental noise, twenty-eight per panel, and the two columns differ in nothing but the horizontal coordinate: on the left the instrumental noise $\widetilde{S}$, on the right the noise per channel $r = \widetilde{S}/N_{\mathrm{ch}}$. On the left the four channel counts are four separate curves per colour, a decade apart; on the right they merge, so seven of the twenty-eight are visible. Nothing is fitted, rescaled or normalized between the columns. The top row is the horizontal axis of the body figure itself, $\tau_{\mathrm{int}}$ of the least-squares residual, and it collapses with the rest: a reader who measures $\tau_{\mathrm{int}}$ on their own record has measured $r$ without knowing it. The vertical bars are the bootstrap quantiles of each cell, absent on the top row alone, so the agreement can be read against the width of the points being compared: each point is known to about $3\%$ and the four channel counts land within $1\%$ of one another, which is closer than any one of them is known. The number in each right-hand panel is the residual disagreement between cells that share $r$ exactly at the same interval, with no interpolation. The $5\%$ of \texttt{ILSE} and the $8\%$ of \texttt{IR} are within the scatter of fifty-six groups of three-per-cent points; the one large value, \texttt{R}'s, is the single ill-conditioned cell at $N_{\mathrm{ch}} = 10^4$ and $\widetilde{\Delta} = 1$ that Figure~\ref{fig:plane} greys out and this figure does not, without which \texttt{R} agrees to $23\%$. In the bottom row \texttt{IR}'s departures from unity are the width of its own error bar and not structure.}{\includegraphics[width=\linewidth]{Figure_5_S2}}
% TRIMMED 2026-08-13, 376 words to 294, against the 150-260 published band; it was the longest
% caption of the Figure 5 set and remains over the band, which is a deliberate stop. Nothing
% measured was dropped, and that is why it stops there: the
% 3%/1% sentence stays because the body sentence that used to carry it was trimmed on the same day
% and left this caption its owner; the R corner and the 5/8% excursions stay because a reader sees
% those numbers printed in the panels. What went is redundancy with the body figure's own caption
% ("twenty-eight are drawn and seven can be seen"), the tail "and needs no separate estimate of
% either quantity", the quantile-row housekeeping folded into a clause, and the "worth reading for a
% second reason" framing of the last sentence.
% src: projects/eLife_2025/figures/paper_both/figure_5_S2.Rmd, written 2026-08-11. It draws the
% measurement that licenses the body figure to be one row: if the two design axes are one axis, then
% tau_int and the interval are the whole parameterization. The residual-disagreement numbers are on
% the 56 groups that share r exactly at one interval; TRAP, and it was fallen into twice on the day
% this was written, in this file and in figure_5.Rmd's collapse chunk: group on the integer decade,
% round(log10(S)) - round(log10(N_ch)), never on the double S/N_ch, which is not bit-identical
% between (0.01, 10) and (0.1, 100) and silently splits matched cells apart, understating the
% collapse (it read 76% instead of 88% for R).
% SUPPLEMENT ADDED 2026-08-12: tau_int over the design plane. It is the surviving half of what was
% figure_4_supplement_lag_kappa.Rmd, whose other half was the condition number of the Gaussian
% Fisher. The two were on one page and could not share a colour bar, tau_int running 1.00 to 9.3
% and kappa over decades, which made it the only page in the set with two bars. Kappa was dropped
% rather than moved: its only job in the paper is the masking rule kappa > 3e4, which Methods
% states in prose, and no sentence anywhere claims anything about it over the plane. The producer
% is now figure_5_S3.Rmd. It is a Figure 5 supplement and not a Figure 4 one
% because the quantity is this figure's own horizontal axis.
\figsupp[The memory left in the standardized residual, over the design plane, for all eight members.]{\textbf{Where a recording leaves memory in its own residual.} The horizontal axis of the figure above, drawn over the design plane instead of used as a coordinate: $\tau_{\mathrm{int}}$, the integrated autocorrelation of the standardized residual in normalized form, for all eight members, one column each. It counts how many intervals a fit effectively has for every interval it believes it has, and reads one for a white residual. It is computed from the data against the member's own fitted mean and variance, with no parameter, no derivative and no second likelihood to compare against. The two least-squares arms agree to within a per cent over most of the plane and both are drawn, for the classical fit at the sampling instant and for the same fit averaged over the window. The scale is one-sided, since a residual cannot carry less memory than none. Eight columns rather than the twenty of Figure~\ref{fig:plane}, one per member and none per parameter, so the cell is $2.7$~mm, the largest in the set.}{\includegraphics[width=\linewidth]{Figure_5_S3}}
% TRIMMED 2026-08-13, 202 words to 172. What went is the third statement of the offer ("the one
% quantity here an experimentalist can obtain from a single recording"), which 02_framework.tex, the
% Introduction's close and the body paragraph above already make, and the "each have their own panel
% to stand in" construction. The definition of tau_int stays even though the body caption above it
% carries the same words: this legend has to stand alone in the supplement list.
\end{figure}


% NEW 2026-08-05. The measurement is figures/paper_both/figure_4_S2.Rmd, built
% 2026-08-02, whose own header states the argument: "Panel B at 1 means an effective-N rescaling is
% exact, and the factor it should use is panel A. Panel B above 1 means no such rescaling exists at
% any factor." The manuscript already leaned on it in one clause of the fig:limit subsection ("which
% is why the magnitude and the anisotropy are reported as a pair") and never cited the figure.
% ROSTER NOTE, updated 2026-08-06: that supplement runs six members and names the least-squares arm
% ILSE, the interval-averaged configuration. ILSE is now defined in Theory (the window axis crosses
% all three levels of the gating description), so the sentences below use the tag directly.
% CLOSED 2026-08-12. The supplement was re-rendered on SEVEN members, with the un-averaged arm
% beside the averaged one, so the choice the note below left open ("either re-render on seven
% members or say 'the interval-averaged arm'") was taken the first way. The chain of stale claims
% this block records is worth keeping as a warning, because each was true when written and the run
% landed hours later:
%   2026-08-05, and FALSE from 2026-08-06 21:57: "Every least-squares quantity on the design plane
%   is ILSE: the grid was never run with the un-averaged arm (zero nonlinearsqr_g files under
%   figures/data), and LSE proper is measured only at the single cell of Figure 3 and drawn in
%   Figure 1."
%   2026-08-06, and understated: "figures/data/a202e03 carries nonlinearsqr_g at four channel
%   counts by four noise labels as battery cells". It carries thirty cells, the same thirty as
%   ILSE, at seven sampling intervals each. That directory was still filling while the note was
%   being written, which is exactly how a count gets frozen too low.
% Measured 2026-08-12 off figures/figure_4_source_data/*.csv (210 cells for each arm) rather than
% off a file count, because the source data is what the figures actually draw.
A reader who accepts the distortion will reach for the cheapest repair before changing likelihood:
declare that the recording holds fewer independent intervals than it appears to, and deflate the
count. That number is already measured, and Figure~\ref{fig:memory} carries it as an axis. The integrated autocorrelation of the standardized residual,
which counts how many intervals a likelihood effectively has for every
interval it believes it has, reads $1.008$ for \texttt{IR}, $1.30$ for \texttt{R}, $3.93$ for
\texttt{NR} and $9.26$ for \texttt{ILSE} at $\widetilde{S} = 0.01$. % src: projects/eLife_2025/figures/paper_both/figure_4_supplement_lag_kappa.Rmd (integrated residual autocorrelation over the plane; values at noise label 0.1)
% ANCHOR MISMATCH, found 2026-08-10 while promoting the figure, and left for Luciano rather than
% silently edited. Three of these four numbers are identical at both anchors and reproduce exactly
% from figure_4_source_data_cell_scalars.csv (ILSE 9.260, R 1.299, IR 1.008). NR does not: 3.935 is
% the sim anchor and the pool anchor reads 4.463, and Figure 5 draws the pool anchor, as every
% distortion quantity in this section does. Either quote 4.46 here or say which anchor the series is
% on. It is one digit and it is the only number in the paragraph that moves.
The classical fit therefore behaves as though it had about nine times more independent observations
than the recording supplies, and the integrated autocorrelation is a quantity an experimentalist can
compute on a real recording without an ensemble and without a known truth.

Read along that axis the family splits on the recursion and on nothing else (Figure~\ref{fig:memory}). From a white classical
residual out to one carrying twenty times its own length, \texttt{ILSE} and \texttt{INR} run from
calibrated to factors of $57$ and $33$, while \texttt{R} and \texttt{IR} stay inside $1.26$ and
$1.08$ over the same sweep. % src: projects/eLife_2025/figures/paper_both/figure_5.Rmd (numbers chunk, size over the 210 shared cells)
The split has a reading in the residuals themselves: across the whole grid \texttt{IR} leaves
its own record carrying between $0.98$ and $1.07$ of its own length, where the least-squares
arms leave up to $17.4$ (Figure~\ref{fig:memory}--figure supplement 1). % src: projects/eLife_2025/figures/paper_both/figure_5_S1.Rmd (numbers chunk, own tau_int min and max per member: IR 0.976-1.07, R 0.976-1.39, ILSE 0.990-17.36, LSE 0.989-17.34). Placed HERE, ahead of the collapse paragraph, so that the two Figure 5 supplements are cited in ascending order, the same rule 02_framework.tex records for the Figure 3 supplements.
The channel count and the noise level enter only through their ratio, which is the instrumental
variance against the gating variance the channels carry, and that ratio is what the measured
statistic reads: at matched $\tau_{\mathrm{int}}$ the least-squares distortion agrees to within
$1\%$ across four decades of channel count and the five of instrumental noise that accompany them,
and holding the ratio fixed holds $\tau_{\mathrm{int}}$ to within $3.5\%$ and the distortion of every
member to a median of $1$ to $2\%$, with the exception of the coarsest sampling, where the largest
patch is the ill-conditioned corner and least squares there disagrees between channel counts at the
same ratio by a factor of $13.6$. Drawn against each of the two axes in turn, the four channel
counts are four separate curves against the noise and one curve against the ratio
(Figure~\ref{fig:memory}--figure supplement 2). % TRIMMED 2026-08-13: the closing clause, "and the typical disagreement that survives, one per cent, is smaller than the three per cent to which any one of those cells is itself known", is that supplement's own caption in the same words ("each point is known to about 3% and the four channel counts land within 1% of one another, which is closer than any one of them is known"). The citing sentence stays, which is what the supplement needs. % src: figure_5.Rmd, collapse chunk, which is where this sentence has always pointed and where the chunk now actually is; figure_5_Nch.Rmd carries the same measurement extended to the full ladder, and figure_5_S2.Rmd draws it. Re-measured 2026-08-11 at the pool anchor. The cells that share r = S_tilde/N_ch at different (noise, N_ch) are separate simulations of different preparations, 8 r values and 56 (interval, r) groups, so their agreement is the whole content and there is no fitting in between. tau_int spreads by 0.1 to 3.5%; the distortion by a median of 1-2% for every member. MAXIMA over the ladder, quoted three ways because they differ, as all cells / dropping the N_ch = 1e4 corner cell at Delta_tilde = 1 / dropping the whole coarsest interval: LSE 1256-99-99, ILSE 5-5-5, NR 29-29-29, INR 12-12-9, R 88-23-23, MR 11-11-11, VR 16-16-9, IR 8-7-4, in per cent. The sentence quotes the third. The two large ones are the corner cell alone. Drawn as Figure 5 supplement 2 (figure_5_S2.Rmd), left column against S_tilde and right column against r.
% PROVENANCE CORRECTED 2026-08-11. This sentence used to cite a "collapse chunk" of figure_5.Rmd,
% which has no such chunk (its chunks are setup/data/render/numbers) and no record of ever having had
% one. The claim itself reproduces, and four of its five numbers reproduce exactly, so nothing here is
% retracted. Two things were wrong in the note: it read "at most 4% for ILSE" where ILSE measures 5.1%,
% and it gave those maxima without saying they exclude the coarsest sampling interval, at which the
% sentence's "within a few per cent" was false by a factor of 13.6 for least squares.
The acquisition interval does not, which is why the panel carries it in colour, and it is the one
other number the reader already has: at $\tau_{\mathrm{int}}\approx2.9$ the same measured memory carries a
distortion of $8.9$ at the shortest interval and $1.6$ at $\widetilde{\Delta}=0.5$. % src: same file, verified 2026-08-10 against figure_4_source_data (at tau_int 1.02-1.04 and interval 0.01, ILSE reads 1.134/1.140/1.138/1.144 across N_ch 10 to 10^4 at noise 100 to 10^5; at tau_int 2.86-2.87 and interval 0.01 it reads 8.859/8.859/8.884/8.908, against 1.56-1.63 for the interval-0.5 group at tau_int 2.6-3.0)
The relation is a mechanism and not a coincidence of that axis: scored against the memory each member
leaves in its own residual rather than in the classical one, the two track each other in every member
that has any memory to leave (Figure~\ref{fig:memory}--figure supplement 1). It runs one way only.
The residual a member leaves white is no certificate, \texttt{NR} misreporting its information by up
to a factor of $2217$ in cells where its own residual is indistinguishable from white, which is the
same blindness a recursive likelihood has by construction, stated at the start of this section.

Where on the design plane a recording leaves that memory is itself a map, drawn for all eight
members in Figure~\ref{fig:memory}--figure supplement 3.
% TRIMMED 2026-08-13, outside-read pass, 84 words to 27. What went is the second sentence, "It is
% the one quantity in this paper an experimentalist obtains without an ensemble, without a known
% truth and without a second likelihood to compare against, so it is the axis of the figure above
% and the coordinate on which a reader places their own recording before choosing a method."
% It is the THIRD statement of the offer and this file's own [BESSEL-BOUND] note already says so:
% the argument is written out at 02_framework.tex, the offer is made in the Introduction's closing
% paragraph, and the figure's caption carries it as well ("It is what a reader holds before choosing
% a method"). The citing sentence for the supplement is what this paragraph is for and it stays.
% [BESSEL-BOUND] CLOSED 2026-08-11. The bound is PRINTED, at 08_appendix_derivation.tex (moved there 2026-08-12) in the
% paragraph beginning "How much of the kernel the uniform window misses". Everything below was
% written while it was open (2026-08-06) and is kept for its site-specific content only.. Third and last of the three sites that hand the residual
% autocorrelation to a reader with their own data; the other two are 01_introduction.tex, closing
% paragraph, and 02_framework.tex, where the argument is written out. Canonical note at
% 02_framework.tex.
% ON A REAL RECORDING the analogue filter contributes to this same statistic, a lag-one residual
% correlation of 0.075/(f_c*Delta) from the instrument alone, so tau_int carries a filter term
% that has nothing to do with how the fit models the gating. It is small where the record is
% sampled at or below the filter's bandwidth, 1.5% at f_c*Delta = 5, and it is the dominant term
% below that. The values quoted in this paragraph (1.008 / 1.30 / 3.93 / 9.26) are unaffected:
% they are measured on simulations with no filter in them.
% WHAT TO WRITE, if anything is written here: a condition rather than a caveat. The clause
% "without an ensemble and without a known truth" is what makes the offer, and the honest form
% adds one more thing it does need, which is a record whose acquisition window is not short
% against the anti-alias filter. Ten words.

That repair works if the distortion is a scalar, every direction wrong by the same factor. The bar
on every mark of Figure~\ref{fig:memory} is that test taken one cell at a time, and
Figure~\ref{fig:plane}--figure supplement 2 takes it over the whole plane, through the magnitude and
the anisotropy of the distortion (defined above). It is not a scalar. Both least-squares arms and the open-loop interval member over-state their information by a factor
of about $5$ to $6$, and the per-direction factor spreads by about $3.2$ around it, flat across four
decades of channel count, so directions exist that are wrong by twenty and directions exist that are
barely wrong at all. % src: projects/eLife_2025/figures/paper_both/figure_4_S2.Rmd (table chunk, medians over the seven intervals at noise 0.1, pool anchor, re-measured 2026-08-12 on the seven-member render: magnitude LSE 5.79/6.03/6.12/6.13, ILSE 5.79/6.10/6.15/6.08, INR 5.16/5.01/4.94/4.93; anisotropy LSE 3.15/3.20/3.21/3.25, ILSE 3.17/3.20/3.17/3.22, INR 3.18/3.26/3.28/3.30 across N_ch 10 to 10^4. The two arms agree to two digits at this noise level; where they part is the coarsest interval, in the caption.)
No single effective count serves both, and the claim a reader would otherwise make, that the
residual autocorrelation supplies the correction, is refuted by the same measurement that would have
supplied the number: the two boundaries of the map below are boundaries on which likelihood to use
and not on which correction to apply.

The three recursive members separate on different axes of the same pair. \texttt{R}'s magnitude
drifts up with the channel count while its spread sits flat at about $1.18$, so its failure is mostly
a size it could in principle correct for. \texttt{IR} is at one in magnitude at every count and its
spread falls toward one; at ten channels that spread is $1.25$, above the strict criterion, which is
the same few-channel corner every other measurement in this section points at. \texttt{MR} is worse
than \texttt{R} on both axes and at every channel count.

That reading is on the two invariants; the distortion itself, over the same plane and for the four
rungs of the recursive family, says the same thing and adds the second partial correction
(Figure~\ref{fig:plane}--figure supplement 3). On the closing rate \texttt{MR} is worse than
\texttt{R} at every channel count and the gap widens as channels are added, while \texttt{VR} sits
on \texttt{R} along the rates and above it on the channel number. Neither partial
correction buys anything on this plane, and the second endpoint is what moves the closing rate to
$1.01$.
% TRIMMED 2026-08-13: the two four-decade series (MR 1.45 / 1.78 / 1.78 / 1.94 against R 1.42 / 1.49
% / 1.48 / 1.46, and VR within 5% of R on the rates at three counts of four and 17 to 20% above it on
% the channel number) go to the % src at the foot of this subsection, which already carries all four
% of them to the digit. Applying this file's own test: replace the numbers by "measurably" and both
% findings, that MR is worse at every count with a widening gap and that VR sits on R, survive the
% substitution, so they belong in the parenthesis and not in the line.

The interval average is doing two different jobs, and the plane separates them where a single cell
cannot. In the second moment the two least-squares arms are the same member at every acquisition
interval: their distortion on the closing rate reads $121.0$ against $121.9$ at the finest window
and $1.98$ against $2.04$ at the coarsest, and stays within three per cent of itself in all seven.
In the first moment they are the same member too until the window reaches half a relaxation time,
where the un-averaged arm picks up an offset of $0.013$ in $\log_{10}$, a factor of $1.03$, and the
averaged one stays at $0.0009$. So to a fit that does not model the gating variance the interval
average does one thing only, which is to keep a window comparable to the kinetics from aliasing the
deterministic mean. To the two members that do model it the same step is worth a factor of $9$ to
$50$ on that offset at every interval, and a factor of $240$ on the channel number at the coarsest.
The contrast is the one the recovery cell reports at a single window, and it holds over two decades
of them. % src: projects/eLife_2025/figures/paper_both/figure_4.Rmd (interval-pairs-along-delta chunk, 2026-08-12 render; medians over the four channel counts at noise label 0.1, read from m and not from Bconf/Dconf; tables A, C and D)

The interval axis carries one reading of its own. The distortion of every member that does not
recurse scales as the inverse of the window, each halving of the window
roughly doubling it, since a shorter window puts more correlated intervals into the same recording
and the information-matrix equality fails on the count of them rather than on their length. % src: same chunk, table D. THE TWO RANGES WERE HERE UNTIL 2026-08-13: 121 to 2 over two decades for the least-squares arms, which the paragraph two above states in full ("121.0 against 121.9 at the finest window and 1.98 against 2.04 at the coarsest"), and 201 to 2.8 for INR, which is stated nowhere else and comes from here. The two
recursive members are flat instead, \texttt{R} reading between $1.41$ and $1.50$ at every window, so
what it gets wrong is not a property of the window at all. The one departure from the scaling is
\texttt{NR} at the coarsest window, which reads $70$ against
the $6.4$ of the window before it and sits in the same ill-conditioned corner as the greyed cells
above. % src: same chunk, table D

\subsection*{Sampling a hundred times faster buys the noise level and nine per cent of a rate}

Everything so far asks whether a member's report is honest. The complementary question is what the
report is worth, and it is answered by the standard error each member actually delivers, the square
root of the diagonal of its distortion-corrected covariance
(Figure~\ref{fig:plane}--figure supplement 4). Read along the acquisition interval it says something
the design axes do not otherwise make visible: sampling faster is not more data, it is more
\emph{independent} data, and only the parameters that live in the uncorrelated part of the signal
collect it.

Sampling a hundred times faster, from $\widetilde{\Delta}=1$ to $\widetilde{\Delta}=0.01$ at
$N_{\mathrm{ch}}=10^{4}$, divides the error on the instrumental noise level by $11.8$ to $20.5$
across the noise sweep, close to the factor of ten that a hundredfold increase in independent
samples would give and never far from it. It divides the error on the channel number and the
unitary current by $6.7$ and $7.7$ at the lowest noise every member reaches, and that gain decays
monotonically with instrumental noise, to $1.7$ at $\widetilde{S}=10$ and to nothing beyond. And it
divides the error on either rate by at most $1.09$, anywhere on the plane: a hundredfold increase in
samples buys a rate nine per cent. % src: projects/eLife_2025/figures/paper_both/figure_4_S4.Rmd (what-each-axis-buys chunk, 2026-08-12 render, medians over the six members at N_ch = 1e4; the three non-decade noise levels are one member only and are excluded, and the amplitude ratios at noise label 1e7 are the undetermined corner where the sandwich is unbounded)

The three answers are one statement, ordered by the correlation time of whatever each parameter is
measured from. The instrumental noise level is the scale of a white process, so every additional
sample is independent and its relative error follows the sample count: measured against the
interval, the log-log slope is $0.60$ for \texttt{IR} and $0.60$, $0.52$ and
$0.58$ for the other three that carry it, against the $0.5$ of an inverse square root, while over
the whole noise sweep the same quantity varies by a quarter. It is the one parameter whose error the
noise level does not set and the sampling rate does. % src: same chunk, Q1a and Q1b
The amplitude pair is measured from the gating fluctuation, which is correlated on the scale of the
kinetics, so faster sampling buys the part of its spectrum above that scale, which is real and
bounded, and buys nothing once the instrumental noise has covered the fluctuation. The rates are
measured from the shape of the deterministic transient, which any interval shorter than the
relaxation already resolves, so the extra samples are redundant. Averaging $n$ samples of a
correlated process divides the variance by $n/\tau_{\mathrm{int}}$ and not by $n$, and
$\tau_{\mathrm{int}}$ is the quantity Figure~\ref{fig:memory} puts on its horizontal axis.

One consequence is practical and belongs to the reduction rather than to the acquisition. The
sampling rate of the rig is not what this axis is: a record acquired fast and uniformly is reduced
before it is fitted by grouping consecutive samples and averaging them, and the average of window
averages is the average over the window they span, so the reduced record is the same observable at
a larger $\Delta$. What the numbers above say is what that grouping costs. For a kinetic question
it costs almost nothing, and for an amplitude one it costs the whole gain. The interval members
take the window as an argument rather than as a constant, so the groups need not be equal and the
schedule can follow the transient; the members written for an instant describe a different random
variable once the record is grouped at all.
 % src: projects/eLife_2025/figures/paper_both/figure_4_S3.html (measured-table chunk, 2026-08-12 render: medians over the seven intervals at noise label 0.1, read from m; k_off R 1.4246/1.4869/1.4810/1.4561, MR 1.4477/1.7780/1.7768/1.9422, VR/R 0.955/0.998/1.023/1.091, N_ch VR/R 1.135/1.204/1.168/1.168, IR k_off 1.0055 at 1e4) % src: same chunk (magnitude R 1.09-1.24, MR 1.30-1.56, IR 1.02-1.00; anisotropy R 1.29-1.19, MR 1.40-1.39, IR 1.25-1.04)

\subsection*{In the few-channel corner the boundary-conditioned member misreports two parameters in opposite directions}
\begin{figure}[htbp]
\begin{fullwidth}
\centering
\includegraphics[width=\linewidth]{Figure_6}
\end{fullwidth}
\caption{\textbf{Where the boundary-conditioned filter stops reporting an honest error bar, and which of the two discarded moments is responsible.} The same measurement as Figure~\ref{fig:plane}, the Gaussian information distortion at the pooled optimum on the same runs, resolved on the corner where \texttt{IR} departs: on Figure~\ref{fig:plane}'s scale a factor of $1.5$ is a faint tint and here it needs an axis. The three blocks are the distortion and the two parts it decomposes into: (A) the total, (B) the part carried by the per-sample non-Gaussianity of the score, and (C) the part carried by its correlation across intervals; their product reproduces the total to within $1\%$ over the whole grid. Each block is a grid of the design plane in the orientation of Figures~\ref{fig:plane} and~\ref{fig:usage}: the channel count increases to the right, the dimensionless noise $\widetilde{S}$ increases upward, and inside every cell the horizontal axis is $\widetilde{\Delta}$ from $0.01$ to $1$, shared by every panel and labelled once per block. The vertical axis is the distortion factor on a logarithmic scale, shared by all twelve columns so that they can be compared, with the green line at $1$ marking a report that is neither over- nor under-confident. Colour is the parameter. Bands are $95\%$ percentile bootstrap intervals over recordings, of median half-width $0.03$. The two readings the figure supports, and the limits on them, are in the text.} % src: projects/eLife_2025/figures/paper_both/Figure_6_caption.md (full caption, with the component paths and the composition check) ; figure_6.Rmd
% RENAMED fig:design -> fig:limit on 2026-08-05, in the same pass that removed the information-budget
% subsection, which is what the note here asked for. Nothing else referenced the old label.
\label{fig:limit}
\end{figure}


Figure~\ref{fig:limit} resolves the corner where \texttt{IR} departs, on a scale
Figure~\ref{fig:plane}B cannot show. Finding that corner was half the object of the measurement, since
a diagnostic that never fires on the member it is applied to says nothing about what it can resolve.

The departure is two-sided within a single cell. At ten channels and $\widetilde{S} = 0.005$ the
reported error bar of the closing rate is too small by a factor of $1.62$ at the shortest interval
and that of the channel number too large by a factor of $0.65$, so one likelihood is over-confident
about a rate and conservative about a count in the same recording. % src: as the figure
The two parts of the decomposition answer to the acquisition interval in opposite directions: the
correlation part of the closing rate falls as the interval coarsens while the per-sample part rises,
so sampling more coarsely buys back the memory and pays for it in the per-sample non-Gaussianity. % src: as the figure. The four values were in the line until 2026-08-13: correlation 1.52 to 1.27 and per-sample 1.06 to 1.19, across the interval axis at ten channels and S_tilde = 0.005, the same cell as the sentence above.
Both parts vanish by $\widetilde{S} = 1$,
where the instrumental noise has made the emission Gaussian enough that neither closure is strained. % src: as the figure

Two limits travel with the figure. The family applies both of its Gaussian approximations at every
step and the score sees only their product, so a departure measured here cannot be assigned to one of
them; separating them needs the microscopic recursion, a companion paper's subject. And the grid is
truncated where the effect grows: the closing rate's departure is still accelerating at the lowest
simulated noise, reading $1.22$, $1.37$ and $1.52$ over the last three noise steps at ten channels,
so the figure locates the onset and not the far side. % src: as the figure

\subsection*{No measured regime carries amplitude information and an honest classical error bar at the same time}
\begin{figure}[htbp]
\begin{fullwidth}
\centering
\includegraphics[width=\linewidth]{Figure_7}
\end{fullwidth}
\caption{\textbf{What a macroscopic recording can be asked for: a usage map over channel number and instrumental noise.} The plane is the design space, the number of channels in the patch against the dimensionless instrumental noise, both on logarithmic scales, at one acquisition interval $\widetilde{\Delta}=0.1$. Four boundaries divide it. Colour is the parameter the boundary is about. Line type is the question: solid asks whether the parameter can be measured at all, drawn where the distortion-corrected standard error crosses a factor of two; dotted asks whether the reported error bar of least squares is honest, drawn where its information distortion crosses $1.15$; dashed asks the same of the likelihood's own error bar, and is the boundary below which the Gaussian closure is misspecified. Ribbons are the same boundary swept over $\widetilde{\Delta}$ from $0.01$ to $1$, so a ribbon says where a reader's own sampling rate puts the boundary and is not an error bar. Dots are the measured crossings and the line through them is their fitted power law, drawn faint where it leaves the simulated range, which is marked. Vertical strips mark the two vertices, each a range set by the strict and the loose criterion. The boundaries partition the plane into five regions, keyed by what a recording in each can be asked for and named in order in the text, and the three labelled boxes mark where common preparations sit. The map is a concept map, not a phase diagram.
% 2026-08-04: added the colour regions and the preparation boxes, which dominate the rendered figure
% and which the caption did not mention at all, so the legend did not stand on its own. Removed the
% explanation of why the opening rate binds first, which is a result and is in the text.
% VERIFY against the render: the count of five regions and of three boxes comes from the Results
% prose and from a figure audit, not from opening Figure_7.pdf.
Its boundaries are level sets of continuous diagnostics, so a looser criterion moves each of them by up to a decade in noise without changing the layout.} % src: projects/eLife_2025/figures/paper_both/Figure_7_caption.md and figure_7.Rmd header (encoding: one visual channel per meaning; ribbon = interval sweep)
\label{fig:usage}
% Figure 7 carries no supplements. The `regions` and `frontiers` variants were superseded on
% 2026-07-31, when the canonical map became the plain map, and figure_7.Rmd now writes them into
% figures/archive/figure_6_superseded_20260731/ rather than beside the body figure. Their
% \figsupp declarations pointed at files that no longer exist beside the manuscript and were the
% compile failure. Nothing was lost with them: the "concept map, not a phase diagram" caveat both
% carried is already the last sentence of the caption above. What is not carried anywhere now is
% the five named regions and the three recording footprints; if that is wanted back it is a new
% figure and not a restored reference.
% NO SUPPLEMENT HERE, 2026-08-12. The delivered standard error was declared on this parent for one
% day and moved to Figure~\ref{fig:plane}, where it is supplement 4. This map only CONSUMES that
% field, as the level set at a factor of two that draws two of its boundaries; the readings the
% figure itself supports are about what each axis of the DESIGN buys, which is the other parent's
% subject. Parentage follows the claim, and the claim moved with the paragraph.
\end{figure}


Figure~\ref{fig:usage} collects the measurements into one plane, the channel number against the
instrumental noise at a single acquisition interval $\widetilde{\Delta}=0.1$, in which the measured
cells are the input and the four boundaries are the output.

Read upward at a fixed channel count the recording loses capabilities in a fixed order, which gives
five regions, numbered from the bottom. % src: papers/_program/nomenclature.md (regions of the usage map; 0 at the bottom, 4 at the top)
Region~0, below the lowest boundary, is where the Gaussian closure is itself misspecified and the
recording is not a macroscopic problem, the microscopic regime seen on this plane. In region~1 the likelihood delivers the unitary current, the channel number and an honest
interval while least squares reports an interval several times too narrow; in region~2 the amplitude
pair stops being separable and the rates alone survive; in region~3 least squares is calibrated too
and buys the same answer more cheaply; in region~4 nothing useful is left.
The two boundaries that say a parameter has stopped being measurable are level sets of one
continuous field, the standard error each member actually delivers, which is the square root of
the diagonal of its distortion-corrected covariance rather than of the one it reports; that field
is drawn over the whole plane, for every member and all five parameters, in
Figure~\ref{fig:plane}--figure supplement 4, so a reader who wants a criterion other than the
factor of two drawn here can take it off that map. % src: projects/eLife_2025/figures/paper_both/figure_7.Rmd:129 (the solid boundary is q = "se", threshold 0.3 in log10 = a factor 2, on k_on, k_off, i and N_ch) and figure_4_supplement_standard_error.Rmd (the field itself, five parameters, six members)
The lowest boundary is the
one with a negative slope, $-0.56$ in
$\mathrm{d}\log_{10}\mathrm{noise}/\mathrm{d}\log_{10}N_{\mathrm{ch}}$, because instrumental noise
makes the emission more Gaussian and so relaxes the occupancy closure while hurting everything else. % src: projects/eLife_2025/figures/paper_both/figure_7.html (fitted slopes)
% TODO(Luciano) 2026-08-11: THE -0.56 DOES NOT MATCH TODAY'S RENDER and is not edited here, because
% which of two numbers it should be is a judgement. figure_7.html now reports -0.50 for BH, the
% drawn boundary itself (figure_7.Rmd:865, fitline on `irbad`, "below it IR is dishonest"), and
% -0.62 for `floor`/k_off in the crossings table. -0.56 is exactly the midpoint of the two, which
% may be how it was obtained or may be coincidence. The other two slopes this paragraph quotes,
% 1.01 and 1.03, match the render to the digit, so this is one number and not a stale paragraph.
% The 2026-08-11 relabelling pass cannot be the cause: its diff is comments, display strings and
% one variable rename, with the file-matching regex byte-identical, so no computed value moved.
Regions~1 to~4 sit inside the Gaussian regime, and what orders them is how much the data carry
rather than whether the closures hold. The telegraphic regime is a statement about $\Delta$, so it is
not a region of this plane; it is reached along the interval axis of Figure~\ref{fig:plane}.
% The last three sentences came from 02_theory.tex on 2026-08-06. They also close a loop that was
% circular before: this paragraph pointed at the caption for the region names and the caption
% pointed back here. The names are now here, which is what the caption claims.

The boundary at which the classical error bar becomes honest is one the mechanism predicts: the
gating variance grows with the channel count while the instrumental variance does not, so the ratio
of the two decides whether a window-ignoring fit is honest. Fitted in logarithms over four measured
crossings in each of two parameter directions, it runs as instrumental noise proportional to channel
number, with slopes of $1.01$ and $1.03$, a measured slope rather than an assumed scaling. % src: projects/eLife_2025/figures/paper_both/figure_7.html (crossings at noise label 164 and 130 at N_ch 10, 1.28e3 and 1.53e3 at 100, 1.54e4 and 1.51e4 at 1000, 1.62e5 and 1.59e5 at 10^4, channel-number and closing-rate directions)

The ordering of the boundaries is the result the map exists to state: there is no region of the
measured plane in which the amplitude still carries information and the classical error bar is
simultaneously trustworthy, the two being separated by one to three decades of noise everywhere
measured. The scope of that statement has to be stated with it: the classical arm here is
\texttt{ILSE}, least squares on the deterministic mean current averaged over the acquisition window,
with the unitary current held fixed, in its four-parameter
configuration. % CORRECTED 2026-08-11 (Luciano): this said "least squares" unqualified while the map is built on the interval-averaged arm. figure_7.Rmd reads its classical arm from files matching `nonlinearsqr`, which is ILSE (figure_4.Rmd:329-330; the data-file tokens are crossed on purpose, see figure_1.Rmd), and the un-averaged `nonlinearsqr_g` is never read there. The producer's own labels were saying LSE and were corrected in the same pass, so the rendered figure now names ILSE too. REASON RETRACTED 2026-08-12, and NOT replaced with another one. The note here said the choice "looks deliberate" because ILSE was the arm with the high-noise coverage the map needs, running to noise 1e7 at N_ch 1e4 where IR stops at 1e5. That is a reason against IR and NOT against LSE: the un-averaged arm reaches the same 1e7 corner on the same thirty cells (figure_4_source_data, 210 cells for each arm, measured 2026-08-12). So nothing on record says why this map uses one arm rather than the other, and the honest state is that it is unexamined. What IS measured: over the plane the two arms agree to a median 1-3 per cent in both quantities this map takes level sets of, the total distortion and the corrected standard error, with the median widening at the coarsest sampling (3.4 and 9.4 per cent at Delta_tilde = 1) and with individual ill-conditioned cells disagreeing without bound. The map's ribbon sweeps Delta_tilde from 0.01 to 1 (figure_7.Rmd:95), so the coarse end is inside it and "the boundaries would not move" is NOT established. Drawing the second arm is one line in figure_7.Rmd's read_algo pattern; whether the boundary moves is a question for Luciano and not one to answer by assertion. It is not the variance-mean regression, this paper does not benchmark non-stationary
fluctuation analysis, and nothing here should be read as a measured comparison against it.

Minimising the corrected standard error over the whole noise sweep, the channel number is unreachable
below about $17$ channels and the opening rate below about $52$ at this interval, while the unitary
current and the closing rate have no floor within the simulated range. % src: projects/eLife_2025/figures/paper_both/figure_7.html (reachability floors)

The evidence is thinner than the picture, and the subsection says so rather than leaving it to a
caption. The two boundaries at which least squares becomes honest, and the closure floor, are each
fitted through four measured crossings. The channel count at which the third region opens spans $120$
to $1368$ depending only on which criterion is used. The lower vertex is extrapolated to
$N_{\mathrm{ch}}$ between $3$ and $9$, below the simulated floor of ten channels, and the ten cells
that would pin it have not been run. All the boundaries are drawn at one acquisition interval, the
ribbons showing where each moves as the interval is swept from $0.01$ to $1$ in units of $\tau$. % src: projects/eLife_2025/figures/paper_both/figure_7.html (region 3 opens at N_ch 120-1368 over the two criteria; map closes from below at N_ch 3-9, extrapolated)

% PREPARATION FOOTPRINTS, held back 2026-08-05 pending one sourced number, and this is a hold and not
% a cut. The three boxes are drawn on Figure 7 and the caption names them. The argument they carry is
% the one that makes the map matter, that every common recording configuration except the oocyte
% falls where the boundary-conditioned likelihood is the one that reports honestly. What is missing
% is documentary and specific: the minimum resolvable current for two of the three configurations is
% a working value with no source (figure_7.Rmd:411-413), and the noise ranges are read off published
% traces by a procedure whose own write-up flags its biases
% (docs/bibliography/recording_configurations/TRACE_WIDTH_METHOD.md). The channel-count half of each
% footprint IS sourced. Two ways to put it back: source the two currents, or make the argument on the
% channel-count axis alone, which the data already support. Either restores about 90 words here.