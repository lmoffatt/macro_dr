\documentclass[9pt]{elife}

\usepackage{amsmath,amssymb,mathtools}
\usepackage{siunitx}
\usepackage{graphicx}
% Figures live in the shared figure tree and are regenerated there; point at the source so
% the draft always compiles against the current PDFs. Bundle them into the submission dir at
% submission time. Path is relative to this file (latexmk runs from manuscript-drafts/).
% Repointed 2026-07-28 from figures/paper/ (retired with the three-paper split; the directory
% is now figures/paper_1/ and does not hold the merged-roster figures) to figures/paper_both/,
% which is where figure_1/3/4/5/6 and every supplement of the merged paper are written.
\graphicspath{{../../../../projects/eLife_2025/figures/paper_both/}}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{tcolorbox}
\usepackage{bm}

% TITLE. Reviewed 2026-07-28 against the merged frame, in which the comparison anchor is least
% squares, the contribution is the validity criterion plus the map, and the conclusion is an
% ordering on the design plane rather than a threshold. Three candidates, live one first.
%
% (1) LIVE, below: names the biological system, states the two deliverables, promotes no
%     algorithm, carries no threshold and no "only".
% (2) The title this file carried until now: "Validity limits of time-averaged likelihoods for
%     macroscopic kinetic inference". It reads the failure as a consequence of time averaging,
%     which the results contradict (least squares averages nothing over the window and is the
%     worst-calibrated arm; the boundary-conditioned member averages and is calibrated), and
%     "limits" invites a threshold reading the map does not support. Restorable in one line.
% (3) "Honest uncertainty in kinetic inference from macroscopic ion-channel currents: a
%     validity criterion and a design map". Longer, and "honest" in a title may read as a
%     claim about other people's work rather than about a measured quantity.
\title{A validity criterion and a usage map for likelihoods of macroscopic ion-channel currents}

\author[1*]{Luciano Moffatt}
\affil[1]{Instituto de Qu\'imica F\'isica de los Materiales, Medio Ambiente y Energ\'ia (INQUIMAE), CONICET; Facultad de Ciencias Exactas y Naturales, Universidad de Buenos Aires, Ciudad de Buenos Aires, C1428EHA, Argentina}
\corr{lmoffatt@qi.fcen.uba.ar}

\begin{document}
\maketitle

% ABSTRACT, rewritten 2026-07-28 in the merged frame (201 -> 200 words). Slots, in order:
% object and how it is measured; the limitation; "here we test X against Y"; the criterion;
% the results, the last of which carries the map; the closing statement. The clause "including
% Bayesian recursive filters" is the position against munch2022bayesian, a published Bayesian
% Kalman filter for this problem in this journal: what is offered here is the test of whether
% such a filter tells the truth about its own uncertainty and the map of where it does not.
% eLife wants the biological system in the title or the abstract; it is in both.
% No channel-number threshold is quoted, per SPINE §7: the data support none, so the few-channel
% departure is stated as a direction. If a number is ever wanted here, it is the measured
% reachability floor (N_ch below 17 for the channel count, below 52 for the opening rate,
% figure_6.html), not "about 100".
\begin{abstract}
Kinetic schemes are usually fitted to macroscopic ion-channel currents by least squares on the deterministic mean, which discards the fluctuations. Likelihoods that use the fluctuations exist, including Bayesian recursive filters, and have almost no uptake, because whether such a filter reports its own uncertainty correctly was never characterised. Here we test a ladder of macroscopic likelihoods, from least squares to a filter conditioned on both ends of the acquisition interval, against data simulated exactly from the process they approximate. At the true parameters the score must average to zero and its covariance must equal the Fisher information reported; the mismatch is measurable. A likelihood that models the gating variance but carries no recursion is calibrated interval by interval and under-reports parameter uncertainty ten to fifteen fold once a record accumulates; a sandwich correction restores nominal coverage. % src: papers/1_method/decisions/D-4_ranking_verdict.md §1a and §3 (NR ellipse-area factor 10 to 15; sandwich returns every rung to 0.87-1.07) ; projects/eLife_2025/figures/paper_both/figure_3.html (per-interval identity holds, accumulated ratio does not)
% TODO-SIGN: direction convention as fixed in Diagnostics; if the pending code read reverses it, flip every TODO-SIGN site in one pass.
The boundary-conditioned filter reports honestly at 94\% of the measured points, departing toward few channels. % src: papers/1_method/decisions/recompute/d4_distortion_envelopes.py re-run over 1c2ae6f + 87889e6 + 0ffbda7 (1977 of 2100 points within 15%)
The region where the fluctuations still determine the amplitude parameters and the region where the classical error bar is honest never overlap, lying one to three decades of noise apart. % src: projects/eLife_2025/figures/paper_both/figure_6.html (crossing table and fitted log-log slopes, Delta.k_off = 0.1)
This supplies a test of whether a macroscopic-current filter tells the truth about its own uncertainty, and a map of where it does not.
\end{abstract}


\begin{abstract}
S1: Variables biologicas resueltas en el tiempo suelen 


Kinetic schemes are usually fitted to macroscopic ion-channel currents by least squares on the deterministic mean, which discards the fluctuations. Likelihoods that use the fluctuations exist, including Bayesian recursive filters, and have almost no uptake, because whether such a filter reports its own uncertainty correctly was never characterised. Here we test a ladder of macroscopic likelihoods, from least squares to a filter conditioned on both ends of the acquisition interval, against data simulated exactly from the process they approximate. At the true parameters the score must average to zero and its covariance must equal the Fisher information reported; the mismatch is measurable. A likelihood that models the gating variance but carries no recursion is calibrated interval by interval and under-reports parameter uncertainty ten to fifteen fold once a record accumulates; a sandwich correction restores nominal coverage. % src: papers/1_method/decisions/D-4_ranking_verdict.md §1a and §3 (NR ellipse-area factor 10 to 15; sandwich returns every rung to 0.87-1.07) ; projects/eLife_2025/figures/paper_both/figure_3.html (per-interval identity holds, accumulated ratio does not)
% TODO-SIGN: direction convention as fixed in Diagnostics; if the pending code read reverses it, flip every TODO-SIGN site in one pass.
The boundary-conditioned filter reports honestly at 94\% of the measured points, departing toward few channels. % src: papers/1_method/decisions/recompute/d4_distortion_envelopes.py re-run over 1c2ae6f + 87889e6 + 0ffbda7 (1977 of 2100 points within 15%)
The region where the fluctuations still determine the amplitude parameters and the region where the classical error bar is honest never overlap, lying one to three decades of noise apart. % src: projects/eLife_2025/figures/paper_both/figure_6.html (crossing table and fitted log-log slopes, Delta.k_off = 0.1)
This supplies a test of whether a macroscopic-current filter tells the truth about its own uncertainty, and a map of where it does not.
\end{abstract}



% Impact statement (eLife requires one, <= 40 words). Third person, no "We show", no threshold,
% no algorithm promotion. 30 words.
% Testing macroscopic ion-channel likelihoods against exactly simulated data measures how far each misreports its own uncertainty, and maps where on the design plane its reported interval can be believed.

% ================= SKELETON: titles only below. Fill-hints in % comments. =================
% One file per top-level section under sections/, so that one section = one owner = no merge
% conflicts (01_writing_plan.md, T-1). The fill-hints live in the section files, with the
% section they brief. Add nothing here but \input lines.

\input{sections/01_introduction}
\input{sections/02_theory}
\input{sections/03_diagnostics}
\input{sections/04_results}
\input{sections/05_discussion}
\input{sections/06_methods}
\input{sections/07_backmatter}

% Repointed 2026-07-28 from biblio.bib (15 entries, which broke on the first prior-art citation)
% to biblio_full.bib (124 entries), verified a strict superset of biblio.bib and carrying every
% key the sections cite. No entries were copied across; this was a one-line repoint.
% \nocite{*}
\bibliography{biblio_full}

\end{document}
