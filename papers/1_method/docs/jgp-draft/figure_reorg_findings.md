# Revisión de economía de paneles (workflow figure-reorg) — 2026-09-03

Fuente: 6 agentes mirando el arte renderizado + captions + prosa + fuentes R. Salida completa:
tasks/wqclyx0hi.output de la sesión. Veredicto global: ninguna figura sobra; nada de fusiones
grandes; podas puntuales y dos reordenamientos. Por familia:

## clouds
Keep the family as two separate figures. Figure 2 is intrinsically dense by design, not by supplement pressure: its per-pair distortion numbers and the QQ's all-directions coverage answer different questions, on different cells and members, so no panel wants to migrate and a merge would push a 7.0x6.8-in fullwidth figure past JGP's one-figure-one-page limit while recreating the eLife packing the promotion just relieved. The real work in this family is promotion-grade finishing of the QQ figure (new Fig 3): it still looks like a supplement (baked-in art title duplicating the caption, ~0.9 in dead canvas, strip labels 'Delta 0.05/Delta 1' unglossed, and 'noise 0.05' in the art vs S-tilde=0.005 in the caption, the known x10 label-scale clash). Optionally, its own Rmd already plans the widening that would let it earn body status: small multiples over LSE/NR/R/IR at existing 100-fit cells. One vestige in the parent: a lone panel tag 'A' survives from the retired panel B and the caption never references it.

- [recommended] **keep**: Keep Figure 2 and the QQ as two separate body figures; do not merge and do not migrate the QQ's coverage panels into Figure 2. Also keep Figure 2's six blank placeholder panels: they state a real fact (LSE/ILSE do not estimate i or sigma), keep the 8 columns aligned across rows, and a ragged patchwork grid is exactly the layout-trap territory the project has already paid for.
  - beneficio: Figure 2 stays at its one-page ceiling (7.0x6.8 in + 150-word caption); the QQ's different cell, member and group size would read as incommensurate inside the parent; the promotion to 18 body figures removes the only reason to pack them.
  - costo: None; no R sources touched.
- [recommended] **simplify**: Promotion-grade finish for Figure_2_S1 (new Fig 3): (1) drop the baked-in art title, the LaTeX caption owns it at JGP; (2) trim the ~0.9 in dead right canvas or recenter to content width (~4.7 in); (3) reconcile notation with the caption: 'noise 0.05' is the x10 label scale where the caption says S-tilde=0.005, and the 'Delta 0.05 / Delta 1' strips should use the manuscript's Delta-hat symbol or be glossed in the caption, which currently never explains why there are two panels.
  - beneficio: The one former supplement in this family that visibly still looks like a supplement reads as an ordinary body figure; removes a notation clash a referee or copyeditor would query.
  - costo: figure_2_S1.Rmd only (title line, canvas width, strip labeller) + one caption sentence in 04_results.tex; re-render.
- [recommended] **simplify**: Remove the vestigial panel tag 'A' from Figure 2 (figure_2.Rmd:524, labs(tag="A")). Panel B was retired when the IR corner moved out; the current caption references no panels and a lone '(A)' invites a production query.
  - beneficio: Removes an internal inconsistency at zero design cost.
  - costo: One line in figure_2.Rmd; re-render (the layout arithmetic is unaffected: the tag row's height is measured, not assumed).
- [optional] **simplify**: Widen the QQ to small multiples over members (LSE/NR/R/IR at their existing 10000-recording cells, 100 fits each), the increment figure_2_S1.Rmd's own header already specifies, stating INR's absence. Alternatively or additionally, restack the two (or more) panels to one column for JGP's preferred 85 mm width at full font size.
  - beneficio: As a body figure its 'IR alone' limitation is now more visible; member multiples turn a one-member spot check into a ladder-wide coverage test (binomial error 2.2% at 100 fits, still far inside the 91-vs-95 gap), and a stacked layout hits 85 mm without shrinking type.
  - costo: Real rework of figure_2_S1.Rmd (data loading across member clouds, faceting, annotation placement), caption rewrite, and the figure's claim weight changes, an author-level decision; the coverage numbers per member must be re-read, not assumed.
- [recommended] **keep**: No panel of Figure 2 was compressed by the supplement regime and none should change under the new one: the magnitude/anisotropy/corrected numbers feed the prose's ladder-ordering and 'no single rescaling serves' claims per pair, which the QQ (one member, six directions collapsed to a scalar) cannot carry. The only JGP-revision item is arithmetic, not design: re-derive the 7 pt font floor for JGP's include scale (~1.2% stretch vs eLife's 6.1%) before touching anything, per the fonts-first rule.
  - beneficio: Avoids gratuitous redesign of a hand-optimized figure whose density is load-bearing.
  - costo: None now; if the font floor moves at revision, figure_2.Rmd's measured layout recomputes itself from the new non-panel space.

## time
Coherent family with a clear division of labor: the parent (new Fig 4) is an 8-member x 7-row calibration checklist on two parameters, and S1/S2 (new Figs 5-6) are the per-step information deep dive on all four parameters over 7 members. Redundancy is limited and mostly purposeful (parent rows C and E are compressed subsets of Figs 5-6, kept in-figure because the E-times-G-equals-F factorisation must be readable on one page). The one action I recommend is the merge the author already floated: S1 and S2 are never cited separately (04_results.tex:884, 05_discussion.tex:90, 06_methods.tex:729 all cite the pair jointly), and the family's washout claim, that k_on and N_ch collapse to the floor while k_off and i survive, currently straddles the two PDFs (k_on in S1, N_ch in S2), so the two-pairs contrast is invisible in either figure alone. The merge is mechanically cheap because one Rmd already draws both halves in one loop, but it must be redrawn at about 7.0 x 8.8 in, not the pre-split 7.0 x 11 in, to respect JGP's one-page cap; info-row height drops from ~1.8 to ~1.3 in, still twice the parent's row height. A small labeling repair should ride along: the S1/S2 least-squares column is the interval-averaged arm (digest LSE = av1 = ILSE, figure_3.Rmd:143) but its header prints LSE, which as a peer body figure now contradicts Figure 4's LSE/ILSE distinction two pages earlier. The parent itself I would keep as is: it is very dense (56 panel slots) but it is a small-multiples grid, authored at 7pt in a 7.0 in = ~178 mm width that lands on JGP two-column with essentially no scaling, fits one page with its caption (the tex comments show the fit was measured to the point), and every row anchors a dedicated Results paragraph, so no row is free to drop without prose surgery. Note for the port: Figure_3_S3.pdf (noise band) was vetoed 2026-09-03 (L-REVIEW comment at 04_results.tex:878-880) and is outside this family and the 18-figure roster; its Rmd and caption md still sit in the directory.

- [recommended] **merge**: Merge Figure_3_S1 and Figure_3_S2 back into one four-band body figure (the author already floated this), redrawn at ~7.0 x 8.8 in (JGP two-column width, under the one-page cap including its caption), tags A-H, one column-header block and one legend. Mechanically near-trivial: figure_3_S1_S2.Rmd already builds both halves in one loop over a HALVES list (lines 348-367); collapse it to one entry idx = 1:4 and change the ggsave size. Do NOT resurrect the pre-split 7.0 x 11 in form: it exceeds a JGP page and rescaling it is exactly the 4.8pt-font failure that forced the 2026-08-05 split under eLife.
  - beneficio: The pair is never cited separately (all three citations are joint: 04_results.tex:884, 05_discussion.tex:90, 06_methods.tex:729), and the family's washout claim, k_on + N_ch collapse to the floor while k_off + i survive, currently requires flipping between two figures because the pairing crosses the split (k_on in S1, N_ch in S2). One visual field restores the four-parameter contrast the layout was designed for (the pre-split figure was exactly this). Also drops the figure count 18 to 17, removes a duplicated header block, legend and near-identical caption preamble, and retires the S2 caption's 'other half of the preceding supplement' scaffolding.
  - costo: R sources: figure_3_S1_S2.Rmd only (HALVES list, ggsave dimensions, tag vectors; keys and digests untouched). Tex: fuse the two captions at 04_results.tex:575-585 into one (their shared reading rules merge cleanly; the per-figure sentences about the LSE ratio row and the six-interval floor were split copies of one original). Info-row height falls from ~1.8 to ~1.3 in, a visible but safe compression (parent rows are ~0.6 in and legible; fonts are authored in points so nothing shrinks). Downstream renumbering 6-18 to 6-17 is free if done before the port freezes numbers, expensive after.
- [recommended] **simplify**: Relabel the least-squares column of S1/S2 (or the merged figure) from 'LSE / Least Squares' to 'ILSE / Interval Least Squares'. The digest it draws (figure_3_digest_LSE.rds) is averaging_approximation = 1, which the parent maps to the ILSE column (DTOK at figure_3.Rmd:143 assigns digest 'LSE' to ILSE and 'LSE_av0' to LSE). Cosmetic label change in ALAB/FULL at figure_3_S1_S2.Rmd:113-116; internal keys, EXCL and printed caption-chunk numbers stay as they are. Then trim the caption's compensating clause 'the least-squares arm being the interval-averaged ILSE' to a plain statement.
  - beneficio: Under eLife these were second-class supplements and a caption disclaimer sufficed; as body Figures 5-6 sitting right after Figure 4, the same data column carries two different member names two pages apart, and a reader comparing the LSE column of Fig 4 (instantaneous arm, r-squared pinned at 1, median ratio +0.184 attributed to 'ILSE' in the s1 caption) with the 'LSE' column of Figs 5-6 is being invited to a false match.
  - costo: One R source (figure_3_S1_S2.Rmd, two label vectors and the knit-time cat messages), one caption sentence in 04_results.tex, and a check that the caption-numbers chunk prose quoting 'LSE' medians is reworded consistently. No data or layout change.
- [recommended] **keep**: Keep the parent Figure_3 (new Fig 4) as is: 8 columns x 7 rows, 7.0 x 5.89 in, two-column at JGP.
  - beneficio: Every row anchors a dedicated Results paragraph and the figure's argument is the cascade itself, read top to bottom as a checklist with the null value on each row; the E/F adjacency carries the central cancellation claim (R reaching 1.07 from two opposite errors) and the D/F pair carries the factorial reading of the two lattice switches. The art lands on JGP two-column width with at most ~2% scaling so the measured 7pt font floor survives untouched, and figure plus caption fit one page. Redesign here would force rewriting a section whose prose was compressed around this exact row list through four documented passes.
  - costo: None now; the figure remains at the dense end and permanently rules out single-column presentation, and any future row addition (e.g. restoring the vetoed noise band) would break the one-page fit.
- [optional] **drop-panel**: If the author wants air in the parent under the new regime: drop row C (per-step information, the only row tested against no reference), whose content is a strict subset of the promoted Figs 5-6 upper rows (which add the J_t overlay and the k_on and i bands), and let the caption define the informative-interval mask verbally against those figures.
  - beneficio: One row (~0.6 in plus its caption item) back for the six rows that carry tests; eliminates the largest parent-vs-supplement duplication, which only existed because supplements were second-class and the body needed a self-contained information row.
  - costo: Real prose surgery, which is why this is optional and I lean keep: 'Row (C) alone says where each parameter is measured' (04_results.tex:881) must re-anchor to Figs 5-6; the caption's 'curves stop where the information reaches the numerical floor' loses its visible referent, and rows D/E are drawn only over that mask, so the parent would apply a mask a reader cannot see in it. R sources: figure_3.Rmd (row roster, patchwork heights, caption-numbers chunk) plus the caption at 04_results.tex:485.

## plane
The five-figure family is coherent, not redundant: parent = verdict (bias + total distortion), S1 = mechanism (the total factored into per-sample and temporal parts, exact in log-det), S2 = same verdict for the recursive rungs, S3 = invariant summaries (magnitude/anisotropy) that bridge to Figures 12 and 16, S4 = the achieved (distortion-corrected) standard error that feeds Figure 18's boundaries. The apparent overlaps are load-bearing (S1's factors multiply to parent B; S2's R/IR columns are the within-figure baseline and target for the MR/VR trade; cross-page colour comparison would not survive their removal). The one genuine economy the promotion enables is in the parent: its white-on-dark-casing contour overlay of the corrected standard error duplicates, as level sets, exactly the field S4 now shows as a first-class body figure; it is the parent's second line code, costs a legend row, and lays dash traffic across otherwise-clean panels (LSE/ILSE/INR/IR bias half). It is also a one-flag change (se_lines in fig4_render, already FALSE in S3/S4). Order 7-11 is already first-citation order (lines 836/1050/1085/1643) and should not change; what needs repair is that Fig 11's only Results citation sits in a subsection AFTER the memory family (line 1643 vs 1463+), breaking numeric citation order under the new all-body numbering - fixed by one early citing clause in the plane subsection (natural if the parent sheds the SE overlay and defers 'can it be measured at all' to Fig 11) or by moving the 'Sampling a hundred times faster' subsection up. Outside-family flag for the coordinator: fig:limit-s2 (new Fig 17) is first cited at line 1068, inside the plane subsection, before Figs 11-16. At revision, none of these figures can live at 85 mm; all five are 7.4 in fullwidth two-column one-page figures, and JGP's ~6.9 in measure means a ~7% shrink that takes the 7 pt base font below floor, so a re-render at JGP width via the existing per_row calibration (noted in figure_4_layout.R; tmp/calib_fig4.R + measure_fig4.py) is expected for all five - fonts set to the JGP floor first, layout re-solved from what remains.

- [recommended] **simplify**: Parent (new Fig 7) sheds the distortion-corrected-SE contour overlay (the white-lines-on-dark-casing code) now that the SE field is first-class as Fig 11: pass se_lines = FALSE in figure_4.Rmd's fig4_render call (S3/S4 already do), drop the se_title key row, and cut the caption's 'white lines on a dark casing' clause, replacing it with one sentence deferring 'whether the parameter can be measured at all' to Figure 11. Decide consistency for S1/S2, which default to se_lines = TRUE (figure_4_S1.Rmd, figure_4_S2.Rmd): flip both for family consistency, or keep them if the author wants the measurability context on the mechanism figures. No prose outside the captions reads these lines (verified: only the parent caption and the usage figure's own, separately drawn, lines mention the casing code).
  - beneficio: Removes the second line code and a legend row from the most loaded figure in the paper; the bias half's clean members (LSE, ILSE, INR, IR) stop carrying horizontal dash traffic that currently makes them look busier than their result; buys legibility headroom for the ~7% shrink to JGP's two-column measure; and the new deferring sentence simultaneously gives Fig 11 the early citation it needs. Counterargument the author may weigh: the overlay also warns where colour should not be over-read because the parameter is unmeasurable anyway, and he added it deliberately.
  - costo: figure_4.Rmd (one flag plus its legend/key chunk and build note), caption edit in 04_results.tex; optionally figure_4_S1.Rmd and figure_4_S2.Rmd (same flag) and their captions' 'same two line families / same two criteria' phrasing. figure_4_layout.R and figure_4_common.R untouched.
- [recommended] **keep**: Keep S1 (Fig 8) exactly as is, and keep parent panel B alongside it: total is the verdict, the factors are the mechanism, and the factorisation being exact only in the log-determinant means neither view substitutes for the other. Do not merge into a three-block figure.
  - beneficio: Preserves the NR-vs-INR reading (per-sample, not temporal, failure) and the Discussion's two anchor citations at zero cost.
  - costo: None (a merge would need 30 columns, below the measured 1.4 mm cell floor).
- [recommended] **keep**: Keep S2 (Fig 9) with its duplicated R and IR columns; they are the within-figure baseline and target without which the MR/VR trade (shave the first moment, give it back in the second) cannot be read on one page.
  - beneficio: Colour-field comparison stays within one figure; the parent stays at 20 columns.
  - costo: None; the alternative (28-column merged parent) fails the author's own cell-size measurement.
- [optional] **keep**: Keep S3 (Fig 10), but it is the family's weakest member if a display item must go at revision: one citing sentence (line 1085), and panel A is parent B pooled over directions. If JGP pressure ever forces a cut, the LSE-conservative reading could move to a parent-B annotation and the m/a bridge could lean on Fig 12's caption; until then, keep.
  - beneficio: Keeping costs nothing now; knowing it is the marginal figure makes any future triage fast.
  - costo: A cut would touch figure_4_S3.Rmd (retire), 04_results.tex (line 1085 sentence and caption), and Fig 12/16 captions that lean on the plane-wide m/a context.
- [recommended] **keep**: Keep S4 (Fig 11) unchanged as art, but repair its citation order: its first Results citation (line 1643, its own subsection between the memory and limit sections) lands after the memory family's first citations (1463+), which breaks cite-in-numeric-order once all figures are body figures. Cheapest fix is one early citing clause in the plane subsection (falls out of the parent-shed proposal for free); alternative is moving the 'Sampling a hundred times faster' subsection to directly follow the plane subsection, which its content permits (its tau_9 reference to fig:memory becomes a forward citation of Fig 12, still in order). Related outside-family flag for the coordinator: fig:limit-s2 (Fig 17) is first cited at line 1068 inside the plane subsection, ahead of Figs 11-16.
  - beneficio: JGP compliance without renumbering; the figure itself needs no re-render for this.
  - costo: 04_results.tex prose only; no R sources.
- [optional] **simplify**: Revision-stage family-wide re-render at JGP's two-column measure: all five are 7.4 in fullwidth, and the ~7% shrink to ~6.9 in takes the 7 pt base type below the legibility floor. Per the author's own rule, fix fonts to JGP's floor first and re-solve each figure's per_row from the remaining space using the calibration loop recorded in figure_4_layout.R's comments (tmp/calib_fig4.R + tmp/measure_fig4.py); PER_ROW is per-figure, so each of the five Rmds is touched once. Shedding the parent's SE overlay first makes its re-solve easier.
  - beneficio: Meets the one-page/print-legibility constraint without changing any figure's content or grammar; 85 mm single-column is not attainable for this grammar and should not be attempted.
  - costo: figure_4.Rmd, figure_4_S1.Rmd, figure_4_S2.Rmd, figure_4_S3.Rmd, figure_4_S4.Rmd (per_row and page-size headers; shared figure_4_layout.R only if JGP's exact measure differs from the hardcoded 7.4), plus render_figure_4_set.sh rerun and a caption pass for any size-dependent statements.

## memory
The memory family is already in good shape for JGP and needs no panel surgery. The three figures do three distinct jobs with the same 210-cell distortion quantity on three different horizontal coordinates: Figure 12 (Figure_5.pdf) is the decision plot (distortion vs 1/tau9 of the classical residual), Figure 13 (Figure_5_S1.pdf) is the license (noise and channel count act only through r = S/Nch, drawn twice to show non-collapse vs collapse), Figure 14 (Figure_5_S2.pdf) is the mechanism (each member against its own residual, with NR as the necessary-not-sufficient counterexample that backs an abstract claim). Overlaps are by design, not redundancy. All three PDFs are 504 pt = 177.8 mm wide, under JGP's 180 mm two-column, and the tallest (Fig 13, 198 mm) is well under one page, so no re-rendering is needed at revision; none of the three can plausibly reach 85 mm single-column and should not try. The parent keeps all four panels: they are the factorial of the two design axes, R being the only recursion-without-averaging member, and the prose quotes all four sweep maxima (57, 33, 1.7, 1.08). On placement: keep the collapse AFTER the parent (13 after 12). The real issue the promotion exposes is citation order under flat numbering: the running text currently cites the collapse (04_results.tex ~line 1463) before the parent (~1476), and a 2026-09-03 sentence cites fig:limit-s1 (new Figure 16) at ~1474 ahead of Figures 14 and 15. Both are tex-only fixes; swapping 12/13 would also cure the first but would lead the family with a 10-panel control figure and demote the diagnostic that the Discussion, the abstract chain, and the interval-cost subsection all lean on. Producers: figure_5.Rmd, figure_5_S1.Rmd, figure_5_S2.Rmd in projects/eLife_2025/figures/paper_both/ (note the 2026-08-27 producer/output swap: figure_5_S1.Rmd writes the collapse). Rejected encodings live in figures/archive/ (figure_5_Nch.Rmd, figure_5_noise.Rmd, figure_5_variant_2members.Rmd, figure_5_variant_8members.Rmd), evidence the layout space was already explored. Figure_5_S3 was dropped 2026-08-26 and stays dropped.

- [recommended] **keep**: Parent Figure 12 keeps all four panels and the single shared y. The four are the factorial of the two family axes; R is the only recursion-without-averaging member and isolates which axis does the repair; the prose quotes all four sweep maxima (ILSE 57, INR 33, R 1.7, IR 1.08).
  - beneficio: Zero redesign cost; the producer-header arguments (04_results.tex:1289-1317) already answer every trim one could propose, including why the near-empty R/IR panels stay.
  - costo: None. No R sources touched.
- [recommended] **keep**: Keep the collapse figure AFTER the parent (13 after 12), and fix citation order in the text instead: the first running-text citation of the collapse (04_results.tex ~1463) precedes the parent's (~1476). Move the parent's introducing sentence ('Figure ref{fig:memory} plots each likelihood's distortion against its inverse...') to the head of that paragraph so 12 is cited first. Swapping the numbers would also restore order but would lead the family with a 10-panel control figure and demote the diagnostic that the Discussion, the abstract chain, and results ~1666 cite.
  - beneficio: JGP in-order first-citation compliance with the decision figure leading the family; a reader paging through meets the payoff before its license, and the parent's caption forward-ref to Figure 13 remains legitimate.
  - costo: 04_results.tex only (one sentence moved within the opening paragraph of the subsection). No R sources.
- [recommended] **simplify**: Resolve the forward reference to fig:limit-s1 (new Figure 16) in the 2026-09-03 L-REVIEW sentence at 04_results.tex ~1474: under flat numbering it is cited before Figures 14 and 15 ever are. Carry the point in words ('along the product Nch*S, where single openings resolve') and drop the parenthetical ref, letting the limit family's own subsection introduce Figure 16.
  - beneficio: Removes a flat-numbering citation-order violation the eLife supplement convention used to absorb; the sentence loses nothing since the claim is restated where Figure 16 is introduced.
  - costo: 04_results.tex, one parenthetical. No R sources. Cross-family: worth coordinating with the limit-family reviewer before editing.
- [optional] **simplify**: Figure 14, NR panel: clip or grey the single ill-conditioned corner cell (Nch 1e4, interval 1) that drags the log axis to 1e7, mirroring the greying rule Figure 7 (plane) already owns, with one caption clause naming the exclusion.
  - beneficio: NR's real 6.7-99 structure, and the white-residual-yet-distorted cells at the coarse intervals that the abstract leans on, currently occupy the bottom fifth of the panel; clipping makes the load-bearing counterexample visible at print size.
  - costo: figure_5_S2.Rmd (scale clip or grey rule) plus one caption sentence in 04_results.tex. Needs author sign-off: the outlier is honest and he may prefer it shown.
- [taste] **simplify**: Figure 12 refactored to a 2x2 grid (rows = recursion, columns = averaging) to reach JGP's preferred 85 mm or a 127 mm 1.5-column, drawing the factorial literally.
  - beneficio: Hits single-column width; the 2x2 design of the member roster becomes visual rather than stated.
  - costo: figure_5.Rmd relayout, and it breaks the shared-baseline single-row comparison the producer header explicitly defends (vertical distance between members at the same x is the figure's delivered quantity); fonts-first arithmetic must be redone from the JGP floor. The encoding space was already explored and rejected once (figures/archive/figure_5_Nch.Rmd, figure_5_noise.Rmd, figure_5_variant_*.Rmd). Not advised unless JGP production pushes for single-column; at 177.8 mm the figure already fits two-column with no shrink.

## limit
Sound family, wrong order. Content-wise all three figures earn their place and the apparent 15/16 redundancy dissolves on inspection: 15 (corner zoom) is per-parameter, pooled-optimum, design-plane oriented, and carries the B-times-C decomposition and the two-sided departure (k_off error bar 1.62 too small while N_ch is 0.65 too large in the same cell), none of which the direction-aggregated one-law figure can show; 16 (one law) is all 560 cells at simulation truth collapsed onto x = N_ch*S~*sqrt(Delta~), and its constants are what the Discussion (05_discussion.tex:55), Figure 7's caption extrapolation and the cover letter quote; 17 (traces) is the only raw-record display and the physical picture of the macro/micro frontier. The real defect is ordering: first prose mentions run 17 (results.tex:1068, inside the PLANE subsection, before fig:plane-s3 at 1085), then 16 (line 1474, memory subsection), then 15 (line 1714), the exact reverse of the assigned 15/16/17, violating JGP's first-mention-equals-numeric-order rule that the jgp adaptation plan already flags. So yes, the traces figure should come earlier, and the manuscript itself says so; but NOT as part of 15, which is already 72 mini-facets at eLife's 7.4 in hard max with a hand-tuned patchwork (and will already shrink a few percent below its 7 pt font floor at JGP print width). Recommended shape: reorder the family to traces -> corner zoom -> one law (art untouched, tex-only edit), and decide explicitly whether the line-1068 citation moves or the traces figure takes an even earlier number near the plane family. Hold one contingency for 15 at revision: if JGP's two-column width pushes fonts below floor, drop block A (recoverable as B*C, stated at 1% in the caption) or the flat noise=1 row.

- [recommended] **migrate-panel**: Reorder the family to traces -> corner zoom -> one law (new 15 = current Figure_6_S2, new 16 = current Figure_6, new 17 = current Figure_6_S1, or 16/17 swapped to match first mentions exactly: s2 then s1 then limit). Rewrite the fig:limit subsection (results.tex:1714-1739) to open on the physical picture (the record begins to resolve single openings), then the measured departure, then the law; and decide the line-1068 citation: either leave it as the figure's legitimate first mention (which argues for numbering traces even earlier, near the plane family) or soften it so the family stays contiguous. Art untouched in all three PDFs.
  - beneficio: Satisfies JGP's first-mention-equals-numeric-order requirement, which the current 15/16/17 assignment violates in exact reverse (first mentions at lines 1068, 1474, 1714); puts the cover letter's frontier picture where a reader meets it; the subsection gains the natural record -> measurement -> law arc.
  - costo: Zero R sources. 04_results.tex only: move two figure environments, rewrite ~3 sentences of the subsection opener and the closing 'What these cells look like' sentence (1739), optionally relocate the traces caption's law-extrapolation clause; update pointers in Figure_6_caption.md comments; covered by the already-planned A2 renumber pass.
- [recommended] **keep**: Do NOT merge the traces figure into Figure 15 (the corner zoom), despite the temptation to put the physical picture beside the measurement. 15 is already 72 mini-facets at 7.4 in fullwidth with a hand-tuned patchwork (measured YW constant, sub-block seams, spacer widths in figure_6.Rmd:70-176), and the traces come from a different pipeline (uniformization simulator CSVs + ir_reliability digest vs figure_4_common.R bootstrap stats).
  - beneficio: Preserves two legible figures and two independent, already-frozen producers; avoids re-opening the patchwork layout traps; keeps 15 inside JGP's one-page limit.
  - costo: None (this is the recommendation against a change). The rejected merge would cost: fusing figure_6.Rmd and figure_6_traces.R, data-path plumbing, full re-budget of the fullwidth height, and a caption rewrite, for a figure that would exceed one page or crush the trace panels.
- [optional] **simplify**: Contingency for Figure 15 at JGP print width only: if the two-column width forces the 7 pt floor under (the figure was built to eLife's 7.4 in), drop block A (the caption already states A = B x C to within 1%, so it is recoverable) or drop the flat noise = 1 row (its message is one caption sentence, 'the whole grid is flat at S~ = 1'). Either buys 30-50% more area per remaining facet.
  - beneficio: Keeps every glyph at or above the font floor at JGP's narrower print width without redesigning the grid; the flat rows/columns currently spend ink saying 'nothing happens here'.
  - costo: figure_6.Rmd (CMP or NZ constants, YLIM recompute, fig.width/height, ggsave line), the Figure 6 caption in 04_results.tex plus Figure_6_caption.md, and re-anchoring the prose totals at results.tex:1717-1719 if A goes (the 1.62/0.65 numbers would then be sourced from data rather than a visible panel). Do not execute preemptively; measure the shortfall at revision first.
- [taste] **keep**: Figure 16 (Figure_6_S1) unchanged: four panels, each carrying a cited claim (a and m the law pair, C the Discussion's 1.8/1.3 headline, D the 290-recordings pooling budget). If an 85 mm single-column slot is ever wanted, restack the 2x2 patchwork to 4x1 (figure_6_S1.R lines 88-89 only), but the default is the 1.5- or 2-column slot as drawn.
  - beneficio: No risk to the most-cited figure of the family; the m/a (no D_AI) design was the author's explicit 2026-08-29 decision and the quantile-not-envelope choice is documented in the file header as referee-checkable.
  - costo: Zero as-is; the optional 4x1 restack touches figure_6_S1.R (patchwork composition and ggsave dimensions) and nothing else.
- [taste] **keep**: Figure 17 (Figure_6_S2) art unchanged; only its position moves (proposal 1). If an 85 mm single-column slot is wanted, restack from 3 columns x 2 rows to 2 x 3 (columns = N_ch, rows = S~) via the NCHS/STS/ncol lines of figure_6_traces.R; the script already parametrizes the grid (VARIANT/DHAT), so this is a small edit.
  - beneficio: The six panels each do a distinct job (below-floor extrapolation column, the sweep's own corner, the no-visible-step-still-departs cell); nothing to cut, and per-panel free y scales already protect the corner from the noisy column.
  - costo: Zero as-is; the optional restack touches figure_6_traces.R only (facet ncol, ggsave width/height, x-break vector).

## singles
Healthy family, both members keep. The two singles are the paper's bookends: Figure 1 fixes the member vocabulary in pictures before any measurement, Figure 18 converts all the measurements into a usage decision. They share nothing and overlap with nothing (the map's boundaries are re-derived in body Figures 11 and 16, which the no-supplement regime has now made first-class, strengthening 18's self-containment rather than creating redundancy). Figure 1's figure-vs-table question was litigated by the author twice (2026-08-11 carve decision; T12 2026-08-27, both on record in 02_framework.tex comments) and the promotion of supplements does not reopen it: the roster-as-table job already belongs to Table 1, and Figure 1 carries only what a table cannot (IR's paired occupancies with the covariance arc, interval bands, innovation bars, the absent posterior of the open-loop columns). Figure 18 is self-contained as closer: region meanings, criteria, and the IR/ILSE gloss are printed in its own key; its one outward pointer (the hatched corner needs Figure 15's correctable-1.5 reading) is handled in caption and text. JGP practicality: neither survives 85 mm single-column (Figure 1 has four panel columns; Figure 18 sits at the 7 pt font floor already), but both are natural two-column figures at ~7 in and each fits one page with margin to spare, so claim two-column width for both at revision and nothing else changes. No panel should migrate into this family from any other.

- [recommended] **keep**: Figure 1 stays a figure, unchanged, as new-manuscript Figure 1. Do not convert to a table and do not merge with Table 1: the table job (8-member roster, three-name map) is Table 1's, and the figure carries exactly what a table cannot (the IR covariance arc between boundary occupancies, the widening interval bands, the innovation bars, the structurally absent posterior in the open-loop columns). The author litigated the keep twice (carve decision 3, 2026-08-11; T12, 2026-08-27) and the no-supplement regime changes nothing for it, since it was never packed under eLife pressure; it was actually loosened (6 columns cut to 4 in 2026-08-12). At JGP revision, claim two-column width; the 4-column grid cannot survive 85 mm.
  - beneficio: Preserves the only image of the filter cycle the reader sees, with a settled caption at ~275 words; zero rework.
  - costo: None now. At revision only a width declaration; no touch to figure_1.Rmd or figure_1_panels.R.
- [recommended] **keep**: Figure 18 (Figure_7_hatch) stays a single-panel closer, unchanged. Do not migrate any panel into it (candidates considered and rejected: Figure 16's limit-law panels, Figure 11's corrected-SE blocks; both are now first-class body figures the map can simply cite, and either would double the closer's size and break the one-map reading). The hatch-over-fill variant remains the right drawing (the purple-override sibling erases what is recoverable in the corner). At JGP revision, claim two-column width and one page; both hold as-is at 7.0 x 5.9 in with the 7 pt floor already enforced.
  - beneficio: The synthesis map remains one self-contained decision surface, with its promoted supporting figures (11, 15, 16) now in the body backing every boundary.
  - costo: None; no touch to figure_7.Rmd.
- [taste] **simplify**: Fix the literal 'N_ch' in Figure 18's embedded key: figure_7.Rmd:1243 draws the label 'ILSE holds i fixed and cannot separate N_ch from it' as plain text with an underscore, while the panel's own x-axis renders a true subscript. Change the label to plotmath (or the same device the axis uses) on the next knit; both siblings (Figure_7 and Figure_7_hatch) are rewritten by the same chunk so the fix propagates automatically.
  - beneficio: Removes the one typographic inconsistency inside the closer, cheap to bundle with any future knit; JGP production staff notice exactly this kind of thing.
  - costo: One line in figure_7.Rmd plus a re-knit (author compiles; do not run R).
